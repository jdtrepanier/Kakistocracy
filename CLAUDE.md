# CLAUDE.md — Kakistocracy: Survive the Term

Instructions for AI coding assistants (and humans) working on this repo.
The full game design is in **[docs/GAME_PLAN.md](docs/GAME_PLAN.md)**. Read the relevant section before building a feature.

## What this is

An isometric satirical political management game for the web, styled like a cozy life-sim (think _The Sims_) rather than a top-down SNES RPG. You play a 4-year term (JAN 2024 → DEC 2027) as a cabinet of 6 officials, spending 3 Executive Actions per month while five national stats (Debt, Interest Rate, Inflation, Party IQ, Happiness) plus DEFCON try to end the run. Survive the term to win. _Shining Force_ is the reference for the screen layout (windows, portraits, cross-shaped menu) — and, for Declare War specifically, for actual turn-based tactical combat too (`engine/battle.ts`, GAME_PLAN §7.1). Every other action still resolves with a plain success roll, no grid involved.

## Commands

The project uses **Yarn 1** (`yarn.lock`). npm also works (`npm start`, `npm run verify`).

| Command           | What it does                                                                   |
| ----------------- | ------------------------------------------------------------------------------ |
| `yarn start`      | Dev server (http://localhost:5173), opens the browser                          |
| `yarn dev`        | Dev server without opening the browser                                         |
| `yarn build`      | Typecheck + production build into `dist/`                                      |
| `yarn preview`    | Serve the production build                                                     |
| `yarn test`       | Run all Vitest tests once                                                      |
| `yarn test:watch` | Tests in watch mode                                                            |
| `yarn typecheck`  | TypeScript only                                                                |
| `yarn lint`       | ESLint                                                                         |
| `yarn format`     | Prettier (write)                                                               |
| `yarn verify`     | **typecheck + lint + format check + tests. Run before calling any task done.** |

Requires Node 22 LTS (see `.nvmrc`; Node ≥ 20.19 works).

## Architecture rules (non-negotiable)

1. **`src/engine/` is pure TypeScript.** No React, no Zustand, no Pixi, no DOM, no `localStorage`. ESLint enforces this.
2. **No `Math.random()` or `Date.now()` in `engine/` or `data/`.** Use `createRng()` from `engine/rng.ts` (seeded, so runs are replayable) and the in-game calendar from `engine/calendar.ts`.
3. **Engine functions are pure:** take a state, return a new state. Never mutate `GameState`.
4. **Content is data.** Actions, characters, events, showdowns, rooms and endings live in `src/data/`. The engine interprets them.
5. **No magic numbers in the engine.** Every tunable value goes in `src/data/balance.ts`.
6. **`src/store/gameStore.ts` is the only bridge** between React and the engine. Components read from the store and call its actions; store actions call engine functions.
7. **No hard-coded UI text.** Add a key to `src/i18n/en.ts` _and_ `src/i18n/fr.ts` (TypeScript fails if French is missing a key), then use `const t = useT()` and `t('key')`.
8. **Character names only come from `src/data/characters.ts`** (the name layer, real vs. parody names). Never type a character's name in a component or string.
9. **Every engine change comes with Vitest tests**, colocated as `*.test.ts`.

## Layout

```text
src/
├─ engine/    pure game rules (calendar, rng, state; economy, effects, actions, endings, movement…)
├─ data/      content + balance numbers (actions, characters, countries, rooms, roomLayouts…)
├─ store/     Zustand store: the bridge
├─ i18n/      en.ts (source of keys), fr.ts, translate.ts
├─ ui/        React components (hud/, room/, menus/, dialogue/, screens/) + hooks
├─ styles/    global.css imports the other CSS files
├─ App.tsx    stage + screens
└─ main.tsx   entry point
public/assets/   sprites, tilesets, Tiled maps, audio
scripts/         Node scripts (balance simulator in Phase 1)
docs/            GAME_PLAN.md
```

Import with the `@/` alias (`@/engine/calendar`), relative imports inside the same folder are fine.

## Conventions

- TypeScript strict mode with `noUncheckedIndexedAccess`. No `any`; prefer `unknown` + narrowing.
- Named exports only (no default exports).
- Files: `camelCase.ts` for modules, `PascalCase.tsx` for components, tests next to the code (`calendar.test.ts`).
- Type-only imports use `import type`.
- Formatting is Prettier's job (single quotes, trailing commas, 100 columns). Don't hand-format.
- Commit messages: short imperative summary (`Add monthly economy tick`).
- Add dependencies with `yarn add` (or `yarn add -D` for tools), and ask before adding one. Current runtime deps: react, react-dom, zustand, @fontsource/press-start-2p.

## Pixel art and screen rules

- Internal resolution **480×270**, scaled up with whole-number zoom (`ui/useStageScale.ts`).
- Rooms are **isometric** (2:1 dimetric), not a flat top-down grid: `ui/room/isometric.ts` projects a room-grid `{x, y}` position to a diamond footprint **32px wide × 16px tall**; walls and objects are extruded blocks (`clip-path`-based, no new dependency). `engine/movement.ts` and `data/roomLayouts.ts` still describe rooms as a plain rectangular grid — only the rendering layer projects that grid isometrically, so a real PixiJS isometric renderer later only touches `ui/room/`.
- Font: Press Start 2P at **8px** (or 16px for titles). One character = 8px wide: count characters when laying out the HUD.
- Short names must fit in 8 characters.
- The 6 US officials and Canada's battle roster use real 4-direction pixel-art sprites (`public/assets/sprites/`, wired via `data/characters.ts`'s `sprite`/`spriteForDirection` and `data/battleRosters.ts`'s `sprite` field on `BattleUnitTemplate`). Every other character (other countries' rosters) still falls back to a coloured square with initials (`placeholder` in `data/characters.ts` / `battleRosters.ts`).

## Tone guardrails (docs/GAME_PLAN.md §14)

- Satirize public actions and policies, not bodies, families or private lives.
- No invented quotes that could pass for real statements. Parody must be obviously parody.
- Cartoon disasters only: wars and meltdowns are gags, maps and headlines, never casualties.
- Opponents, media and bureaucrats get roasted too.

## Status

- [x] **Phase 0 — Setup:** Vite + React + TS, ESLint (with engine guard), Prettier, Vitest, folder structure, name layer, EN/FR strings, seeded RNG, calendar, placeholder title screen + HUD.
- [x] **Phase 1 — "Spreadsheet" prototype:**
  - `engine/effects.ts`: effect DSL (`delta`/`spread`/`delayed`/`chance`/`flag`/`rage`) + delayed-effect queue (`tickPending`)
  - `engine/economy.ts`: month-end tick (§6 formulas, constants from `balance.ts`), DEFCON calm-recovery, Congress/midterms/impeachment tracking
  - `engine/actions.ts` + `engine/resolve.ts`: availability rules (3 EA/month, once-per-month safe actions, IQ gates), success %, success/backfire, diminishing-returns Headlines. _One-action-per-official is deferred to Phase 2_, since there's no character-selection UI yet.
  - `engine/endings.ts`: all loss endings (bankruptcy, hyperinflation, brainFreeze, revolt, nuclear, impeachment) + survival at DEC 2027, with fixed precedence
  - `engine/scoring.ts`: Headlines → rank
  - `data/actions.ts`: 17 actions from §9
  - UI: action list with success %, End Month button, month-end report, ending screens, all EN/FR strings
  - `scripts/simulate.test.ts`: Monte Carlo balance simulator (3000 runs × random/cautious policy) — also asserts survival-rate bounds as a regression guard, run via `yarn simulate`
  - Typecheck, lint and format all verified clean on the real toolchain. **`yarn test` / `yarn verify` / `yarn simulate` still need to be run on a native Windows or macOS/Linux shell** — the assistant's device-bridge shell is a Linux VM whose mounted `node_modules` only has Windows-native Rollup bindings (`@rollup/rollup-win32-*`, no `@rollup/rollup-linux-x64-gnu`) and no network access to fetch the missing one, so Vitest can't execute from there. Run `yarn verify` yourself once from a normal terminal to confirm the suite passes.
- [x] **Phase 2 — Room slice:**
  - `engine/movement.ts`: pure grid movement/collision (`isWalkable`, `moveWithin`) — data in, position out, so the CSS/DOM renderer below can be swapped for real PixiJS/Tiled later without touching it
  - `data/roomLayouts.ts`: Oval Office + Treasury as ASCII-grid tile layouts, one action-bound object each, a door connecting them
  - `data/countries.ts` + new `declareWar`/`purchaseCountry` effects: **World Map menu** — Declare War / Buy a Country each pick a random not-yet-hit country (Canada, Greenland, Panama, Mexico) via the seeded RNG; `GameState` tracks `atWarWith`/`countriesOwned`
  - UI: `RoomView` (CSS/DOM tile grid — see the isometric pivot below, arrow-key/WASD movement, facing + object hint), `OfficialWindow` + `RoomWindow` (§11's info boxes), `CrossMenu` (DECREE/TALK/ITEM/SWITCH), `MapScreen` (world status board), `ActionsOverlay` (DECREE = room-scoped actions, and a toolbar "Executive Actions" escape hatch to the full catalog so nothing is lost while only 2 of 10 rooms exist), TALK/ITEM stubs for Phase 3/5
  - Switching between 3 officials (Trump, Bessent, Vance) — one shared party position for now; per-official positions are a later refinement
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as Phase 1 for actually running the suite — see above)
- [x] **Phase 3 — Resolution & showdowns:**
  - `engine/preview.ts`: pure `summarizeEffects` — walks an action's effect list into an immediate/over-time stat summary for the Preview window (recurses into `chance`'s `then` branch, flags `hasChance` and `hasRandomCountry`, ignores `flag`/`rage`)
  - `engine/showdown.ts` + `data/showdowns.ts`: showdown DSL (3 rounds × 3 dialogue choices, each choice a success-chance modifier) — two showdowns, **Fed** (paired with Lower Interest Rates) and **Press Conference** (paired with a new `press_conference` action, action #18/§9 row 33, since none existed to pair with a second showdown)
  - `engine/resolve.ts`: `computeSuccessChance`/`resolveAction` gained a backward-compatible `successModifier` parameter so a showdown's total modifier reaches the actual roll, not just the preview display
  - `store/gameStore.ts`: `performAction` replaced by a `resolution` state machine — `beginAction` → (`showdown` rounds, only for the 2 paired actions) → `preview` (confirm/cancel) → roll → `result` (revealed after a brief delay) → `continueResolution` applies the state and logs it, same as the old month report
  - UI: `ResolutionOverlay` renders all three phases (showdown dialogue, preview with success % and stat-delta list, delayed roll reveal with headline flavor text and a stat-diff table matching the month-end report's style)
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as Phase 1/2 for actually running `yarn test`/`verify`/`simulate` — see above)
- [x] **Art direction pivot — isometric room camera (pre-Phase 4):** dropped the flat top-down 16-bit grid for a 2:1 isometric ("dimetric") camera, closer to _The Sims_' dollhouse view (see GAME_PLAN v0.3's changelog and §11).
  - `ui/room/isometric.ts` (new, pure, tested): `projectIso`/`isoGridBounds`/`projectIsoWithin` turn a grid `{x, y}` into a diamond-projected screen point; `isoDepth` gives painter's-algorithm draw order; `buildCubeFaces` builds the three `clip-path` polygons (top/left/right) for an extruded wall or object block
  - `ui/room/RoomView.tsx`: fully rewritten to render floor/door tiles as flat diamonds and walls/objects as extruded blocks, with a camera (`ui/room/useElementSize.ts` measures the viewport) that follows the player, translating a `room-iso-world` container instead of the old fixed 1:1 tile grid
  - `engine/movement.ts` and `data/roomLayouts.ts` are **untouched** — rooms are still plain rectangular grids; only the rendering layer reinterprets them isometrically, so a future real PixiJS isometric renderer only touches `ui/room/`
  - HUD chrome, the pixel font, and menu conventions are unchanged for now — this pivot is scoped to the room camera only; a broader UI restyle is a possible later art pass, not part of this change
  - i18n: `game.subtitle` (EN/FR) no longer says "16-bit"
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as above for actually running the suite)
- [x] **Pivot — Tactical battles for Declare War (pre-Phase 4):** Declare War now plays out as a _Shining Force_-style turn-based battle instead of an instant roll (GAME_PLAN §7.1); battle outcome (`usWin`/`enemyWin`) directly drives the action's success/fail effects.
  - `engine/battle.ts` (new, pure, tested): grid, interleaved turn order, BFS `reachableTiles`, `moveUnit`/`attack`/`endTurn`, a simple `enemyTakeTurn` AI, `checkOutcome`
  - `data/battlefield.ts` (new): the fixed battlefield grid + US/enemy spawn points
  - `data/battleRosters.ts` (new): `US_BATTLE_UNITS` (the 3 switchable officials) and `BATTLE_ROSTERS` per country — Canada's roster is the six names requested for this feature (5 real officials + 1 fictional wildcard), Greenland is polar bears; **Iran, Venezuela and Russia (new war-only targets) use invented, non-real-person rosters** rather than real current heads of state, since — unlike the original four — they're in live real-world conflicts (see that file's doc comment)
  - `engine/effects.ts`/`engine/resolve.ts`: `resolveWarEffects` substitutes the battle's actual target country into `declare_war`'s data-declared random-country effect at resolution time; `resolveBattleAction` rolls from the battle's own evolved RNG state, not the pre-battle one (so the battle's moves/attacks aren't replayed)
  - `store/gameStore.ts`: new `battle`/`battleCountry` resolution-phase state, `battleMove`/`battleAttack`/`battleEndTurn`/`battleRunEnemyTurn` actions, `settleBattle` helper
  - `ui/battle/BattleView.tsx` (new): the battle screen, wired into `ResolutionOverlay`'s new `'battle'` phase
  - `data/countries.ts`: added `iran`/`venezuela`/`russia` (war-only, not purchasable)
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as above for actually running the suite)
- [x] **Phase 4 — Vertical slice:**
  - 3 new rooms + roster/action expansion (22 actions total), all 6 officials playable with their own actions
  - `data/events.ts` + `engine/events.ts`: 20-entry random monthly event system (`rollMonthlyEvent`, weighted, calls `checkEnding`), surfaced in the month-end report and ticker
  - Congress/midterms UI: banners in `MonthEndReport.tsx`, a Congress-lost `HudCell`
  - Save/load: `store/gameStore.ts`'s `SavedGame` (localStorage-backed autosave) + Continue/New Game on the title screen
  - `ui/audio/sfx.ts` + `MuteToggle.tsx`: placeholder Web Audio chiptune blips/jingles/ambient hum, wired in: `App.tsx` runs the ambient hum only on the room/battle screen and mounts `MuteToggle` next to `LangToggle`; `CrossMenu`/`GameScreen`'s toolbar play `blip` (or `confirm` for End Month); `ResolutionOverlay` plays `blip` for showdown choices and the result-continue button, `confirm`/`cancel` for the preview buttons, and `success`/`fail` at the moment a result is revealed; `MonthEndReport` plays `headline` when a new report appears; `EndingScreen` plays `success`/`fail` once when a run ends
  - Real pixel-art sprites for the 6 US officials and Canada's 6-person battle roster (`public/assets/sprites/us/`, `public/assets/sprites/canada/`, cropped from user-provided reference sheets), replacing their coloured-square placeholders on the title screen, room camera, official windows, resolution preview and battle screen — every other country's battle roster is still `placeholder` art
  - Title screen polish: roster in a framed panel with a hover highlight and per-official name-tag color, brief fade-in on load, and `playSfx` on Start/Continue/New Game
  - Typecheck, lint and format verified clean on the real toolchain by syncing to the device via the remote-devices bridge and running `tsc`/`eslint`/`prettier` there (found and fixed two files where hand-predicted Prettier formatting was slightly off). **`yarn test`/`vitest` still can't run from either the assistant's sandbox or the device-bridge shell** — same Linux-VM/Windows-Rollup-binary mismatch as every prior phase; run `yarn verify` yourself from a normal terminal to confirm the suite passes.
- [x] **Art update — US sprite sheet v2 (post–Phase 4):** replaced all 24 US official sprites (`public/assets/sprites/us/`) with a user-supplied transparent-background sheet (`public/assets/sprites/source/us-cabinet-sheet-v2.png`), extracted via an alpha-threshold bounding box (no more white-background ink detection) that also excludes the sheet's baked-in drop shadow so it doesn't double up with `.room-player-shadow`.
  - The new sheet's 4 poses per character are diagonal 3/4 views (front-left/front-right/back-left/back-right), not the old sheet's straight cardinal ones — `data/characters.ts`'s `CharacterSprite` slots are now assigned by which screen diagonal `ui/room/isometric.ts`'s `projectIso` actually draws each `Direction` moving toward (worked out from the projection formula, not guessed), so a character now visibly faces the right way when walking in every direction instead of just approximating it.
  - Canada's roster sprites are untouched (still the original sheet).
- [x] **Fix — player sprite z-index race during fast movement (post–Phase 4):** the player token could momentarily disappear behind a wall/object block while moving quickly. Root cause: `.room-player`'s `left`/`top` CSS glide (120ms, `ROOM_MOVE_TRANSITION_MS` in `ui/room/isometric.ts`) and its `isoDepth`-derived `z-index` (which jumps instantly when the store updates) fell out of sync mid-glide. A first attempt (delaying the `z-index` CSS transition by 120ms) only fixed the up/left case and made down/right worse, since a flat delay is wrong for moves that _increase_ depth. Replaced with `ui/room/RoomView.tsx`'s `usePlayerDepth` hook: holds `Math.max(old depth, new depth)` immediately on every move (correct for both directions at once) and only settles to the new tile's true depth once the glide finishes, via a timer keyed to `ROOM_MOVE_TRANSITION_MS`; resets immediately (no `max`) on a room change, since depth isn't comparable across rooms. `.room-player-shadow` still uses the raw, un-held depth. `src/styles/room.css` no longer transitions `z-index` at all.
  - Typecheck/lint/format could **not** be run on the real toolchain for this fix — the device's local shell (`device_bash`) has been down since a Windows update on Sept 8 ("Windows update released September 8 prevents Claude's workspace from reaching your files"), so the change was synced to the device via direct file-write (`device_commit_files`) rather than the usual sync-and-verify flow. Run `yarn verify` yourself once the shell is usable again, and confirm in-game that the sprite no longer flickers/disappears in all four movement directions.
  - **Regression tests (`ui/room/playerDepth.ts` + `ui/room/playerDepth.test.ts`):** the max/settle decision that fixes the bug was pulled out of the `usePlayerDepth` hook into two pure functions — `holdPlayerDepth` (same-room: `Math.max(old, new)`; room change: snap to the new depth) and `settlePlayerDepth` (settle to the true depth) — so it can be tested directly with Vitest instead of through React rendering (this repo has no React component/hook test setup — no jsdom, no Testing Library — so keeping the actual fix logic pure and colocated with a plain `*.test.ts` file, like every other `engine`/`data` module, was preferred over adding that infrastructure for one hook). `playerDepth.test.ts` covers both directions explicitly using the real `step`/`isoDepth` functions (not just abstract numbers): down/right must rise to the new depth immediately, up/left must hold the old depth until settled, a room change must always snap rather than `max`, and a held-down key (repeated moves with no settle in between) must never dip. These tests reproduce the exact bug report ("only fixed for Left/Up, still disappears for Right/Down") and would fail against the first, CSS-only fix attempt.
  - Could not run this new test file on the real toolchain either — same `device_bash` outage as above, **and** this session's cloud sandbox currently gets `403 Forbidden` from the npm registry for any package (confirmed with a plain `npm view react version`), so `vitest` couldn't be installed there as a fallback. The test file was hand-traced instead (each assertion's expected value worked through by hand against `holdPlayerDepth`/`settlePlayerDepth`'s logic) and Prettier's line-wrap/collapse rules were approximated by hand (100-column check via a script). Run `yarn test` (or `yarn verify`) yourself to confirm the new tests actually pass and are clean.
- [ ] **Phase 5 — Content** (in progress):
  - **Rose Garden room** (`data/roomLayouts.ts`): the Oval Office hub's fifth walkable spoke, alongside Situation Room/Federal Reserve/Commerce from Phase 4. A new door on the Oval Office's north wall at `{x:6, y:0}` (distinct from Commerce's existing door at `{x:15, y:0}` on the same wall) leads to a new 22×7 room whose single DECREE object triggers the four actions already tagged `room: 'roseGarden'` in `data/actions.ts` — Rally, Press Conference, Flip and Be Best — which existed since Phase 1/3 but, until now, were reachable only through the toolbar's full-catalog escape hatch, never by walking there. No other code changed: `RoomView`, `ActionsOverlay`, and the door-transition logic in `store/gameStore.ts` all key off `RoomLayout`/`RoomId` generically, so a new room in `ROOM_LAYOUTS` is enough on its own.
  - `data/roomLayouts.test.ts`: extended the existing "hub spoke" loop test to include `roseGarden`, and added it to `hasRoomLayout`'s true-case list (now 6 of the 10 rooms in §10 have a walkable layout, up from 5).
  - Verified by hand against the actual test assertions (rectangular grid, object/door tiles match the legend, start tile walkable, bidirectional door) via a small Python reproduction of `roomLayouts.test.ts`'s checks — same toolchain gap as the two entries above (`device_bash` down, cloud sandbox npm registry blocked), so this still needs a real `yarn test` run to confirm.
  - **Pentagon, Starbase and National Mall rooms** (`data/roomLayouts.ts`): three more Oval Office hub spokes, each a small room around the one action already tagged with that room in `data/actions.ts` — Pentagon → Rename a Department, Starbase → Fire a Rocket, National Mall → Military Parade. Same pattern as Rose Garden: a new door on the Oval Office's wall (north at `{x:21,y:0}` for Pentagon, west at `{x:0,y:2}` for Starbase, south at `{x:22,y:10}` for National Mall — each on a wall that already had a door to a different room, so they share the wall but not the tile) and a matching door back on the new room's _opposite_ wall, following the convention every prior spoke already used (exit the hub via wall W, enter the spoke via its own opposite wall). The Oval Office now has 8 doors total. This makes every `RoomId` walkable except `mapRoom`, which GAME_PLAN §10 deliberately keeps as the full-screen World Map status-board menu rather than a room — so this closes out "new room + its actions" as a category for Phase 5; anything still in the toolbar-only catalog from here on is there because it doesn't have a room yet in §10 either (e.g. `un_speech`, which the code already notes has no built UN room).
  - `data/roomLayouts.test.ts`: generalized the hub-spoke test to loop over all 7 spokes (was Rose Garden alone) plus an explicit door-count assertion on the Oval Office, and rewrote `hasRoomLayout`'s test to assert it's true for every `RoomId` except `mapRoom` rather than a hand-maintained true/false list.
  - Verified the same way as Rose Garden — a full Python reproduction of every assertion in `roomLayouts.test.ts` run against the final file content, all passing — since `device_bash` is still down and the cloud sandbox's npm registry access is still blocked. Still needs a real `yarn test` run to confirm.
  - **This closes out "add a room to an existing `RoomId`" as a task** — `engine/actions.ts`'s `RoomId` union only ever had these 10 values, and 9 of them are now walkable (`mapRoom` deliberately isn't). Capitol, Mar-a-Lago, the UN and Greenland from §10 are each a bigger lift than a room: none of them are `RoomId` values yet, and none of the actions that belong in them exist yet either (Government Shutdown/Pass a Big Beautiful Bill for Capitol, Launch a Memecoin/Strategic Crypto Reserve/Pay the Debt in Dogecoin for Mar-a-Lago, a real UN room for `un_speech` — currently tagged `room: 'mapRoom'` as a stand-in, per that action's own code comment — and Greenland is explicitly "unlockable... only after a successful purchase" so it needs that unlock condition wired up too, not just a grid). So the next room-shaped task in Phase 5 is really "extend `RoomId`, add the missing actions, then add the room" together, not a room in isolation.
  - Next up in Phase 5, roughly in order of self-containedness: round out the action catalog toward 32 (including the Capitol/Mar-a-Lago actions above, which would unlock those two rooms as a natural follow-on), the two remaining showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, ending art, full French translation, and difficulty modes.
  - **"Do them all" pass — action catalog, Capitol/Mar-a-Lago, both remaining showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, difficulty modes:** closes out nearly everything left in Phase 5 except ending art (no image-generation tool available in this environment) and three explicitly deferred lower-priority items — a Fire the Fed Chair showdown, an Impeachment Senate-trial showdown, and a real UN room (`un_speech` still stands in on `mapRoom`, per its own code comment).
    - **Action catalog → 37 actions** (`data/actions.ts`): added the 14 remaining §9 rows plus Elon's reconciliation action (15 total) — 90-Day Pause, Liberation Day (`oncePerRun`), Tariff Dividend, Launch a Memecoin, Strategic Crypto Reserve, Pay the Debt in Dogecoin, Sell Gold Cards, Build a Ballroom, Golden Dome, Post at 3 AM (`oncePerMonth`), Government Shutdown, Pass the Big Beautiful Bill, Executive Order Spree, Hire an Adult in the Room, and Patch Things Up With Elon. Every simplified mechanic (no per-tariff pending-effect tracking, no tariff-revenue ledger, no recurring monthly crypto swing, no Federal Judge injunction system, no randomized 1–3 month delay) is documented inline at the action that needed it, matching the precedent already set by `flip`/`vanish`/`un_speech`. `impose_tariff` gained a small `rage` effect it was missing (GAME_PLAN §8 lists tariffs as one of two things that fill Elon's meter; the doc comment on the new Liberation Day action already assumed it had one).
    - **Golden Dome and the two EA-modifying flags are real mechanics, not flavor:** `engine/effects.ts`'s `applyEffect` 'delta' case now intercepts and consumes a `goldenDomeActive` flag against the next negative `defcon` delta from any source (action, event, or battle loss); `engine/economy.ts`'s new `resolveActionsLeft` (called from `tickMonth`) caps the next month to 1 EA for `shutdownPending` or grants +2 EA for `executiveOrderBonus`, with a shutdown winning if a month somehow ends with both flags set.
    - **Elon's Rage Quit meter** (GAME*PLAN §8): `GameState` gained `elonRage: number` (0–100); the `rage` `Effect` kind (previously an intentional no-op stub) now actually adds to it, clamped; a new `unflag` `Effect` kind (`{kind:'unflag', clear: string}`) is the DSL's only way to remove a flag, used by the reconciliation action; `engine/effects.ts`'s new `checkElonRage` sets `elonLeftCabinet` once the meter hits 100 (idempotent, called at the tail of both `resolve.ts`'s `finalizeAction` and `events.ts`'s `rollMonthlyEvent`, the same way both already call `checkEnding`). `launch_rocket`/`doge_chainsaw` (Elon's own actions) gained `requires.notFlags: ['elonLeftCabinet']` so he can't act while gone. `data/events.ts` gained a `sabotage?: boolean` field and three sabotage events (SpaceX Pulls Out of a Federal Contract, Starlink "Mysteriously" Goes Dark, DOGE's Old Access Leaks Everything, 23 events total); `eligibleEvents` now returns \_only* sabotage events while `elonLeftCabinet` is set and never returns them otherwise. `engine/actions.ts`'s `ActionRequirements` gained `flags`/`notFlags` (all-must-be-set / none-must-be-set) and `ActionLimit` gained `'oncePerRun'`, both used by the reconciliation action and Liberation Day respectively; `checkAvailability` enforces both, with a new `'flags'` `UnavailableReason`.
    - **Capitol and Mar-a-Lago rooms** (`data/roomLayouts.ts`): `RoomId` extended with `'capitol'`/`'marALago'`; two more Oval Office doors (north wall at `{x:25,y:0}` for Capitol, east wall at `{x:29,y:7}` for Mar-a-Lago — the Oval Office has 10 doors now) with matching return doors on each new room's opposite wall, same convention as every prior spoke. Capitol hosts Government Shutdown and Pass the Big Beautiful Bill; Mar-a-Lago hosts Launch a Memecoin.
    - **Buy a Country and Pass the Big Beautiful Bill showdowns** (`data/showdowns.ts`): the two remaining §17 showdowns, same 3-round/3-choice shape as Fed and Press Conference, wired automatically since `store/gameStore.ts`'s resolution flow already looks up a showdown by `actionId` generically rather than hard-coding the first two.
    - **Melania's hide-and-seek** (GAME_PLAN §8, store-level, not engine — it's UI/session flavor, not a deterministic rule): `gameStore.ts` gained `melaniaRoomId`/`melaniaFoundThisMonth`, re-rolled every `endMonth` with plain `Math.random()` (same precedent as the store's existing `randomSeed()`); `movePlayer` grants +1 EA (via a new pure `engine/economy.ts` helper, `grantBonusAction`) the first time a door transition lands the party in that month's room, surfaced through the existing news-ticker mechanism (`lastEventNameKey: 'melania.found'`) rather than a new toast component. Persisted in `SavedGame` with a fallback for older saves.
    - **Difficulty modes** (`data/balance.ts`): `DifficultyId` (`'intern' | 'normal' | 'thirdTerm'`) and `BALANCES`, each a handful of knobs nudged off the existing `BALANCE` (more/fewer EA per month, gentler/harsher debt and inflation formulas, more/less forgiving ending thresholds) rather than a parallel set of constants — `normal` _is_ `BALANCE` unchanged. This was heavily de-risked by `Balance` already being threaded as an optional parameter through every engine entry point since Phase 1; the actual work was mostly `gameStore.ts` now resolving and passing the chosen `Balance` explicitly everywhere it used to rely on the default, plus a `difficulty` store field (persisted in `SavedGame`) and a title-screen picker (`TitleScreen.tsx`, `difficulty-picker` in `styles/title.css`). "Press Start" (the no-save path) now calls `newGame()` instead of `startGame()`, since the pre-built initial game state can't know a difficulty picked after the store was created.
    - **Test coverage added/fixed alongside every change above** (`CLAUDE.md` rule 9): `data/actions.test.ts` (count → 37, `truth_post_3am` added to the `oncePerMonth` list), `data/roomLayouts.test.ts` (Capitol/Mar-a-Lago added to the spoke loop and door count), `data/showdowns.test.ts` (all four showdown actionIds), `data/events.test.ts` (count → 23, sabotage-gating tests), `engine/events.test.ts` (sabotage-only-while-enraged tests), `engine/economy.test.ts` (new describe block for the shutdown/bonus EA refill), `engine/preview.test.ts`/`engine/effects.test.ts` (see the two real bugs below). No new tests were added directly for `store/gameStore.ts` — the store already has no test file (no React/hook test setup in this repo, per the existing precedent noted under the player-depth fix above) — but its logic is a thin, mostly type-checked pass-through over the now-tested engine/data functions.
    - **Two real bugs found and fixed while verifying this pass**, both from `elonRage`/`unflag` work done earlier in the same session, before the rest of the catalog existed to exercise them: `engine/effects.test.ts`'s `CTX()` test fixture was missing the now-required `elonRage` field (a compile error), and its `'applyEffect: rage'` test still asserted the _old_ no-op stub behavior instead of the real one — both fixed, with new coverage added for `rage`'s accumulation/clamping, `unflag`, and `checkElonRage` itself, plus Golden Dome's DEFCON-absorption. Separately, `engine/preview.ts`'s `summarizeEffects` `switch` wasn't updated for the new `'unflag'` effect kind, which meant it silently fell through (a real compile-time exhaustiveness error) instead of treating it like `flag`/`rage` (no preview-visible effect) — fixed, with a test added.
    - **Verification method:** `device_bash` (the user's device shell) has been down since a Windows update on September 8, and this cloud sandbox's own npm registry access is blocked (`403 Forbidden`), so `yarn test`/`yarn verify` still could not be run for real — same gap as every entry above since the player-depth fix. This pass went further than the usual hand-traced Python reproduction, though: a global `tsc` binary (v6.0.3) happened to be available in the sandbox outside the project's own `node_modules`-less install, so every changed file was actually type-checked against a throwaway config with hand-written ambient stubs for `react`/`react-dom`/`zustand`/`vitest`/`@fontsource/press-start-2p` (real logic and imports fully checked; only those five packages' own type shapes are faked as `any`, which is what produces expected noise — implicit-`any` callback params, JSX `key`-prop mismatches, "cannot find module" for those five bare specifiers — that isn't a real bug). That check is what caught both bugs above. It is not a substitute for a real `yarn verify` run (JSX prop-shape checking against real React types, ESLint, Prettier, and actually _running_ the test suite are all still unverified), so one is still needed to fully close this out.
    - Not done, by design: ending art (needs an image-generation tool this environment doesn't have), a Fire the Fed Chair showdown, an Impeachment Senate-trial showdown, and a real UN room — all explicitly deferred as lower-priority given the size of everything above.
  - **Closing the showdown count to 6/6** (`data/showdowns.ts`, `data/actions.ts`): re-checked Phase 5 against GAME_PLAN §17's own completion line item-by-item and found it was 2 showdowns short of "6 showdowns" despite the "two remaining showdowns" bullet above already being marked done (that bullet closed Buy a Country/Bill, not Fed Chair/Impeachment — a naming mixup in the earlier pass). Fixed properly this time:
    - **Fire the Fed Chair showdown** (`fedFiring`, paired with the existing `fire_fed_chair` action, which had no showdown until now): same 3-round/3-choice shape as the rest.
    - **Impeachment Senate-trial showdown** (`impeachment`, paired with a new action, `senate_trial`, action #38): there was no action to pair a trial showdown with — Impeachment has only ever been a `checkEnding` game-over condition (`lowHappinessStreak` vs. `impeachmentStreakMonths`, post-midterms), not something the player does anything about. Added `senate_trial` (Capitol, Trump, 2 EA, `oncePerMonth`, `requires.flags: ['congressLost']`) so there's something to survive: success is a flat +15 Happiness (there's no "reset the streak" `Effect` kind, so this just leans on `checkEnding`'s own logic — big enough Happiness reliably pushes back above `impeachmentHappiness` for the month, which is what actually breaks the streak); failure is -10 Happiness, i.e. leaning into it instead. `engine/economy.ts`'s `tickMonth` now adds a `'congressLost'` flag to `state.flags` the one month `congressLost` first flips true (it was already a `GameState` boolean, just never mirrored into `flags`, which is what `ActionRequirements.flags` actually reads) — a one-line, idempotent addition alongside the existing `resolveActionsLeft` flags plumbing.
    - Both showdowns needed no new UI or store code — `store/gameStore.ts`'s resolution flow and `ResolutionOverlay.tsx` already key off `getShowdownForAction(actionId)` generically, same as the Buy a Country/Bill pair before them.
    - Action catalog is now 38 (`data/actions.test.ts` updated); `data/showdowns.test.ts` now asserts all 6 actionIds. i18n: 1 new action name + 24 new showdown dialogue keys (12 per showdown) in both `en.ts`/`fr.ts` — 308 keys each, parity confirmed by script (no real `vitest` run possible here either, same toolchain gap as below).
    - New tests: `engine/economy.test.ts` (3 new cases for the `congressLost` flag: set on loss, not set when Congress is kept, never duplicated on a later tick), `data/actions.test.ts` (`senate_trial` is gated on `congressLost` and lives in `capitol`).
    - **Verification:** same toolchain gap as every entry above (`device_bash` still down as of this pass — re-tested, same Windows-update error — and the cloud sandbox's npm registry access is still blocked), but this time verified against a real `tsc` (v6.0.3) run over `engine`/`data`/`i18n` using the same throwaway harness/stubs built during the pass above, copied fresh from the current source: clean other than one pre-existing, unrelated stub-noise error (`expect.arrayContaining` isn't typed on the hand-written `vitest` stub's `expect`, a limitation of the stub itself, not the code — same category of noise documented above). Still needs a real `yarn test`/`yarn verify` run to fully confirm.
    - **This closes Phase 5's showdown-count and action-count line items.** The two items marked "not done, by design" two bullets up are unchanged and still open: ending art (needs image generation) and a real UN room (`un_speech` still stands in on `mapRoom`). Per user direction, these are accepted as permanent, environment-driven exceptions rather than blockers, and the project is moving on to Phase 6.
- [x] **Phase 5 — Content:** closed out except for the two accepted exceptions noted directly above (ending art, a real UN room) — every other §17 target (38 actions vs. a 32 target, 6/6 showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, difficulty modes, full French translation, all `RoomId`s except the deliberately-menu `mapRoom` walkable) is done.
- [x] **UX fixes from first real playtest feedback (pre-Phase 6):** three issues reported directly against the running build, not the code:
  - **Removed the always-on ambient hum:** `ui/audio/sfx.ts`'s `startAmbientHum`/`stopAmbientHum` were wired into `App.tsx` to auto-start the moment the room screen mounted, with no way to stop just that drone short of muting all sound (SFX included) — reported as "the background sound non-stop." Fix: dropped the `useEffect` in `App.tsx` that called them; the functions themselves are left in `sfx.ts`, fully working and documented as currently unused, for whenever real ambient music (or a separate music-only toggle) replaces this placeholder. SFX jingles (blips, confirm/cancel, success/fail, headline) are untouched and still play normally.
  - **Moved the mute/language toggles onto the news ticker:** `styles/global.css`'s `.lang-toggle`/`.mute-toggle` used to float just above the ticker (`bottom: calc(var(--ticker-height) + 6px)`), which put them directly over the room screen's toolbar — specifically its rightmost "End the Month" button, as seen in the reported screenshot. Moved both to `bottom: 0` (i.e. actually on top of the ticker strip, per the request to put them "over the news feed") with `z-index: 1` so they paint above the ticker `<footer>` despite coming earlier in `App.tsx`'s DOM order, and shrank their padding to fit the ticker's 14px height.
  - **Cross menu (`ui/menus/CrossMenu.tsx`) now uses icons instead of text, smaller buttons:** GAME_PLAN §11 always described this as "four icons in a diamond" — the DECREE/TALK/ITEM/SWITCH text labels were a placeholder (same category as the mute button's own emoji, which already set this precedent), and long French labels (`ÉCHANGER`, `DÉCRET`) were overflowing/clipping inside the grid cells regardless. Replaced with 📜 (decree) / 💬 (talk) / 🎒 (item) / 🔄 (switch), each `aria-hidden`, with the real accessible label moved to `aria-label`/`title` on the button (same i18n keys the text used to render, so translations are unaffected). `styles/room.css`'s `.cross-menu` grid shrank from 40×16px cells to 18×16px now that there's no label-width math to satisfy.
  - **Verification:** same toolchain gap as always (`device_bash` still down, re-confirmed again this pass; cloud sandbox npm registry still blocked) — but this time checked with `tsc` against the _full_ `src` tree including all UI/React components (previous passes' harness only covered `engine`/`data`/`i18n`), using the same hand-written stubs. Zero new errors from any of the five changed files (`App.tsx`, `CrossMenu.tsx`, `sfx.ts`, plus the two CSS files, which `tsc` doesn't check); every error the harness reported is pre-existing stub incompleteness in files this pass didn't touch (missing `src`/`disabled`/`role`/`ref`/etc. props on the hand-rolled JSX intrinsic-element stub, missing `useRef`/`CSSProperties`/`KeyboardEvent`/`RefObject` on the `react` stub, missing `it.each` on the `vitest` stub). Still needs a real `yarn verify`/`yarn dev` look to confirm the on-screen layout and that the hum is actually gone.
- [x] **Real world-map art, and picking your own Declare War target (more playtest feedback):**
  - **World Map background image:** the user supplied a real equirectangular world-map PNG (`public/assets/maps/world-map.png`, 1013×380, re-encoded RGB/no-alpha to shrink it ~30%) to replace `.map-world`'s flat navy rectangle. `styles/room.css`'s `.map-world` now sets it as a `background` with `background-size: 100% 100%` — a deliberate stretch rather than `cover`/`contain`, since `data/countries.ts`'s `mapX`/`mapY` are plain percentages of the box and stretching is what keeps "38% across" landing on the same spot on the image regardless of the flex-sized box's actual aspect ratio. All 7 `mapX`/`mapY` pairs were re-tuned by eye against this specific image (sampled it programmatically — a coarse land/ocean/ice color-classification grid — to locate each landmass rather than guessing) so the markers now sit roughly on the right country instead of on arbitrary placeholder coordinates; the doc comment on `CountryDef` says plainly these are "tuned by eye … not surveyed coordinates."
  - **Target selection for Declare War** (real user feedback: "you should be able to select your country to attack"): `declare_war` used to auto-pick a random eligible country the instant the preview was confirmed (`pickRandomCountry` in the old `confirmPreview`). Replaced with a real choice:
    - New `ResolutionPhase`, `'selectCountry'`, sits between `'preview'` and `'battle'`. `confirmPreview` now checks whether any war-eligible, not-already-at-war country exists and, if so, moves to `'selectCountry'` instead of picking one — the "ran out of countries" fallback (straight to the ordinary roll) is unchanged.
    - New store action, `selectBattleCountry(country)`: validates the pick is actually eligible (last-line sanity check, same pattern as `checkAvailability`), then does exactly what the old random-pick branch did — builds the rosters, starts the battle — just with the chosen country instead of an RNG draw.
    - New component, `ui/screens/SelectCountryScreen.tsx`: reuses the World Map's own `.map-world`/`.map-country` markup and the new background image, but only plots the currently-eligible countries, each as a real `<button>` (new `.map-country-button` CSS modifier for the hover/focus affordance a `<span>` didn't need) that calls `selectBattleCountry`. Wired into `ResolutionOverlay.tsx` as a new phase branch, same pattern as the existing `'battle'` → `<BattleView />` branch.
    - The preview screen's copy adjusted to match: the generic "targets a random country" note (`resolution.randomCountryNote`) is now suppressed for battle-gated actions specifically (still shown for Buy a Country, which is still a random pick — this request was about Declare War only) in favor of a new note, `battle.selectCountryNote` ("Pick who's about to have a very bad day."), reused as the new screen's own subtitle.
  - **Verification:** same toolchain gap as always (`device_bash` still down, re-confirmed again). `tsc` over the full `src` tree: zero errors from any of the changed/new files (`data/countries.ts`, `store/gameStore.ts`, `ui/screens/ResolutionOverlay.tsx`, the new `ui/screens/SelectCountryScreen.tsx`) beyond the one already-documented, pre-existing stub gap (`role`/`aria-modal` not typed on the hand-rolled JSX intrinsics — the same gap `MonthEndReport.tsx`/`BattleView.tsx` already hit before this pass). i18n parity (310 keys, en/fr) confirmed by script. Still needs a real `yarn verify`/`yarn dev` look, especially to eyeball whether the re-tuned map markers actually land on the right countries on screen.
- [ ] **Phase 6 — Polish & release** (started):
  - **Balance pass with the simulator (GAME_PLAN §17's Phase 6 line item), plus a real verification-method upgrade:**
    - **Extracted `scripts/simulate.ts` from `scripts/simulate.test.ts`.** The documented folder structure (`CLAUDE.md`'s own Layout section, and GAME_PLAN's) has always named `scripts/simulate.ts` as the balance simulator, but the actual pure logic (`runOne`, `simulate`, `printReport`, `pickAction`, the `random`/`cautious` policies) had only ever lived inside the `.test.ts` file itself, with no plain script to run outside Vitest. Moved all of it to `simulate.ts` (now exported), added an `import.meta.url` entry-point guard so `tsx scripts/simulate.ts` prints the report with no test framework involved, and slimmed `simulate.test.ts` down to importing those exports and wrapping them in the two survival-rate assertions. `yarn simulate` (`vitest run scripts/simulate.test.ts`) behaves identically to before — same report, same assertions, just reading from the new file.
    - **This was actually run, for real, dynamically — a first for this entire session.** Every previous "verification" this session (going back to before this file's `[x]` Phase 1–4 entries) was either hand-traced or `tsc`-type-checked only, because `device_bash` (the user's device shell) has been down since a Windows update and this cloud sandbox's own npm registry access returns `403 Forbidden`, so `vitest` itself was never installable to actually _run_ anything. What changed: this pass discovered a working `tsx` binary (v4.21.0) already present in the sandbox outside the blocked registry, plus a minimal hand-written `vitest` shim (`describe`/`it`/`expect` with real assertion logic, not just type stubs) built earlier in the session for the `tsc` type-check harness. `tsx --tsconfig tsconfig.app.json scripts/simulate.ts` resolves the real `@/` path alias and actually executes the real `data`/`engine` modules end to end. The shim was missing several matchers other test files use (`toHaveLength`, `toContainEqual`, `toBeUndefined`, `toBeDefined`, `toMatch`, `expect.arrayContaining`, `it.each`) — added all of them, then ran **every one of the 28 `*.test.ts` files in the project** (engine, data, i18n, and the one pure-logic `ui/` test file) this way: **269 assertions, 0 failures, 0 crashes.** This is a real dynamic pass over the actual behavior of everything built this session (the full Phase 5 content pass, the UX fixes, the world-map/target-selection feature, and this refactor itself), not just types lining up — the strongest confirmation available in this environment short of the user's own `yarn verify` run. It does **not** cover ESLint, Prettier, `tsc -b`'s project-reference build mode specifically, or anything React-component-rendering-shaped (this repo still has no React test setup, by longstanding precedent) — those still need a real `yarn verify` on a working toolchain.
    - **Balance findings, read honestly rather than tuned reflexively:** `random` policy survives 1.5% of 3000 runs (well under the test's `<20%` bound), losing mostly to `brainFreeze` (63% of its losses) — Party IQ crashing to the floor, because a random policy never specifically prioritizes `read_briefing` and most actions cost a little IQ as their joke ("dumber is stronger, until it isn't," GAME_PLAN §1). `cautious` policy (which explicitly reads a briefing under IQ 25) survives 71.5% of runs (inside the `>30%`/`<95%` bound) with 0% `brainFreeze` losses and its remaining losses almost all `bankruptcy` (24.3%). Read together, this isn't a balance bug: it's the IQ-management mechanic working exactly as designed — ignore it and it's the actual primary failure mode; manage it and Debt becomes the real long-term threat instead. No balance numbers were changed off the back of this pass; both existing bounds pass with real margin, and the shape of the failure distribution matches the game's own stated design intent. Worth a second look if a future pass changes IQ-cost or `brainFreezeIq` numbers, now that this can actually be re-run in under a minute.
  - **"Juice" — screen shake and animated tick-up numbers (GAME_PLAN §16/§17):**
    - **Screen shake:** `store/gameStore.ts` gained a `shakeSeq: number` counter (session/UI flourish, not part of `GameState`/`SavedGame` — same category as `screen`/`overlay`) and a `triggerShake()` action (`shakeSeq + 1`). It fires from two places: `ResolutionOverlay.tsx`'s result-reveal effect, on a **failed** action only (a win never shakes, so the shake itself reads as "that went badly," on top of the existing fail jingle); and `battleAttack`/`battleRunEnemyTurn` in the store, on a landed hit (the enemy-turn case checks the battle log actually grew, since `enemyTakeTurn` can just reposition without attacking). `ui/ScreenShake.tsx` (new) wraps the stage's contents, subscribes to `shakeSeq`, and restarts the CSS animation on every bump — including rapid re-triggers — with the standard remove-class/force-reflow/re-add-class trick. `App.tsx` now wraps `Hud`/`CurrentScreen`/`LangToggle`/`MuteToggle`/`Ticker` in it, inside `.stage` rather than on `.stage` itself, so the shake's `transform` never fights `.stage`'s own zoom-scale `transform`. New CSS in `global.css`: `.stage-shake-layer`/`.stage-shake`/`@keyframes stage-shake` (a 350ms rattle). No extra `prefers-reduced-motion` rule was needed — the existing global rule (`animation: none !important`) already covers this new keyframe animation.
    - **Tick-up numbers:** `ui/useTickUp.ts` (new hook) animates a displayed number from `from` to `to` over 700ms via `requestAnimationFrame` with ease-out easing, snapping straight to the end value when `prefers-reduced-motion: reduce` is set (checked manually via `matchMedia`, since this is a JS-driven animation the CSS-only global rule can't reach) or when `from === to`. It returns the raw interpolated number so callers still run it through their normal formatter, so a mid-count frame reads with the right unit/rounding (e.g. "$41.3T" ticking up to "$42.0T") instead of a differently-formatted intermediate value. The month-end report and the action-result screen used to each have their own near-identical stat-table JSX; extracted into a shared `ui/screens/ReportStatsTable.tsx` (`ReportStatsTable`/`StatRow`) that calls `useTickUp` per row, now used by both `MonthEndReport.tsx` and `ResolutionOverlay.tsx` in place of the duplicated markup.
    - **Verification — a step up from the usual gap:** `device_bash` is still down (Windows-update issue, unchanged since Phase 5), so the device sync for this batch went through `SendUserFile` + `device_commit_files` directly, same as prior `device_bash`-outage work. But verification itself was stronger than most of this project's history: ran a real `tsc` project check over the full `src` tree (`tsconfig.verify-ui.json`) — every error reported was the same pre-existing hand-rolled-stub noise already documented above (`useRef`/`CSSProperties`/`KeyboardEvent`/`RefObject` missing from the `react` type stub; `ref`/`src`/`role`/`disabled`/`scope`/etc. missing from the JSX-intrinsics stub's narrow prop union), now also hit by the two new files (`ScreenShake.tsx`'s `ref`, `ReportStatsTable.tsx`'s `scope`) for the same reason every other component already hits it — no error outside that known category. Then, using this session's `tsx`-plus-vitest-shim breakthrough (first exercised for the balance-simulator entry above), **re-ran all 28 `*.test.ts` files for real**: 28/28 pass, 0 crashes — confirms the store/UI changes didn't regress any engine/data behavior (there's still no test file for `gameStore.ts` itself, or any React-rendering test, per this repo's long-standing no-React-test-setup precedent, so the shake/tick-up logic's own correctness was confirmed by code reading rather than an executed test). Still worth a real `yarn dev` look to eyeball the shake feel and the tick-up timing in the browser.
  - **Canada battle roster art refresh + Jagmeet Singh added (7th unit):** the user supplied 4 new individual reference sheets (Mark Carney, Justin Trudeau, Jagmeet Singh, Dominic LeBlanc — `public/assets/sprites/source/canada-*-sheet(-v2).png`), each 4 character poses over a transparent background with a text-label caption baked in below them. Per the user's own note, poses 1–2 are the same "front" direction and poses 3–4 are the same "back" direction (not four distinct directions like the US sheet's diagonal convention) — since `data/battleRosters.ts` only ever renders a single static `sprite` per battle unit (battle units aren't direction-aware like switchable officials, confirmed by reading `engine/battle.ts`'s `BattleUnitTemplate` and `ui/battle/BattleView.tsx`), only the first ("front") pose from each sheet was cropped and used; `-back.png`/`-left.png`/`-right.png` weren't regenerated for the 3 existing characters since the game never reads them regardless of source art vintage.
    - **Cropping:** an alpha-channel bounding-box scan (numpy, same family of technique as the Phase-4 US-sheet method) found each sheet's 4 pose columns and separated the sprite row-band from the caption text row-band beneath it; the first pose column was cropped tight to its own content with a few pixels of padding. Output sizes (~99–104px × 239–249px) landed close to the existing Canada sprites' own range (90–111 × 239–247) without any deliberate resizing — both the roster-swatch and on-field battle sprite are rendered with CSS `object-fit` (`cover`/`contain`), so exact pixel dimensions don't need to match anyway.
    - **Replaced** `public/assets/sprites/canada/carney-front.png`, `trudeau-front.png`, `leblanc-front.png` in place (same filenames, new art). **Added** `singh-front.png` plus a new roster entry, `canada-singh` (`data/battleRosters.ts`, between LeBlanc and the fictional wildcard), and its i18n name key (`battle.unit.canada.singh`, en/fr — 311 keys each, parity confirmed by script).
    - **Roster size went from 6 to 7**, which the battle system had a real hard cap on: `data/battlefield.ts`'s `ENEMY_SPAWN_POSITIONS` had exactly 6 slots, sized specifically because "Canada's roster has exactly 6" (its own prior doc comment). Asked the user whether Singh should replace the fictional "Guy in a Mackinaw Jacket" wildcard (keeping the roster at 6, no battlefield change) or join as a genuine 7th unit — they chose **expand to 7**. Added a 7th `ENEMY_SPAWN_POSITIONS` tile at `{x:8, y:3}` (one column back from the existing `x:9` column, which already spans the field's full open height; confirmed clear of the `x:5` obstacles and every other spawn tile by the updated `battlefield.test.ts` assertions actually running). Updated both doc comments (`battlefield.ts`, `battleRosters.ts`) accordingly.
    - **Test coverage updated to match:** `battleRosters.test.ts` (`getBattleRoster('canada')` length 6→7, enemy-capacity ceiling 6→7 slots), `battlefield.test.ts` (comment + `ENEMY_SPAWN_POSITIONS.length` bound 6→7 — this also re-validates, dynamically, that all 7 enemy spawns including the new one are on open floor and that no two spawns collide, since that's exactly what the existing "on open floor" / "unique, non-overlapping" tests already check generically over the full array).
    - **Verification:** `device_bash` still down (same Windows-update outage, re-confirmed), so this batch was synced via `SendUserFile` + `device_commit_files` again. But both checks that matter most here actually ran for real: `tsc` over the full `src` tree showed zero new errors from any of the six changed/added TS files (identical error list, byte-for-byte, to the pre-change baseline — confirmed by diffing the two `tsc` runs directly rather than eyeballing); and, more importantly for this change specifically (a spawn-grid collision and an array-length invariant are exactly the kind of thing static types can't catch), **the real dynamic `tsx` test run** (all 28 `*.test.ts` files) passed 28/28, including the updated `battlefield.test.ts`/`battleRosters.test.ts` assertions that directly exercise the new spawn tile and the 7-unit roster. i18n key-parity (311/311) confirmed by script. Still worth a real `yarn dev` look to confirm Singh's sprite renders and positions correctly on the battlefield's new 7th spawn tile in an actual Declare-War-on-Canada battle.
  - **HP/MP and per-unit battle quirks (user feedback: "It would be nice to see HP (Health) and MP (Magic)", plus 8 specific per-character requests):** the biggest engine change to `engine/battle.ts` since it was first built — every attack now runs through a small quirk pipeline instead of a flat "attacker hits defender" roll. Asked the user two clarifying questions first (both are genuinely game-design decisions, not something to guess at): whether Carney's "Charm magic" and Melania's "Sorcerer magic" should be a real player-facing cast action (new UI, target-picker) or an automatic proc like everyone else's quirk — they chose **automatic proc**, so there's no new interactive battle action anywhere; and what the two spells should actually do — they picked **Charm = flips the target to attack their own side for one hit, Sorcerer = a lingering power-halving curse instead of direct damage** (not the other two options offered: skip-a-turn/big-nuke, or self-heal/ally-shield).
    - **The requested roster:** Trudeau (Low HP, heals when hit by a woman — the only woman who can ever hit him is Melania, `isWoman: true`), Carney (High HP + Charm magic), the "Random guy" wildcard/Guy in a Mackinaw Jacket (Really High HP, above Carney's), Miller (50% chance his attack backstabs a random living Canada teammate instead), LeBlanc (50% chance any incoming hit just whiffs — a dodge), Trump (60% chance he flees his own attack instead of landing it — "escaping like a coward" read as backing out on offense, distinct from LeBlanc's defensive dodge), Vance (20% chance his attack redirects onto himself), Melania (Sorcerer magic). `data/battleRosters.ts` doc comment explains each choice, including the HP tier ordering (Trudeau 7 < roster baseline ~10 < Carney 18 < wildcard 24).
    - **`engine/battle.ts`'s `attack()`** is now the single place every quirk resolves (used by both the player's click and `enemyTakeTurn`'s AI, so nothing needed to change at either call site): a charged caster's magic is checked first (cast instead of attacking), then flee, then self-hit-or-backstab (attacker-side redirects — mutually exclusive per unit), then dodge (defender-side, checked against whoever the final defender ends up being), then the heal-vs-damage check, in that order. `BattleUnitTemplate` gained optional `fleeChance`/`selfHitChance`/`backstabChance`/`dodgeChance`/`isWoman`/`healsFromWomen`/`magic` fields; `BattleUnit` gained a runtime `magicCharge: number` (0 initially, +1 every one of that unit's own turns it doesn't cast, reset to 0 on a cast — a simple MP pool with no separate regen-rate config, `magic.max` doubling as both the pool size and the charge-up time). Carney's Charm (`castCharm`) resolves the "target fights for Carney's side for one turn" idea **immediately**, as one forced friendly-fire hit against a random living ally of the target's own side, rather than tracking a "skip your real turn" status across future turns — simpler to implement, and reads just as clearly in the log. Melania's Sorcerer (`castCurse`) permanently halves the target's `power` (floored at 1, stacks diminishingly if cast again), no direct damage.
    - **`BattleLogEntry` is now a 5-kind union** (`'attack'` — now with an optional `healed` flag — plus new `'flee'`/`'dodge'`/`'charm'`/`'curse'` kinds, every kind sharing a common `attackerId`/`defenderId` base so existing generic log-reading code didn't need touching). `ui/battle/BattleView.tsx` gained a `battleLogText` function that switches on `entry.kind` to phrase each one correctly (new i18n keys: `battle.log.heal`/`flee`/`dodge`/`charm`/`charmHit`/`charmNoTarget`/`curse`, en/fr — 320 keys each, parity confirmed by script), replacing the old inline "always an attack" JSX.
    - **UI — actually seeing HP and MP:** `UnitRow` now shows the current HP number next to the existing composure bar (previously just an unlabeled bar) and, only for Carney/Melania, a row of MP pips (filled left-to-right as `magicCharge` builds toward `magic.max`) underneath — a plain bar was considered for MP too but pips read better at this pixel scale for a 0–3 pool, and skip the layout cost of a second bar on the 11 units that don't have one. Both stat rows carry a `title` attribute with the exact numbers for anyone who hovers. New CSS: `.battle-stat-col`/`.battle-stat-row`/`.battle-stat-value`/`.battle-magic-pips`/`.battle-magic-pip`.
    - **Test coverage:** `engine/battle.test.ts` gained two new `describe` blocks (`'attack quirks'`, `'attack magic'`) covering every quirk and both spells individually, plus the "no living ally/teammate to redirect to" fallback cases and the "hasn't charged yet, chance is never even checked" case — every probabilistic test cranks its chance to exactly 1 (or 0) rather than hunting for a seed, same determinism trick this file already used (`power: 99`) for guaranteed one-shots. Existing tests that read `entry.amount`/`entry.defeated` needed a small `asAttack()` type-narrowing helper added, since `attack()`'s return type is now the full `BattleLogEntry` union rather than always `BattleAttackLogEntry` — a real, intentional type change, not a workaround. `data/battleRosters.test.ts` gained a generic invariant test (every quirk/magic chance is a valid 0–1 probability, every magic pool's max is ≥0) plus a specific test asserting each of the 8 requested units actually got its requested quirk and the HP tiers are correctly ordered relative to each other.
    - **Verification:** `tsc` over the full `src` tree came back byte-identical (after accounting for shifted line numbers from the added code) to the pre-change baseline — zero new errors, only the same pre-existing stub-noise category documented throughout this file. The real dynamic `tsx` run of all 28 test files passed 28/28, including every new quirk/magic test actually executing and asserting real outcomes (not just type-checking) — this is exactly the kind of change (RNG branching, redirect logic, a spawn-adjacent stat pipeline) where that real-execution check matters most, not just a nice-to-have. i18n parity (320/320) confirmed by script. Still worth a real `yarn dev` look to see the MP pips and HP numbers render correctly at actual pixel scale, and to watch Carney/Melania's spells actually fire in a live battle.
  - **Fix — battle mode crashed on load (`Cannot read properties of undefined (reading 'replace')` in `translate.ts`, from `UnitRow`'s new HP-label `t()` call):** not a code bug — `src/i18n/en.ts`/`fr.ts` on the device were stale, missing `battle.unit.hpLabel` and several rounds of other recent keys (back to before Jagmeet Singh was even added), despite `device_commit_files` having reported success on every prior sync this session. Root cause never fully pinned down (`device_bash` is still down, so there's no way to inspect the device's filesystem/timestamps directly), but re-verified by staging the "synced" files back down and diffing byte-for-byte against the local copy, rather than trusting the tool's success response alone — the practice going forward for every device sync in this project, not just when something's reported broken. Found and fixed two real discrepancies this pass:
    - `en.ts`/`fr.ts`: genuinely stale on the device (missing everything from the Jagmeet Singh round onward) — re-sent and re-verified identical.
    - `styles/global.css`: the **local** copy was the stale one here, missing mobile/touch-polish CSS (`-webkit-tap-highlight-color`, a `touch-action: none` on `.viewport`, and a `@media (hover: hover) and (pointer: fine)`-scoped hover rule) that only existed on the device, from before this session's "juice" work — my `Read`-then-edit that added `.stage-shake-layer` had unknowingly started from an already-incomplete local file and clobbered the touch-polish CSS when it synced. Fixed by adopting the device's copy (a strict superset — it already had both the touch-polish rules and the shake CSS merged) as the new local source of truth instead of re-pushing the incomplete local version over it.
    - `CLAUDE.md` was also stale on the device (a very old, pre–Phase-5 snapshot) — re-pushed and re-verified identical.
    - Everything else checked in this pass (`engine/battle.ts`, `data/battleRosters.ts`/`.test.ts`, `data/battlefield.ts`/`.test.ts`, `engine/battle.test.ts`, `ui/battle/BattleView.tsx`, `styles/battle.css`, `App.tsx`, `store/gameStore.ts`, the "juice" UI files, and the 4 re-cropped Canada sprite PNGs) matched byte-for-byte between local and device, so this wasn't a systemic sync failure — just these specific files, for reasons still unclear.
  - **Battle music (user supplied `d:\tmp\battle01.mp3`, a real ~3-minute MP3, not a placeholder):** `ui/audio/sfx.ts` gains its one and only real-audio-file exception — `playBattleMusic()`/`stopBattleMusic()`, backed by a plain `HTMLAudioElement` (`loop = true`, `volume = 0.5`) rather than the module's usual synthesized Web Audio oscillators, since generating a 3-minute loop procedurally isn't reasonable. Otherwise a first-class citizen of the module: respects the same `muted` module flag (`setMuted` now also pauses/resumes `battleMusic` in place), and never throws over a playback failure — `.play()` rejections (autoplay policy edge cases) are swallowed exactly like `playSfx` already does. The asset lives at `public/assets/audio/battle01.mp3` (copied in as-is, no transcoding — already a reasonable 64kbps/48kHz stereo MP3).
    - **Lifecycle:** `ui/battle/BattleView.tsx` starts it in a mount-only `useEffect` (`playBattleMusic()`) with `stopBattleMusic()` as the cleanup — `BattleView` only ever mounts while `ResolutionOverlay` is showing the `'battle'` phase and unmounts the instant it isn't, so this plays the track for exactly a battle's duration regardless of whether the battle ends in a win, a loss, or the player backing out mid-fight; no separate win/lose-path handling needed.
    - **Verification:** this session's `node_modules` (including the hand-written `tsx`-runnable vitest shim used for real dynamic test execution) had been wiped since the last round — re-discovered a live copy still sitting in the scratchpad `verify` checkout's own `node_modules` and ran everything from there instead of trying to reinstall. `tsc` over the full `src` tree came back with only the same pre-existing stub-noise category documented throughout this file (nothing new from `sfx.ts` or the new import in `BattleView.tsx`); all 27 `*.test.ts` files ran for real via `tsx` and passed 27/27 (no test added specifically for the music feature itself — it's a browser-only `HTMLAudioElement` side effect with no pure-engine logic to unit-test, consistent with this repo's no-React-rendering-test precedent).
    - Synced `sfx.ts`, `BattleView.tsx`, and `battle01.mp3` to the device via `SendUserFile` + `device_commit_files`, then re-verified all three byte-for-byte via a fresh `device_stage_files` + diff — per the practice adopted in the sync-bug fix above, not just trusting the "written" response. All three matched exactly.
    - Not yet confirmed in a live `yarn dev` session: whether the browser actually allows autoplay on battle entry (depends on whether a user gesture already happened that tab) — if it doesn't play on the very first battle, a click anywhere first should unblock it, same as any other autoplay-gated audio.
  - **Fix — the box-shadow rectangle around each unit sprite (user: "The rectangle under the characters came back"), plus a country flag pastille (user: "put a pastille with the logo of the country under the character"):** `styles/battle.css`'s `.battle-unit-iso.is-us`/`.is-enemy`/`.is-current` drew a 1–2px `box-shadow` ring directly around the whole unit sprite to mark its side and whose turn it was — fine over the original colored-square placeholders, but a loud, distracting rectangle once real character art (Phase 4's US sheet, this session's Canada sheets) was sitting inside it. Removed entirely and replaced with two smaller, more legible signals:
    - **A flag pastille under each unit's feet:** a small 10px circle, ringed gold (`is-us`) or red (`is-enemy`) — same at-a-glance side information the rectangle gave, just contained to a badge instead of framing the whole sprite — showing a Unicode flag emoji as the placeholder "country logo" (no real crest/logo art exists yet, same placeholder spirit as the coloured-square sprites and the synthesized SFX). `data/countries.ts`'s `CountryDef` gained a `flag` field (one emoji per country, doc-commented as a placeholder for real art later) plus a standalone `US_FLAG` constant (the US is the player, never a `CountryId`, so it doesn't belong in `COUNTRIES`). `BattleView.tsx` picks `US_FLAG` for `side: 'us'` units and `getCountry(resolution.battleCountry).flag` for enemy units.
    - **A bobbing `▼` marker over the current unit's head** (Shining Force/Fire Emblem-style turn pointer, `@keyframes battle-unit-marker-bob`, already covered by the project's existing `prefers-reduced-motion: reduce` blanket disable) replacing the old cream box-shadow ring for "whose turn it is."
    - **`BattleUnitToken`** went from a single sprite element carrying all three box-shadow variants to a small wrapper (`.battle-unit-iso`, still the one absolutely-positioned/z-indexed element) containing the marker (conditional), the sprite/placeholder (now `.battle-unit-sprite`), and the flag pastille, stacked in a column — the sprite itself no longer carries any side-specific styling at all.
    - **Verification:** `tsc` over the full `src` tree came back with the exact same error count (45) as the pre-change baseline, only shifted line numbers from the new code — confirmed no new error category, not just eyeballed. All 27 `*.test.ts` files ran for real via `tsx` and passed 27/27, including a new `countries.test.ts` assertion that every country's `flag` is non-empty, unique, and distinct from `US_FLAG`. Synced `countries.ts`, `countries.test.ts`, `BattleView.tsx`, and `battle.css` to the device via `SendUserFile` + `device_commit_files`, then re-verified all four byte-for-byte via `device_stage_files` + diff. Still worth a real `yarn dev` look to see the pastilles and bobbing marker at actual pixel scale.
  - **Bigger battlefield with a scrolling Shining-Force-style camera (user: "we could make the battle much bigger and we could scroll the map like in Shining Force"):** asked two clarifying questions first, since this was a real architecture change to a screen whose doc comment explicitly said "never panned — a tactical battle shows the entire field at once" — whether the camera should only auto-follow the active unit each turn or also let the player manually pan, and whether the battle screen should go full-screen (map takes over, rosters become a compact overlay) or keep the existing 3-column layout just with a bigger center map. User picked **auto-follow + manual pan** and **full-screen**.
    - **`data/battlefield.ts` grew from 11×8 to 20×14** (280 tiles, up from 88) — big enough that the projected grid (544×294px) no longer fits in the battle viewport on either axis at this session's estimated viewport size, so there's genuinely something to scroll. The obstacle cluster grew from a 2-tile pair to a 6-tile plus-shape-plus-two-loners rather than just stretching the same two tiles across a much bigger open field, and `US_SPAWN_POSITIONS`/`ENEMY_SPAWN_POSITIONS` were re-centered on the new interior (rows 1–12) rather than reused verbatim from the old 8-row field. `battlefield.test.ts`'s assertions are fully generic (border walls, spawns on open floor, no spawn collisions, spawn count ≥ largest roster) so they re-validate the new layout for real rather than needing new hardcoded expectations.
    - **`ui/battle/battleCamera.ts`** (new, pure, DOM-free, fully unit-tested) holds the actual math: `centerOn(point, viewportSize)` computes the raw "put this point in the exact center" offset; `panCamera(camera, dx, dy)` adds a manual-pan delta; `clampCamera(camera, gridSize, viewportSize)` clamps per axis so the map can never scroll past showing blank space beyond its own edge — except an axis where the grid is smaller than the viewport, which centers instead of clamping to a min/max range (a clamp range would have min > max there). Same "pure logic file has its own real tests, the React hook wrapping it doesn't" split this project already uses for `room/playerDepth.ts` vs. `RoomView.tsx`'s `usePlayerDepth`.
    - **`ui/battle/useBattleCamera.ts`** (new) is the React glue: re-centers on the active unit whenever a `followKey` built from both the unit's id _and_ its grid position changes (so moving the active unit mid-turn re-centers the camera on them again, not only a turn change), and exposes `pan(dx, dy)` for `BattleView.tsx` to call from keyboard/drag input. Manual pan during a turn is left alone by everything else — only a real turn/position change or the viewport's very first real measurement (`useElementSize`'s `ResizeObserver`, reused as-is from `RoomView.tsx` — no reason to write a second version) recenters it.
    - **`BattleView.tsx`**: the map now renders inside a `.battle-viewport` (fixed to whatever size `.battle-body`'s flex layout gives it, `overflow: hidden` — same shape as `RoomView.tsx`'s existing `.room-viewport`) with `.battle-grid-iso` translated by the camera offset inside it. Arrow keys pan by one tile (`PAN_STEP`, `KEY_PAN`) via a keydown handler on the focused viewport; a pointer drag pans continuously, converting client-px deltas to stage-px deltas by dividing by `useStageScale()` (otherwise a drag would scroll faster than the cursor at any zoom above 1×) and suppressing the click that would otherwise fire on whatever tile ends up under the pointer at release (`suppressClickRef` + a capture-phase click handler — without it, releasing a drag over a tile would accidentally move or attack with it). The camera glides on a 160ms CSS transition normally, overridden to instant (`0ms`, via `CAMERA_TRANSITION_MS`) while actively dragging so a hand-pan tracks the pointer 1:1 instead of easing behind it.
    - **Roster panels became compact overlays** (`.battle-roster-compact`, absolutely positioned in the top corners over the map, `.battle-roster-us`/`.battle-roster-enemy`) instead of full-height flex columns competing with the map for width — this is most of where "full-screen" actually comes from, since `.battle-body` was already the full remaining stage height either way; the map now gets nearly the whole area instead of a third of it.
    - **Verification:** `tsc` over the full `src` tree added new errors versus the prior baseline (30 vs. 45, since the file moved around, but every single one individually confirmed, line by line, to be the same already-documented hand-rolled-stub-gap category — `useRef`/`KeyboardEvent`/`CSSProperties`/`RefObject` missing from the `react` stub, now joined by `MouseEvent`/`PointerEvent` for the same reason, and `ref`/`src`/`role`/`disabled`/`scope` missing from the JSX-intrinsics stub, now joined by `tabIndex`/`onPointerDown`/`onPointerMove`/`onPointerUp`/`onPointerCancel`/`onClickCapture` — a small script paired every error with its continuation line and flagged anything outside that known set; it found nothing). One genuinely new issue the stub caught correctly: `style={{ transitionDuration: isDragging ? '0ms' : undefined }}` doesn't type-check against this stub's `Record<string, string | number>` (no `undefined`) even though real React's `CSSProperties` allows it — fixed by always providing a string (`CAMERA_TRANSITION_MS`) instead of relying on `undefined` to mean "unset." All 28 `*.test.ts` files ran for real via `tsx` and passed 28/28, including the 6 new `battleCamera.test.ts` cases (centering, panning, both clamp directions, the smaller-than-viewport centering case, and the exactly-equal case). Synced all 8 changed/added files (`BattleView.tsx`, `battleCamera.ts`, `battleCamera.test.ts`, `useBattleCamera.ts`, `battle.css`, `en.ts`, `fr.ts`, `battlefield.ts`) to the device via `SendUserFile` + `device_commit_files`, then re-verified every one byte-for-byte via `device_stage_files` + diff.
    - **Not yet confirmed in a live `yarn dev` session** (no real browser available in this sandbox): whether the estimated viewport size actually leaves enough room for a legible map at this pixel-art scale, whether the drag-vs-click threshold (`DRAG_CLICK_TOLERANCE`, 5px) feels right, and whether the compact roster overlays sitting over the map corners ever obscure a unit spawned near a top corner.
  - **Fix — drag-to-pan direction, plus reworking the pastille into an oval color badge (user: "The scroll is not working great. It should follow the mouse instead of following the isometric weird angles. Also, the pastille should be under the character like if it was sitting on it. You can give it [an] oval [shape] and a color."):** two real bugs/gaps from the previous round, both from an actual screenshot of the game running on the user's Windows machine.
    - **Drag-to-pan had the wrong sign.** `handleViewportPointerMove` panned the camera by `-dx/-dy` — scrollbar-style ("push the view", content moves opposite the cursor) — when a drag-to-pan gesture should be grab-style ("push the map itself", content moves _with_ the cursor, so whatever point was under the cursor at pointerdown stays under it as it moves). Flipped to `pan(dx / scale, dy / scale)` (no sign flip). The user's "following the isometric weird angles" description was this same bug: panning opposite of the drag direction reads as extra disorienting on a diamond-tile isometric map versus a plain grid, not a separate isometric-specific bug — the camera's `translate()` is a plain 2D CSS transform with no rotation/skew of its own; only the individual tiles' static positions use the isometric projection formula.
    - **The flag emoji pastille rendered as plain text on Windows.** Confirmed by the user's own screenshot: 🇨🇦 showed as literal "CA" letters instead of a flag picture — Windows' emoji font deliberately renders many flag emoji as two-letter country-code text rather than a flag image (unlike macOS/Android), a known platform quirk, not a bug in this app's code. Per the user's own suggestion ("give it an oval and a color"), replaced the emoji entirely with a flat color: `data/countries.ts`'s `CountryDef.flag` field (and the `US_FLAG` constant) became `color`/`US_COLOR` — plain hex strings, same convention `battleRosters.ts`'s `placeholder.color` already uses, so no font/glyph support is needed at all. `countries.test.ts`'s flag-uniqueness test became a hex-color-validity + uniqueness test (`toMatch(/^#[0-9a-f]{6}$/i)`).
    - **The pastille is now a real oval** (`.battle-unit-flag`: 16×7px, `border-radius: 50%` on a non-square box renders an ellipse) filled with the unit's color, no text/emoji content at all — and it visually overlaps the sprite's bottom edge (`margin-top: -6px`) instead of sitting below it with a gap, so the character reads as standing on it rather than the two being stacked separately. Getting the _paint_ order right (character in front of/on top of the oval) while still wanting the oval positioned _last_ visually (at the feet) needed decoupling layout position from stacking order: `BattleUnitToken` renders the oval before the sprite in DOM order (so it paints underneath, following normal DOM-order stacking), and `battle.css` gives the sprite `order: 1` / the oval `order: 2` to move the oval below it visually — CSS `order` reorders flex layout position but not paint order, so this gets both "oval renders behind" and "oval sits at the feet" at once without a z-index hack.
    - **Verification:** `tsc` over the full `src` tree came back with the same 30 known-category errors as the prior round (script-verified line by line again, nothing new or unexpected). All 28 `*.test.ts` files ran for real via `tsx` and passed 28/28, including the updated `countries.test.ts` color assertions. Synced `countries.ts`, `countries.test.ts`, `BattleView.tsx`, and `battle.css` to the device — the first `device_commit_files` call reported success but all four files came back stale on re-staging (the same silent-write failure documented earlier this session, not a one-off); a retry of the same call and a fresh re-stage confirmed all four byte-for-byte.
    - **Still not confirmed in a live browser**: the exact drag feel (does 1:1 client-to-stage-px conversion feel right at every zoom level), and whether the oval's overlap amount (`-6px`) looks right against the actual sprite art's own transparent padding, which varies slightly per character.
  - Battle scrolling round 2 — tile clicks broken, auto-end-turn, pastille stacking (user feedback: "I cannot select a tile anymore. Also, when the player move, and cannot attack, it should end it's turn automatically. ... The pastille should be drawn before the character (zindex lower)"):
    - **Tile-click regression**: real bug — the drag-pan handlers called `setPointerCapture` and started panning on the very first `pointermove`, even for a plain click with near-zero movement, which stole the click away from the tile buttons before it could fire. Fixed with a deferred-drag-start pattern in `BattleView.tsx`: `dragRef` now carries a `dragging: boolean` flag, and nothing drag-related (pointer capture, `setIsDragging`, click suppression) happens until the pointer has moved past `DRAG_CLICK_TOLERANCE` (5px) from its `pointerdown` origin. A plain click never crosses that threshold, so it's never touched by drag logic at all, and tile `onClick` fires normally again.
    - **Auto-end-turn after a move with no attack targets**: new behavior, not a bug fix — real friction the user called out ("it should end it's turn automatically"). `gameStore.ts`'s `battleMove` action now checks, right after a move actually happens, whether the mover has any `targetsInRange` left; if not, it calls `endTurn` (same as the manual "End Turn" button) instead of leaving the turn open. Attacking itself is still always a manual click via `battleAttack` — who to hit stays the player's choice, never auto-picked. No dedicated test was added for this store-level behavior, consistent with this project's standing precedent of not unit-testing the Zustand store layer directly and instead relying on `engine/battle.ts`'s own coverage of the underlying `moveUnit`/`targetsInRange`/`endTurn` functions it calls.
    - **Pastille painting on top of the character instead of underneath it**: real bug, and a mistaken fix from the previous round. The prior pass assumed CSS `order` only affects layout position and that paint order follows raw DOM order — so it put the oval first in the markup and used `order: 2`/`order: 1` to visually push the sprite above it, expecting the sprite (earlier in DOM order) to paint on top regardless. That assumption is wrong: per the Flexbox spec, flex items with `z-index: auto` (the default) paint in _order-modified document order_, so the oval — having the higher `order` value — was actually painting last, i.e. on top, the whole time. Fixed properly in `battle.css`/`BattleView.tsx`: the JSX went back to natural DOM order (turn marker, then sprite, then oval), and both elements got explicit, non-`auto` `z-index` values (`.battle-unit-sprite { z-index: 2 }`, `.battle-unit-flag { z-index: 1 }`), which opts them out of order-based stacking entirely and into normal numeric z-index comparison — the oval now reliably paints underneath the sprite at their overlap.
    - **Verification**: `tsc --project tsconfig.verify-ui.json` — 30 errors, all re-confirmed (via the same error-header/continuation-line pairing script used every round) to fall in the sandbox's known category (the hand-rolled `react`/JSX type shim's missing members — `useRef`, pointer/keyboard event types, `style: Record<string,...>` without `undefined`, etc.), none new. Full test suite (`tsx --tsconfig tsconfig.app.json` over all 28 `*.test.ts` files) — all passing, unchanged by this round since none of the three fixes touched pure `engine`/`data` logic covered by tests. Synced to the device (`BattleView.tsx`, `battle.css`, `gameStore.ts`) and confirmed byte-identical via re-stage-and-diff; as has now happened on every device sync this entire session, the first `device_commit_files` attempt for all three files came back reporting success but was stale on re-staging, and a retry with `force: true` fixed it on the second attempt.
    - **Still not confirmed in a live browser**: whether the click-vs-drag threshold (5px) feels right rather than occasionally eating a very fast intentional drag as a click, and whether auto-ending the turn immediately after a no-target move ever feels too abrupt (e.g. if the player wanted a beat to see the new position before the turn passes).
  - **Real flag-badge art for the battle pastille:** the user supplied a single reference sheet (`public/assets/sprites/source/country-flags-sheet.png`) with 8 gold-ringed oval flag badges — the US plus all 7 `CountryDef` countries — replacing the flat-color oval placeholder from the previous round with real per-country art, same "real sprite replaces colored-square placeholder" upgrade path as the US/Canada character rosters got earlier.
    - **Cropping:** same alpha-bounding-box family of technique as the character-sheet crops (`CLAUDE.md`'s Phase-4/Canada-roster entries) but via connected-component labeling (`scipy.ndimage.label` on an `alpha > 10` mask) instead of a single bounding box, since this sheet has 8 separate badges on one transparent-background canvas rather than one figure per sheet. Found 8 components of consistent size (~330×250px, matching across all 8 — confirms they're all the same badge template with different flag art), sorted into a 2-row×4-col grid by position, matched to country ids by the user's own row-by-row labeling in their message, and resized down to 110px wide (matching the existing character-sprite width convention, ~99–111px) preserving aspect ratio. A contact-sheet preview render confirmed every crop was correctly cropped and correctly labeled before wiring any code.
    - **Data model:** `CountryDef` gained an optional `badge?: string` field (`data/countries.ts`) alongside the existing `color` field — optional and additive, not a replacement, so a future country added without art still renders via the same "real art if present, else a colored placeholder" fallback pattern `characters.ts`'s `sprite`/`placeholder` pair already established (`BattleUnitToken` now checks `badge ? <img> : <span style={{background: color}}>`, exactly mirroring its existing `unit.sprite ? <img> : <div placeholder>` check just above it). `US_BADGE` added next to `US_COLOR` for the player's own side. `color` stays as the fallback and is still what's asserted first in `countries.test.ts`; two new tests assert every country (and `US_BADGE`) has a `badge` path matching `/assets/sprites/flags/*.png` and that `US_BADGE` isn't reused as a country badge.
    - **Styling (`battle.css`):** split `.battle-unit-flag` into a shared base rule (z-index, box-sizing) plus two variants — `:not(.battle-unit-flag-badge)` keeps the old flat 16×7px oval with the side-colored box-shadow ring, only used for the (currently theoretical) case of a country with no badge art; `.battle-unit-flag-badge` sizes the real artwork closer to its own ~110×84 source aspect ratio (14×11px, `object-fit: contain`) with no extra border-radius/box-shadow, since the gold ring and glow are already baked into the image itself.
    - **Verification**: `tsc --project tsconfig.verify-ui.json` — 31 errors (up from the prior round's 30 by exactly 1), the new one re-confirmed to be the same already-known "shim's `img` JSX type is missing `src`" category as the two pre-existing `<img>` elements in this same file (`battle-roster-swatch`, `battle-unit-sprite`) — not a new category, just one more instance of it from the new `<img>` for the badge. Full test suite (`tsx --tsconfig tsconfig.app.json`, all `*.test.ts` files, now 28 files / `countries.test.ts` at 14 cases up from 12) — all passing. Synced to the device: the 4 changed text files (`countries.ts`, `countries.test.ts`, `BattleView.tsx`, `battle.css`) landed byte-identical on the _first_ `device_commit_files` attempt this time (no stale-restage retry needed, unlike every prior round). The 9 new PNGs (8 cropped badges + the source sheet) did **not** come back byte-identical on re-staging even after landing — but a pixel-level comparison (`PIL`, converted to RGBA arrays) confirmed the actual image content is 100% pixel-identical; the only difference is a `caBX`/`jumb`/`jumdc2pa` chunk (a C2PA "Content Credentials" provenance chunk) inserted into every PNG right after its `IHDR`, adding exactly 5770 bytes to every file regardless of original size or content — something in the device-side pipeline (most likely a Windows OS-level or antivirus content-provenance tagging feature, not this app or the sync tooling) stamping transferred images, not a transfer corruption. PNG ancillary chunks like this are safely ignored by any real PNG decoder (confirmed the staged file still opens and `PIL.Image.verify()`s clean), so this doesn't affect how the badges render — noted here as a new, distinct-from-the-stale-write-bug device-sync quirk specific to binary image files, in case file sizes look unexpectedly larger on the device again in the future.
    - **Still not confirmed in a live browser**: whether the 14×11px badge size reads clearly at 1× zoom (vs. the old flatter 16×7px oval), and whether the badge's own baked-in glow looks good sitting that close to the sprite's feet without the extra side-colored ring the flat-oval fallback still has.
  - **Declare War no longer requires low Party IQ** (real user feedback: "ensure we can always Declare war to a country, no need to have a low IQ"): `data/actions.ts`'s `declare_war` action dropped its `requires: { iqMax: 70 }` gate entirely — it now shows up in the Situation Room regardless of the player's current Party IQ, same as most of the catalog. `declare_war_object` (the Sun/windmills gag action) still keeps its own `iqMax: 50` gate; the user's ask was specifically about declaring war on a _country_, and that one has no battle behind it to block. `docs/GAME_PLAN.md`'s §9 action-catalog table (row 1's "IQ gate" column) updated to match. `data/actions.test.ts`'s existing "gates the most spectacular actions behind low Party IQ" test had `declare_war` removed from its expected list (it was asserting the very behavior being removed), and a new dedicated test asserts `getAction('declare_war').requires?.iqMax` is `undefined` so a future change can't silently re-add the gate. Verified via tsc (31 errors, same count as before this change — none new) and the full test suite (`actions.test.ts` now 11 cases, up from 10, all passing); synced to the device (`actions.ts`, `actions.test.ts`, `GAME_PLAN.md`) and confirmed byte-identical on the first `device_commit_files` attempt.
  - **Battle sound cues** (small polish pass, budget-conscious per user request — items chosen this round were code-only, needing no image/audio-generation tool or a working build): `ui/battle/BattleView.tsx` had zero audio feedback for actual battle actions, only the ambient `playBattleMusic` loop — a real gap given battle is the game's newest, most-played feature. Added `sfxForLogEntry`, an exhaustive `switch` over every `BattleLogEntry.kind` (`attack`/`flee`/`dodge`/`charm`/`curse`) picking from the existing `SfxId` vocabulary (`blip` for a plain landed hit, `success` for a KO, `confirm` for a heal or Melania's curse, `cancel`/`fail` for anything that didn't land), wired to a `useEffect` keyed on `battle.log.length` — covers the enemy's attacks too, for free, since `battleRunEnemyTurn` appends to the same log. A move (which appends no log entry) gets a plain `blip` from `handleTileClick` directly, matching the rest of the UI's synchronous-click convention. Verified via tsc (31 errors, same known category, none new) and the full test suite (all passing, unchanged — this is UI logic, no `engine`/`data` change); no dedicated test added, consistent with this repo's precedent of not unit-testing logic embedded in `.tsx` files. Synced to the device, byte-identical after the usual one retry.
  - **Mute preference now persists across reloads** (small polish pass, continued): `ui/audio/sfx.ts`'s mute flag had no persistence at all — muting reset to "on" every reload/relaunch, unlike the language choice next to it in the codebase. Added `MUTE_STORAGE_KEY = 'maga.muted'` plus `initialMuted`/`saveMuted`, the exact same `localStorage`-with-try/catch pattern `gameStore.ts`'s `LANG_STORAGE_KEY`/`initialLang`/`saveLang` already use (same `maga.` key prefix, same private-mode/blocked-storage fallback). Deliberately still lives in `sfx.ts`, not `gameStore.ts`/`SavedGame` — `MuteToggle.tsx`'s doc comment already explained mute is a browser preference, not part of a run, and that split is unchanged, just given its own storage key instead of no storage at all. Verified via tsc (31 errors, same known category, zero in either touched file) and the full test suite (all passing, unchanged); no dedicated test added, consistent with this repo not testing `AudioContext`/`localStorage`-dependent browser modules. Synced to the device (`sfx.ts` needed the usual one retry, `MuteToggle.tsx` landed clean on the first attempt).
  - **Real-bug audit + fix batch** (user asked for "much more" than the two single-item rounds above — a subagent read-only-audited `store/gameStore.ts`, every `ui/*.tsx`, and `engine/*.ts` for concrete bugs, not style nits; 4 of its findings were fixed this round, ranked by how likely each was to actually bite a real player):
    - **HUD Executive Action pips, and the impeachment-watch "X of Y months" line, were wrong on any non-Normal difficulty.** `ui/hud/Hud.tsx` and `ui/screens/MonthEndReport.tsx` both read the always-Normal `BALANCE` constant directly (`BALANCE.actionsPerMonth`, `BALANCE.thresholds.impeachmentStreakMonths`) instead of `getBalance(difficulty)` — Intern (4 EA/month, 4-month impeachment threshold) and Third Term (2 EA/month, 2-month threshold) both showed Normal's numbers (3 and 3) the entire run. Fixed by threading `difficulty` (already a plain store field) through both components; `calendar`-based reads (`termYear`, midterms date) were left on `BALANCE` since `data/balance.ts`'s own doc comment confirms `calendar` never varies by difficulty, only the economy/threshold knobs do.
    - **Melania's hide-and-seek bonus (GAME_PLAN §8) was uncollectable whenever her freshly-rolled room happened to be the room the player was already standing in** — at both `endMonth` (the common case) and game start (`freshGame`, since `MELANIA_ROOMS` explicitly includes the Oval Office, the player's own starting room, and its own doc comment calls that "a fine gag"). The bonus only ever fired on a door _transition_ into her room (`movePlayer`); re-rolling her location doesn't move the player, so there was never a transition to catch it on — a real bug contradicting the feature's own stated intent. Fixed at both call sites: if her new room matches the player's current room, grant the bonus immediately the same way a door-walk-in would. `endMonth`'s fix also avoids clobbering a real monthly-event ticker headline with the smaller "Melania was here" line when both would otherwise land the same month.
    - **`endMonth` could fire mid-resolution and corrupt the save.** `GameScreen.tsx` renders the toolbar's End Month button unconditionally — `ResolutionOverlay`/`ActionsOverlay`/`MapScreen`/`BattleView` are all layered on top of it via CSS, not swapped in for it, so the button stays mounted and keyboard-reachable (Tab) underneath any of them, unlike `movePlayer`, which already guards against exactly this. Firing `endMonth` while a resolution was in progress ticked the month from the stale pre-action `game` snapshot, and the abandoned `resolution` survived the screen swap to `monthReport` — returning to the room and confirming that stale resolution afterward (`continueResolution`) would silently overwrite the just-advanced month's state with the old pre-tick one. Fixed with the same guard `movePlayer` already has: `if (screen !== 'game' || overlay !== null || resolution !== null || game.ending) return;`. The subagent also flagged the deeper accessibility half of this (no focus trap on any modal overlay, so Tab can reach background controls at all) — that's a real, larger CSS/focus-management lift, not fixed this round; noted for a future pass.
    - **`selectBattleCountry` indexed `US_SPAWN_POSITIONS[i]`/`ENEMY_SPAWN_POSITIONS[i]` with an unchecked `as GridPosition` cast** past `noUncheckedIndexedAccess` — if any roster ever grew past its side's spawn-array length (Canada's roster already grew once this session, 6 → 7, when Jagmeet Singh joined it), this would silently produce `undefined` cast as a valid position instead of a caught error, surfacing confusingly later wherever that unit's position is first read. Rather than add a runtime guard (not this codebase's style — it leans on tests to enforce data-shape invariants, per its own established convention), strengthened `battlefield.test.ts`'s existing spawn-capacity test to check every real roster's actual length against the real spawn-array lengths (`SWITCHABLE_CHARACTERS.length`, every `BATTLE_ROSTERS` entry) instead of hardcoded numbers in a comment, and fixed `battleRosters.test.ts`'s sibling check off a hardcoded `"7"` the same way — either would have masked exactly this class of regression before.
    - Three other real findings from the same audit were intentionally **not** fixed this round, to keep scope sane: a full focus-trap/`inert` pass on every modal overlay (noted above — genuinely bigger than the rest of this batch combined), a roving-tabindex fix for the title screen's difficulty `role="radiogroup"`, and more descriptive per-tile `aria-label`s on the battle grid (currently just raw `"x,y"` coordinates) so a screen-reader user could actually tell reachable/attackable tiles apart.
    - **Verification**: `tsc --project tsconfig.verify-ui.json` — 31 errors, same known category confirmed again (plus one pre-existing `role`-prop error in `MonthEndReport.tsx` now individually confirmed rather than just counted), none new. Full test suite: all passing, `battlefield.test.ts`/`battleRosters.test.ts` unchanged in count (4/8) since these strengthened existing assertions rather than adding new ones. Ran `prettier --write` on every touched file this round (available in this sandbox, unlike `vitest`/`vite`) rather than hand-formatting a >100-column line. Synced to the device — `gameStore.ts` needed the usual one retry, the other 4 files landed clean on the first attempt.
  - **Closed out the rest of the audit batch** (user asked to keep going with "much more" — the 3 findings the previous round deliberately deferred, all fixed this round):
    - **Battle tiles had no real accessible name.** `ui/battle/BattleView.tsx`'s `BattleTile` used to set `aria-label` to the raw grid key (`"5,3"`) regardless of what was actually on that tile — a screen-reader user navigating the battle grid heard an undifferentiated list of coordinates, with no way to tell an empty tile from a reachable one, an ally, or an attackable enemy (exactly the information the sighted highlight colors already convey). Fixed by threading the tile's occupant and highlight state into 4 new i18n keys (`battle.tile.empty/reachable/occupied/attackable`, added to both `en.ts` and `fr.ts`, reusing the existing `unitLabel` helper for the occupant's name) instead of the bare coordinate.
    - **The difficulty picker announced as a radiogroup but didn't behave like one for keyboard/AT users.** `ui/screens/TitleScreen.tsx`'s 3 `role="radio"` buttons each kept their own Tab stop; a native radiogroup gives only the checked option a Tab stop and moves both focus _and_ selection with arrow keys. Implemented the real WAI-ARIA roving-tabindex pattern: `tabIndex={difficulty === id ? 0 : -1}`, a small ref array (`difficultyButtonRefs`) so arrow/Home/End keys can move DOM focus to a neighbor, and the same `setDifficulty` call the existing click handler already made (factored into a shared `chooseDifficulty` so both paths stay identical).
    - **No modal overlay trapped focus, so a keyboard user could Tab straight into the still-live room/toolbar underneath any of them** — the deeper issue the `endMonth`-mid-resolution bug (previous round) was one _consequence_ of, not the only one. `ui/screens/GameScreen.tsx` now wraps its room content (chrome, viewport, toolbar) in a `.game-screen-content` div and sets the real `inert` HTML attribute on it whenever `overlay` or `resolution` is non-null — `inert` pulls the whole subtree out of both the tab order and the accessibility tree in one line, no hand-rolled focus trap needed. `styles/game.css`'s new `.game-screen-content` rule is `display: contents` so the extra wrapper element doesn't disturb `.game-screen`'s existing flex layout (every overlay it now shadows is already `position: absolute`, out of flow, so nothing else needed adjusting).
    - **Verification**: `tsc --project tsconfig.verify-ui.json` — 36 errors, up from 31; every single new one individually confirmed (not just counted) to fall in the three already-known shim-limitation categories (`Module "react" has no exported member X` for `useRef`/`KeyboardEvent`; the JSX-intrinsics narrow-prop-union mismatch, now also hit by `inert`/`ref`/`tabIndex`/`onKeyDown` on top of the props it already caught; and the implicit-`any` cascades those two categories cause on inline callback parameters) — grepped the full error list for anything outside those three patterns and got zero matches. Full test suite: all passing, including `translate.test.ts`'s en/fr key-parity and non-empty-string checks against the 4 new i18n keys. `prettier --write` run again on every touched file. Synced to the device — `BattleView.tsx` needed the usual one retry, the other 5 files landed clean on the first attempt.
    - **Still not confirmed in a live browser/screen reader**: none of this round's fixes have been checked with an actual assistive-tech tool (VoiceOver/NVDA) — `inert`'s browser support is solid (`Chrome`/`Firefox`/`Safari` all ship it), but the specific _experience_ of tabbing through a modal now vs. before is still only verified by reading the code, same limitation as every prior round's UI work in this sandbox.
  - **Built a real tactical-battle balance simulator and used it to fix Canada** (user feedback: "are you able to simulate battle with tests? I ask because right now, it's almost impossible to win a battle against Canada"). New `scripts/simulateBattle.ts` — same "pure logic, runnable directly with `tsx`, no vitest import" shape as the existing `scripts/simulate.ts` economy Monte Carlo — plays out thousands of full battles using the _real_ `engine/battle.ts` combat resolution (`createBattle`/`attack`/`targetsInRange`/`moveUnit`/`endTurn`) and the real `data/battleRosters.ts`/`data/battlefield.ts` data, with a `takeTurnAI` helper that generalizes `engine/battle.ts`'s own `enemyTakeTurn` heuristic ("attack weakest in range, else close the distance") to work for _either_ side — using the identical heuristic for both sides keeps the comparison fair, so a lopsided win rate reflects the actual roster data, not one side having a smarter simulated player.
    - **The user was right, and it wasn't close**: 2000 simulated battles per country found every other war target (Greenland, Panama, Mexico, Iran, Venezuela, Russia — all 3-4 enemy units) already loses to the US's fixed 6-unit cabinet 94-100% of the time, but Canada's US win rate was a flat **0.0%** — a mathematical lock, not just "hard."
    - **Root cause, confirmed empirically rather than guessed**: Canada is the _only_ roster with more than 6 units (it grew to 7 when Jagmeet Singh joined it, an earlier session's deliberate content addition). `createBattle`'s `interleave` builds a fixed US/enemy/US/enemy turn order, so a 7-unit enemy against a fixed 6-unit US side means Canada's 7th unit gets one fully unanswered action _every single round, forever_ — confirmed by simulating a hypothetical 6-unit Canada (dropping the wildcard, stats otherwise untouched), which alone brought the US win rate to ~19%, still brutal but nowhere near 0%. Individual HP/power trims tested in isolation (-20% to -35% HP across the roster) barely moved the number (0.8%-7.3%) — proof the turn-count structural edge, not the per-unit stats, was the dominant factor.
    - **Fix**: rather than remove a character (all 7 are deliberately-added named officials, GAME_PLAN §14), halved every Canada unit's `maxComposure` in `data/battleRosters.ts` (Carney 18→9, Trudeau 7→4, Miller/Joly/LeBlanc 10→5, Singh 12→6, the wildcard "Mackinaw guy" 24→12) — confirmed at full 2000-battle scale to bring the US win rate to **~43%**: still clearly the hardest fight in the game (every other country is 94-100%), but no longer a literal lock. The doc comment at the top of `data/battleRosters.ts` records the full reasoning (with numbers) so a future stat change doesn't accidentally reopen this; the existing relative "Low/High/Really high HP" inline comments were left as-is since a uniform halving preserves their ordering.
    - **New regression guard, same pattern as `scripts/simulate.test.ts`**: `scripts/simulateBattle.test.ts` asserts Canada's US win rate stays strictly between 20% and 70% (catches both "someone re-breaks this" and "someone overcorrects it into a joke"), and that every other country's US win rate stays above 70%.
    - **One side effect noted, not fixed**: average battle length roughly doubled (11 → 27 rounds) and ~11% of simulated Canada battles now hit the simulator's 400-turn safety cap without resolving — inspected a few of these directly and they're genuine long grinds between a lone low-power survivor and LeBlanc's 50% dodge chance or Trudeau's heal, not an infinite-loop bug (the real engine has no turn cap and always converges given nonzero expected net damage per exchange). Flagged here in case a real playtest surfaces occasional very long Canada fights as a pacing complaint.
    - **Verification**: full test suite (`tsx --tsconfig tsconfig.app.json` over all `*.test.ts` files, now 30 files including the two new ones) — 296 cases, 0 failures. `tsc --noEmit -p tsconfig.app.json` — 1 error, individually confirmed to be a pre-existing sandbox-only gap entirely unrelated to this round's changes: `Cannot find type definition file for 'vite/client'`, a global `compilerOptions.types` entry, not tied to any specific file — the real `vite` package has simply never been installed in this checkout (this tsconfig's `include` is `["src"]` only, so `scripts/` isn't even in scope for it; the two new script files were instead confirmed correct via direct `tsx` execution, matching `scripts/simulate.ts`'s own precedent). `prettier --write` run on every touched/new file. Synced to the device (`battleRosters.ts`, `simulateBattle.ts`, `simulateBattle.test.ts`) — `battleRosters.ts` needed the usual one retry (`force: true`) after a stale first write, the two new script files landed clean on the first attempt.
  - **New random event: Canada joins the EU** (user request, riffing on the game's "51st state" satire from the other side — Canada trolling back). `data/events.ts` gained `canada_joins_eu` (weight 5, same tier as the other big one-off geopolitical stories like `foreign_crisis`/`weather_balloon_panic` rather than an everyday item): Headlines +12, Happiness −4, DEFCON −1 — a huge, faintly embarrassing diplomatic stunt, not an existential one. Left deliberately ungated: the event system's `requires` only supports `flags: string[]` (checked against `GameState.flags`), while the war/purchase system tracks country state in separate `atWarWith`/`countriesOwned` arrays with no flag bridge between them — gating this on "Canada specifically" would need a real (if small) engine change, out of scope for a single flavor event; every other event in the catalog bar one (`reactor_inspection`) is similarly ungated. New i18n keys added to both `en.ts`/`fr.ts`; `data/events.test.ts`'s hardcoded event count bumped 23→24.
  - **Removed the permanent "??" next to Inflation on the HUD** (user report). Investigated first rather than just deleting the string: the HUD was deliberately showing official inflation next to a _hidden_ "felt inflation" figure (`GameState.feltInflationRevealed`, `data/balance.ts`/GAME_PLAN §4's design), meant to be exposed later by a Bessent "Reveal" action or an event — but grepping the whole codebase found nothing anywhere that ever sets that flag to `true`, and the `Effect` DSL actions/events use to modify state has no mechanism that could (only `flag`/`unflag` on the separate `flags: string[]` array, never a specific `GameState` boolean like this one). So "??" wasn't a bug in the display logic, it was a genuinely unreachable, permanently-stuck dead feature — the reveal mechanic described in the design doc was never built. Rather than leave that half-built and confusing, removed it outright: `ui/hud/Hud.tsx` now always shows the real `stats.feltInflation` value next to official inflation; `feltInflationRevealed` deleted from `GameState` (`engine/types.ts`), `createInitialState` (`engine/state.ts`), and its assertion in `state.test.ts`; the now-unused `hud.hidden` ("??") i18n key removed from `en.ts`/`fr.ts`. `docs/GAME_PLAN.md`'s §4 stat table and §11 HUD mockup updated to match and to record why (so a future reader doesn't wonder where the "hidden inflation" feature went). `feltInflation` itself is untouched and still fully real — it still secretly drives Happiness/hyperinflation exactly as designed, it's just not hidden from the player anymore. Note: `ResolutionOverlay.tsx`'s action-preview screen separately hides `feltInflation` deltas via its own `HIDDEN_PREVIEW_STATS` set ("same as the month-end report" per its comment) — left as-is since the user's report was specifically about the HUD and this wasn't asked about; flagged here in case it reads as inconsistent later (HUD shows the running total plainly, the live preview still hides its deltas).
  - **Verification for both of the above**: full test suite (`tsx --tsconfig tsconfig.app.json`, all 28 `*.test.ts` files under `src/`) — 294 cases, 0 failures, unchanged in count (both changes updated existing assertions rather than adding new ones, bar the event-count bump). `tsc --noEmit -p tsconfig.app.json` — 1 error, the same pre-existing sandbox-only `vite/client` type-declarations gap noted in the entry above (global to the tsconfig, unrelated to any file touched, confirmed already present before this round's changes). `prettier --write` run on every touched file. Synced to the device (`Hud.tsx`, `types.ts`, `state.ts`, `state.test.ts`, `en.ts`, `fr.ts`, `events.ts`, `events.test.ts`, `GAME_PLAN.md`) — all 9 files landed byte-identical on the first `device_commit_files` attempt, no stale-restage retry needed this round.
  - **Real art for Joly and the Canada wildcard, and the wildcard renamed to match** (user-supplied reference sheets, same "4-images-per-sheet, front×2/back×2" layout as the earlier Carney/LeBlanc/Trudeau/Singh sheets). Cropped with the same alpha-bounding-box pipeline as those rounds (`scipy.ndimage.label` on the alpha channel to isolate each figure, tight-cropped to content, resized to match the roster's established front/back pixel-height convention — ~241/246 for Joly, ~247/250 for the wildcard, same numbers the old placeholder art happened to use). Picked the cleaner-looking pose of each front/back pair by eye (Joly: hands-together over the more open-armed alternate; wildcard: straight-on over the two slightly-turned alternates).
    - **One real sizing bug caught before it shipped, not after**: `.battle-unit-sprite` (`battle.css`) is a fixed 18×42px box with `object-fit: contain`, so an unusually _wide_ sprite doesn't overflow the box — it just renders visibly _shorter_ than its teammates, since `contain` picks whichever dimension is the tighter constraint. The wildcard's new art holds a stick and a bucket in outstretched arms, and a first pass that kept the full tight-cropped bounding box (155px wide × 247px tall, aspect ~0.63) would have rendered him at only ~68% of the box height next to a roster whose aspect ratios all cluster around ~0.41-0.45 — caught by actually computing the `object-fit: contain` math before committing to a crop, not by eyeballing the PNG in isolation. Fixed by trimming the crop's x-range to the body's dense-pixel core (found via a per-column alpha-density profile) before resizing, cutting most of the outstretched stick and some of the bucket's overhang rather than the body — landed at 111×247 / 108×250, coincidentally identical to the old placeholder art's own dimensions. Joly's art didn't have this problem (no outstretched props, aspect ~0.49 vs. the family's ~0.42 — mildly wide from her hair, within tolerance, left untrimmed).
    - **Renamed the wildcard**: the new reference art reads clearly as a "cabane à sucre" (sugar shack) maple-syrup worker — red toque, buffalo-plaid shirt, suspenders, a tapping stick and sap buckets — not the original "guy in a Mackinaw jacket" placeholder concept, and the user's own message referred to it as "the sugar shack random guy." Renamed throughout rather than leaving a mismatched internal id: `canada-mackinaw-guy` → `canada-sugar-shack-guy` (`data/battleRosters.ts`, and its test's lookup in `battleRosters.test.ts`), `battle.unit.canada.mackinawGuy` → `battle.unit.canada.sugarShackGuy` (`en.ts`/`fr.ts`, display text "Guy in a Mackinaw Jacket" → "Sugar Shack Guy" / "Gars de la cabane à sucre"), sprite files renamed `mackinaw-guy-{front,back,left,right}.png` → `sugar-shack-guy-{front,back,left,right}.png` (front/back are the new art; left/right are the same old unchanged art, just renamed for consistency — nothing in the codebase references the Canada roster's left/right or back sprites at all currently, confirmed by grep; only `-front.png` is wired up, via each unit's `sprite` field, and used only in the tactical battle screen). `docs/GAME_PLAN.md` and `data/battleRosters.ts`'s own doc comments updated to match and to record the rename's reasoning.
    - **Stale files left on the device, not deleted**: the device shell (`device_bash`) is still down (same Windows-update issue), so the 4 old `mackinaw-guy-*.png` files under `public/assets/sprites/canada/` on the user's machine couldn't be removed from this session — they're harmless (nothing references them anymore) but should be deleted by hand next time the user is in that folder.
    - **Verification**: full test suite — 294 cases, 0 failures (`battleRosters.test.ts`'s rename updated in place, not added to). `tsc --noEmit -p tsconfig.app.json` — 1 error, the same pre-existing sandbox-only `vite/client` gap noted twice above, still unrelated to any file touched. `prettier --write` run on every touched text file. Synced to the device — all 4 new/changed PNGs and all 5 changed text files landed correctly; the PNGs showed the same benign +5770-byte C2PA-stamp size delta documented earlier this session (device-side content-provenance tagging, not corruption) — reconfirmed by a pixel-array comparison (`PIL`/`numpy`, `array_equal` on the full RGBA arrays) rather than just assuming the pattern repeated, all 4 came back pixel-identical.
  - **New random event: Canada joins the EU** (user request, riffing on the game's "51st state" satire from the other side — Canada trolling back). `data/events.ts` gained `canada_joins_eu` (weight 5, same tier as the other big one-off geopolitical stories like `foreign_crisis`/`weather_balloon_panic` rather than an everyday item): Headlines +12, Happiness −4, DEFCON −1 — a huge, faintly embarrassing diplomatic stunt, not an existential one. Left deliberately ungated: the event system's `requires` only supports `flags: string[]` (checked against `GameState.flags`), while the war/purchase system tracks country state in separate `atWarWith`/`countriesOwned` arrays with no flag bridge between them — gating this on "Canada specifically" would need a real (if small) engine change, out of scope for a single flavor event; every other event in the catalog bar one (`reactor_inspection`) is similarly ungated. New i18n keys added to both `en.ts`/`fr.ts`; `data/events.test.ts`'s hardcoded event count bumped 23→24.
  - **Removed the permanent "??" next to Inflation on the HUD** (user report). Investigated first rather than just deleting the string: the HUD was deliberately showing official inflation next to a _hidden_ "felt inflation" figure (`GameState.feltInflationRevealed`, `data/balance.ts`/GAME_PLAN §4's design), meant to be exposed later by a Bessent "Reveal" action or an event — but grepping the whole codebase found nothing anywhere that ever sets that flag to `true`, and the `Effect` DSL actions/events use to modify state has no mechanism that could (only `flag`/`unflag` on the separate `flags: string[]` array, never a specific `GameState` boolean like this one). So "??" wasn't a bug in the display logic, it was a genuinely unreachable, permanently-stuck dead feature — the reveal mechanic described in the design doc was never built. Rather than leave that half-built and confusing, removed it outright: `ui/hud/Hud.tsx` now always shows the real `stats.feltInflation` value next to official inflation; `feltInflationRevealed` deleted from `GameState` (`engine/types.ts`), `createInitialState` (`engine/state.ts`), and its assertion in `state.test.ts`; the now-unused `hud.hidden` ("??") i18n key removed from `en.ts`/`fr.ts`. `docs/GAME_PLAN.md`'s §4 stat table and §11 HUD mockup updated to match and to record why (so a future reader doesn't wonder where the "hidden inflation" feature went). `feltInflation` itself is untouched and still fully real — it still secretly drives Happiness/hyperinflation exactly as designed, it's just not hidden from the player anymore. Note: `ResolutionOverlay.tsx`'s action-preview screen separately hides `feltInflation` deltas via its own `HIDDEN_PREVIEW_STATS` set ("same as the month-end report" per its comment) — left as-is since the user's report was specifically about the HUD and this wasn't asked about; flagged here in case it reads as inconsistent later (HUD shows the running total plainly, the live preview still hides its deltas).
  - **Verification for both of the above**: full test suite (`tsx --tsconfig tsconfig.app.json`, all 28 `*.test.ts` files under `src/`) — 294 cases, 0 failures, unchanged in count (both changes updated existing assertions rather than adding new ones, bar the event-count bump). `tsc --noEmit -p tsconfig.app.json` — 1 error, the same pre-existing sandbox-only `vite/client` type-declarations gap noted in the entry above (global to the tsconfig, unrelated to any file touched, confirmed already present before this round's changes). `prettier --write` run on every touched file. Synced to the device (`Hud.tsx`, `types.ts`, `state.ts`, `state.test.ts`, `en.ts`, `fr.ts`, `events.ts`, `events.test.ts`, `GAME_PLAN.md`) — all 9 files landed byte-identical on the first `device_commit_files` attempt, no stale-restage retry needed this round.
  - **Fixed the sugar-shack-guy crop (user feedback: "the sugar shack guy is cropped!")**: the previous round's crop deliberately trimmed his outstretched stick and part of his bucket to keep his aspect ratio close to the roster's humanoid norm (the technical reasoning was sound — see that round's entry — but the result visibly lost the character's signature props, which is what the user actually flagged). Fixed at the root instead of re-trimming differently: `.battle-unit-sprite`/`.battle-unit-iso` (`battle.css`) were a fixed 18px-wide box tuned for the original roster's narrow humanoid aspect ratio, so _any_ wider pose rendered visibly shorter under `object-fit: contain` rather than overflowing — not a crop problem, a box problem. Widened both to 26px (still comfortably inside a 32px `ISO_TILE_WIDTH` tile), then restored the wildcard's full uncropped pose (stick and bucket both fully visible again) — at 26px his aspect ratio now renders at ~99% of box height, no meaningful shrink left. The wider box is also a preemptive fix for the Greenland art below, which has an even more extreme aspect ratio than any humanoid.
  - **Real art for Greenland's roster, plus a new 5th unit (a seal)** — first real Greenland art of any kind; all 4 bears were flat-color-placeholder-only before this. User-supplied bear and seal reference sheets, same 4-frame (front×2/back×2) layout as every character sheet this project, cropped the same way (alpha-bounding-box, picked the cleaner pose of each pair, no x-trim this time per the lesson just above). The same bear art is shared by all 4 bear units (`bear-a`/`b`/`c`/`boss`) — the boss differs only in stats, not appearance, since the reference sheet has one bear. Sized close to native resolution (a light 15% downscale) rather than forced into the humanoid convention's ~250px height target, since a landscape-oriented quadruped doesn't share that proportion — `object-fit: contain` handles the final on-screen fit regardless of source resolution, so there was no real cost to keeping them close to native size and no reason to force an artificial match.
    - **Caught a real balance regression before shipping it, using the same simulator built for the Canada fix**: adding the new seal as a straightforward 5th roster unit — even with deliberately weak, "comic relief" stats and no gating — dropped Greenland's simulated US win rate from ~96% to ~41-54% depending on the seal's exact stats (confirmed the swing wasn't about the seal's specific numbers: even a completely inert, no-quirk version of it still crashed the win rate to ~54%). Root cause is the same turn-order economy diagnosed for Canada, just running in the direction that had been quietly working _in the US's favor_ the whole time: every country roster before this had _fewer_ than the US's fixed 6 units, so the leftover/unanswered turns in `createBattle`'s interleaved order always favored the player; growing Greenland from 4 to 5 enemies took back one of those free turns _and_ added a whole extra attacker from round 1 — a bigger combined swing than either half alone would suggest. Re-tuned the seal down to `maxComposure: 3` (and dropped an early-draft dodge quirk entirely, since the data showed unit _count_ was the dominant lever, not the quirk) — confirmed at full 2000-battle simulator scale to land back at ~85-87% US win rate, comfortably "clearly winnable" and consistent with `scripts/simulateBattle.test.ts`'s existing `>0.7` regression guard for every non-Canada country (which would otherwise have started failing).
    - **Verification**: full test suite — 294 cases, 0 failures (no test file needed changes; the roster-size checks in `battlefield.test.ts`/`battleRosters.test.ts` were already made data-driven during the Canada round specifically to absorb roster-size changes like this one without edits). `scripts/simulateBattle.test.ts` re-run and passing at the new numbers. `tsc --noEmit -p tsconfig.app.json` — 1 error, the same pre-existing sandbox-only `vite/client` gap noted repeatedly above. `prettier --write` on every touched file. Synced to the device (6 PNGs, `battleRosters.ts`, `en.ts`, `fr.ts`, `battle.css`, `GAME_PLAN.md`, `simulateBattle.ts`) — used `force: true` proactively on the sugar-shack-guy files this time rather than discovering the stale-write pattern via a failed byte check first, then confirmed all 6 PNGs pixel-identical (`PIL`/`numpy`) despite the now-familiar +5770-byte benign C2PA-stamp size delta, and all text files byte-identical.
  - Next up in Phase 6: final art & audio (same image/audio-generation gap as Phase 5's ending art), playtests, and an itch.io web build (needs a working `yarn build` — currently blocked by the same toolchain gap as everything else, since bundling needs the real `vite`/`vitest` npm install this environment has never had). Re-confirmed this round: npm registry is still 403 in this sandbox, and the user's device shell is still down (Windows-update issue, unchanged) — so the build item stays blocked from both sides until one of those clears.
  - **Roster panel restructured to two lines per unit (user feedback on a battle screenshot: "the layout is wrong... on the right side. I would prefer the HP/MP be on a line below the name")**: `.battle-roster-row` (`ui/battle/BattleView.tsx`'s `UnitRow` + `styles/battle.css`) used to be a single `display:flex; align-items:center` row cramming a 12px-wide swatch, the name, and a stat column (HP bar + value, MP pips) into the 92px-wide `.battle-roster-compact` panel — almost no width was left for `.battle-roster-name`, so most names ellipsized after two or three letters ("Po...", "Ha..." in the user's screenshot). Restructured into two lines: a new `.battle-roster-top` wraps the swatch + name in their own flex row (full panel width now, since nothing else shares it), and `.battle-stat-col` (HP bar/value, MP pips) moved to its own full-width line below, indented `padding-left: 15px` to sit roughly under the name text rather than under the swatch. The HP bar itself changed from a fixed `width: 26px` to `flex: 1` since it's no longer competing with the name for space — it now fills the available width on its own line, more readable than before. No prop/behavior changes, pure layout restructuring.
  - **Investigated a second complaint from the same screenshot — battlefield bear sprites rendering oversized and overlapping tiles, with flag/badge pastilles reading as "scattered loose and disconnected" from their characters — found no code bug.** Re-checked everything the report could plausibly implicate: `.battle-unit-iso`/`.battle-unit-sprite` (`battle.css`) are still the 26×42px `object-fit: contain` box from this session's earlier sugar-shack-guy crop fix, with no conflicting rule anywhere else in the CSS cascade (`battle.css` is the last `@import` in `global.css`, so nothing after it can override it); `BattleUnitToken`'s JSX (`BattleView.tsx`) applies the right class names and keys each unit by its own unique `id`, ruling out a React key-collision/duplicate-render explanation; `global.css` has no `img` width/height override beyond `image-rendering: pixelated`. Re-staged the device's live copies of `battle.css`, `BattleView.tsx`, and `battleRosters.ts` and diffed them byte-for-byte against local before touching anything — all three were already in sync (the CSS's 26px box genuinely was already live on the device), so this isn't a repeat of the earlier stale-device-file incident (see the Phase 2 entry above) for these three files specifically. One oddity spotted along the way, not obviously related: the device's `public/assets/sprites/greenland/` folder has `bear-front-1.png`/`seal-front-1.png` alongside the real `bear-front.png`/`seal-front.png`, timestamped about 76 seconds after them — `battleRosters.ts` (confirmed identical on device and local) only ever references the non-`-1` filenames, so these look like harmless leftover duplicates from an earlier sync retry rather than anything the running game actually loads; not cleaned up since `device_bash`'s delete capability is still down and it's cosmetic clutter, not a bug. Best remaining explanation is a stale dev-server/browser cache on the user's end (a Vite HMR CSS update that didn't fully apply without a hard refresh) rather than a real code defect — flagged to the user to hard-refresh/restart `yarn dev` and send a fresh screenshot if it persists, since nothing further could be diagnosed without being able to reproduce it directly.
  - **Verification this round, with a caveat**: `tsc --noEmit -p tsconfig.app.json` — 1 error, the same pre-existing sandbox-only `vite/client` type-declarations gap noted throughout this doc, unrelated to either file touched. **Could not run the test suite this round** — unlike every prior round this session, `node_modules` in this sandbox came up completely empty (`vitest` itself unresolvable, not just the Linux-Rollup-binding gap documented earlier) and `npm install`/`npx vitest` both hit the same 403 from the npm registry noted in the Phase 6 status line above, so `yarn test`/`yarn verify` need to be run by the user locally before trusting this fully — low risk since both changes are pure CSS/JSX layout restructuring with no prop or behavior changes and no existing `BattleView`-level component tests to run anyway, but flagged here rather than silently skipped. `prettier` also unavailable for the same reason; formatting done by hand, matching the surrounding file's existing style. Synced to the device (`BattleView.tsx`, `battle.css`) and reconfirmed byte-identical after commit.
  - **Found the real cause of the "disconnected pastille" half of the report above, once the user sent a cropped close-up screenshot** ("Look at the seal and polar bear. They are not over their pastille") — the earlier round's investigation correctly ruled out a sizing/overlap bug but hadn't isolated the badge-anchoring issue specifically; this round's tighter screenshot made it clearly visible as its own separate problem, not a stale-cache mirage. Root cause: `.battle-unit-sprite`'s `object-fit: contain` had no explicit `object-position`, so it defaulted to dead-center. The original humanoid roster is tall-and-narrow, so in the 26px-wide box they're _height_-constrained — they render the full 42px tall, feet flush with the box's bottom edge, exactly where `.battle-unit-flag`'s tuned negative `margin-top` expects them. Greenland's bear/seal art is much closer to square (the seal is actually _wider_ than tall) — at 26px wide the bear renders only ~27px tall and the seal only ~19px tall, both _width_-constrained instead, and center-anchoring let that short render float in the middle of the box, leaving a big gap of empty box below its visible feet — right where the flag/badge was still anchored underneath, reading as floating/disconnected. Fix: `object-position: bottom` on `.battle-unit-sprite` — anchors every sprite's visible feet to the box's bottom edge regardless of source aspect ratio, so the flag/badge's existing humanoid-tuned offset now lines up correctly for animals too, with no change to how the tall/narrow humanoids render (they were already flush with the bottom). One-line, low-risk CSS fix; no JSX/behavior change.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — same single pre-existing `vite/client` error, unrelated. Test suite still not runnable this session (same empty-`node_modules`/403-registry blocker as the entry above) — pure CSS change, no test coverage exists for pixel-level sprite positioning regardless. Synced `battle.css` to the device — **the first `device_stage_files` re-check after `device_commit_files` came back with the old 11130-byte content and an unchanged mtime despite the commit reporting success**, a live recurrence of the stale-write pattern documented earlier this project; a second `device_commit_files` with `force: true` and a re-stage confirmed the new 12427-byte content landed and diffed byte-identical. Worth calling out: a first stage-after-commit mismatch on this project should be treated as expected/routine now, not a surprise — always re-verify and be ready to retry with `force: true` rather than trusting one success response.
  - **Battle units now turn to face where they're moving or attacking** (user feedback: "it would be nice that the sprites turns in the direction of where it's going or where it attacks" — every unit previously always rendered in its `front` pose, no matter which way it moved or who it hit). Landed as a real engine feature, not a UI-only trick, per this doc's architecture rules ("engine functions are pure," "every engine change comes with Vitest tests") — `enemyTakeTurn` already drives the AI's moves/attacks through the same `moveUnit`/`attack` functions the player's clicks go through, so putting facing logic there means the enemy roster turns correctly for free, with no separate UI-side direction-inference code to keep in sync.
    - `engine/battle.ts`: new `BattleUnitSprite` type (`{front: string; back?/left?/right?: string}`) replaces `BattleUnitTemplate.sprite?: string` — was always just a front-pose path, now a small directional set (front required, the rest optional so a unit with only partial reference art still works, see below). New `BattleUnit.facing: Direction` (imported from `engine/movement.ts`, the same type the room camera already uses) — starts `'down'` at spawn (`createBattle`, matching the old always-front look exactly until a unit actually acts) and only changes when `moveUnit`/`attack` turn it. A new private `directionTo(from, to)` picks whichever axis (x or y) has the larger absolute delta between two `GridPosition`s (ties favor horizontal), mirroring `data/characters.ts`'s existing `Direction`-to-sprite-pose convention (`down`→front, `up`→back, `left`→left, `right`→right) so battle units turn using the exact same 4 poses the room camera already uses — new exported `spriteForFacing(sprite, facing)` does that pose lookup, falling back to `front` for any pose a unit's art doesn't have. `moveUnit` faces the unit toward `to` (a straight-line facing from the old tile to the new one, since a click-to-move is a single jump, not an animated multi-step walk). `attack` faces the attacker toward the _originally-requested_ defender — not wherever a self-hit/backstab quirk ends up redirecting the actual hit to — computed once up front and threaded through every branch (cast, flee, dodge, landed hit), so the attacker visually "faces where it swung" regardless of which quirk fires.
    - `data/battleRosters.ts`: every `sprite:` value changed from a bare front-pose path/string to a `BattleUnitSprite` object. The 6 US officials now pass `getCharacter(id).sprite` directly (the whole `CharacterSprite` object from `data/characters.ts`, which already has all 4 poses — no new art needed, this was already sitting there unused by battle). Canada's Carney/Trudeau/Miller/Joly/LeBlanc/Sugar Shack Guy get full `{front, back, left, right}` objects (that reference art already existed too, from earlier rounds — see the crop-fix/rename entries above). Jagmeet Singh only ever had front art supplied, so his sprite is `{front}` alone — he doesn't turn, same look as before. Greenland's bears/seal only have front/back reference art (no side view was ever supplied), so theirs is `{front, back}` — they'll flip to their back pose walking away, but won't turn left/right; documented inline at each spot rather than left implicit.
    - `ui/battle/BattleView.tsx`: `BattleUnitToken` (the battlefield token) now renders `spriteForFacing(unit.sprite, unit.facing)` instead of always `unit.sprite.front` — the actual visual fix. `UnitRow` (the roster-panel swatch) deliberately keeps using `unit.sprite.front` always, regardless of `unit.facing` — it's a static 12px list icon, not the battlefield token, and animating something that small would read as flicker rather than a turn.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — same single pre-existing `vite/client` error, no new ones, across the whole `src` tree (this tsconfig's `include` covers every `*.test.ts` too, so this also confirms the new test code below type-checks). **Still could not run the actual test suite this session** (same empty-`node_modules`/403-registry blocker noted in the two entries above — this is now a running theme this session, not a one-off) — added real coverage by hand instead, matching this file's existing `template()`/`createBattle()` fixture patterns rather than constructing ad-hoc `BattleUnit` objects: `battle.test.ts` gained a `spriteForFacing` describe block (all 4 poses, plus the front-only-art and front+back-only fallback cases) and new cases under `moveUnit` (turns to face the move direction on all 4 axes, picks the larger-delta axis on a diagonal, leaves facing untouched on a no-op move) and `attack` (turns to face the defender; and, under the existing "attack quirks" describe block, a case proving facing follows the _originally-requested_ target rather than a backstab-quirk's redirected one). This needs a real `yarn test` run to confirm before fully trusting it — flagged rather than silently assumed passing. Synced all 4 changed files (`engine/battle.ts`, `engine/battle.test.ts`, `data/battleRosters.ts`, `ui/battle/BattleView.tsx`) to the device and reconfirmed byte-identical on the first `device_commit_files` attempt this time, no `force` retry needed.
  - **Correction — every "1 pre-existing `vite/client` error, otherwise clean" `tsc` claim in this file, going back to before the Phase-1 entries, was not real verification.** `tsc --noEmit -p tsconfig.app.json` fails at _program construction_ on `tsconfig.app.json`'s `"types": ["vite/client"]` entry (`TS2688: Cannot find type definition file for 'vite/client'`) — and a `tsc` run that fails during program construction never reaches per-file checking at all, for _any_ file, not just the one named in the error. Discovered by deliberately injecting an obvious type error into `events.ts` and confirming `tsc` still reported only the one `vite/client` line, not the injected one — proof the "1 error, unrelated, everything else clean" framing this file has repeated dozens of times was actually "the run never checked anything." **Fix**: a local-only, non-synced stub at `node_modules/vite/client.d.ts` (`export {};` plus a comment explaining why it exists) — this satisfies the `types` reference and lets `tsc` proceed into real per-file checking. Verified safe before adding it: grepped all of `src/` for `import.meta` and any ES-module asset import (`import x from '*.png'` etc.) — both came up empty, since this project always references sprite/audio assets as plain string paths under `public/`, never as imports — so an empty stub loses no real type coverage. **This must never be synced to the device** (it's a workaround for this sandbox's empty `node_modules`, not a real project dependency) and must be recreated at the start of any future session that wants real `tsc` output here, since the sandbox filesystem doesn't persist across sessions.
    - With the stub in place, a real full-project `tsc` run in this empty-`node_modules` sandbox reports **~550-570 errors**, not 1 — nearly all of them expected noise from having _no_ packages installed at all (not just the Linux-Rollup-binding gap some earlier entries in this file describe for a _partial_ install): `TS2307`/`TS2882` "cannot find module 'react'/'zustand'/'vitest'/'@fontsource/press-start-2p'", `TS7026`/`TS2875` JSX-intrinsics-have-no-type, `TS7006` implicit-any on every callback parameter those two gaps cascade into, and `TS2322` "key prop not assignable" (JSX's built-in `key` isn't recognized without real React types). None of this is a real bug — every file in `engine/`+`data/`+`i18n/` (no React/DOM dependency, per this file's own architecture rules) type-checks with **zero errors** under the stub, and the noise categories are stable across passes (verified by diffing full error-category counts between two runs after an unrelated fix, byte-for-byte identical modulo line-shift). **Going forward, use this stub-plus-full-diff method** (run `tsc`, bucket every error by `TS####` code, confirm nothing outside the known noise categories touches a changed file, and specifically confirm zero errors in any pure `engine`/`data`/`i18n` file changed that session) instead of the old "did it print the same 1 line" check — the old check could never have caught a real bug in `engine`/`data` code, since it never got far enough to look.
  - **Pick your own landmark to rename, with persistent "renamed to America" map labels** (user feedback: "When you rename a landmark, it would be nice to be able to select what you want to rename on the map. We could add labels over the map so that we can see everything being renamed to 'America'"). Exactly the same shape of fix `declare_war` already got earlier in Phase 6 (see "picking your own Declare War target" above) — `rename_landmark` used to auto-pick a random not-yet-renamed landmark the instant the preview was confirmed; replaced with a real World Map target picker, mirrored end-to-end from the `declare_war`/`SelectCountryScreen` precedent. Asked 3 clarifying questions first: picker location (**World Map screen**, reusing `.map-world`), what renamed landmarks should be called, and what kind of landmarks should be selectable (**real, recognizable US landmarks**). The naming answer came back a little garbled ("1) but keep the prefix 'Lake America', 'Gulf of America', 'Ocean America'. We you beat a country, it's named 'America'") — interpreted as two asks bundled together: (a) every landmark keeps its own descriptive word rather than becoming the bare word "America" (Gulf of America keeps "of," Lake/Ocean America don't — the user's own three examples already aren't uniform, confirming a template would need per-kind exceptions anyway, so each landmark's renamed name is a full authored i18n key, not composed from parts — this project's standing whole-phrase-key convention); and (b) a related but separate ask — an owned country should _also_ get an on-map "(now America)" tag, tying into the same "renamed to America" visual idea as the landmark labels. Implemented both; the interpretation is called out explicitly here in case (b) wasn't actually wanted.
    - `engine/types.ts`: `LandmarkId` (7 ids — Gulf of Mexico, Denali, Niagara Falls, Grand Canyon, Lake Michigan, Pacific Ocean, Mississippi River), two new `Effect` kinds (`renameLandmark` — data-declared, random-pick, same shape as the existing `declareWar`; `renameLandmarkOn` — the concrete substituted form once a landmark's picked, same shape as `declareWarOn`), and `GameState.renamedLandmarks: readonly LandmarkId[]`.
    - `data/landmarks.ts` (new): `LandmarkDef` (`id`, `nameKey`, `renamedKey`, `mapX`/`mapY` — same "tuned by eye against `world-map.png`, not surveyed" convention as `CountryDef`), the 7-entry `LANDMARKS` array, `getLandmark`, `pickRandomLandmark`, and `actionHasLandmark` (mirrors `engine/battle.ts`'s `actionHasBattle`, used by `ResolutionOverlay.tsx` to gate the preview copy).
    - `engine/effects.ts`: `EffectContext.renamedLandmarks` field; `applyEffect` cases for both new effect kinds; exported `resolveLandmarkEffects(effects, landmark)`, mirroring `resolveWarEffects` — substitutes the concrete `renameLandmarkOn` in place of the generic `renameLandmark`, recursing into `chance` branches.
    - `engine/preview.ts`: `EffectSummary.hasRandomLandmark` flag (mirrors `hasRandomCountry`), switch cases for both new effect kinds (ignoring the already-substituted `renameLandmarkOn`, same as `declareWarOn`).
    - `engine/resolve.ts`: `resolveAction` gained an optional `landmarkId?: LandmarkId` param — substitutes via `resolveLandmarkEffects` into both `onSuccess`/`onFail` before rolling (landmark renaming isn't battle-gated, so it uses the plain `resolveAction` path, not `resolveBattleAction`'s onSuccess-only substitution). `finalizeAction` threads `renamedLandmarks` through its `EffectContext`/`nextState` construction.
    - `data/actions.ts`: `rename_landmark`'s `onSuccess` gained `{ kind: 'renameLandmark' }`.
    - `store/gameStore.ts`: new `ResolutionPhase` value `'selectLandmark'`, `ResolutionState.landmarkId?`, new `selectLandmark(landmarkId)` action; `confirmPreview` gained a landmark-gated branch (mirroring the existing battle-gated branch) that routes to `'selectLandmark'` instead of resolving immediately when the action is `rename_landmark` and an eligible landmark exists.
    - `ui/screens/SelectLandmarkScreen.tsx` (new): mirrors `SelectCountryScreen.tsx` exactly — reuses `.map-world`, plots only not-yet-renamed landmarks as real buttons.
    - `ui/screens/ResolutionOverlay.tsx`: `landmarkGated` flag (via `actionHasLandmark`); preview copy gets a `landmark.selectNote` when gated (suppressing the generic `resolution.randomLandmarkNote` the same way battle-gated actions already suppress `resolution.randomCountryNote`); new `'selectLandmark'` phase branch rendering `<SelectLandmarkScreen />`.
    - `ui/screens/MapScreen.tsx`: renders a persistent label for every already-renamed landmark (gold dot, same visual language as an owned country) plus a `"(now America)"` tag next to any owned country's name — both per the user's "labels over the map... everything being renamed to America" ask, with the owned-country tag as the bundled interpretation noted above. New legend row.
    - `styles/room.css`: `.map-now-america`, `.map-landmark`/`.map-landmark-dot` (persistent label, same shape as `.map-country`), `.map-landmark-button` (picker-screen button modifier, same pattern as `.map-country-button`).
    - i18n: 14 landmark name/renamed keys, `landmark.selectTitle`/`selectNote`/`renamedBadge`, `resolution.randomLandmarkNote`, `map.nowAmerica` — both `en.ts`/`fr.ts`.
    - **Two real compile errors from earlier `EffectContext`-shape work in this same session, caught and fixed by the `tsc`-stub verification method above** (both are `renamedLandmarks`-missing-field errors, not landmark-feature bugs per se — every other `EffectContext`/`GameState` literal in the codebase was updated when the field was added, these two were missed): `engine/events.ts`'s `rollMonthlyEvent` was missing `renamedLandmarks` in both its `EffectContext` literal and its `nextState: GameState` literal; `engine/effects.test.ts`'s `CTX()` test-fixture helper was missing it from its default object. Both fixed. Also explicitly double-checked `ui/screens/MapScreen.tsx` (which a `grep -rln "atWarWith:"` pass had flagged as a possible third site) and confirmed it's a false positive — it only reads `game.atWarWith`/`game.countriesOwned` as plain props into a local `countryStatus` helper, never constructs an `EffectContext`/`GameState` literal.
    - **Verification**: full `tsc --noEmit -p tsconfig.app.json` under the stub — confirmed **zero errors** in every pure `engine`/`data`/`i18n` file this feature touched (`types.ts`, `landmarks.ts`, `effects.ts`, `preview.ts`, `resolve.ts`, `actions.ts`, `en.ts`, `fr.ts`) before and after the two fixes above; the remaining ~560 errors project-wide are 100% the known noise categories documented in the entry above (bucketed by `TS####` code and cross-checked, none outside the known set). `prettier --check`/`--write` run on every touched file (2 needed reformatting — `data/landmarks.ts`, `ui/screens/MapScreen.tsx` — both clean after). **Could not run the actual test suite** — `node_modules` came up completely empty again this session (no `tsx`-runnable vitest shim survived from prior sessions; the sandbox filesystem doesn't persist), so this feature has no real dynamic test run yet, only the type-level check above — flagged rather than silently assumed passing; `yarn test`/`yarn verify` still needed from the user's own machine. Synced all 18 touched/new files to the device (stage → commit → re-stage → byte-diff) — all 18 landed byte-identical on the first `device_commit_files` attempt, no `force` retry needed this round.
  - **Fix — landmark name labels overlapping illegibly on the map (user screenshot: the picker screen showing "Denali"/"Lake Michigan"/"Pacific Ocean"/"Mississippi River" stacked on top of each other; "Maybe we can use dots of the map and when you click on it, you show the name")**: the always-visible name-pill markers the landmark picker just shipped with (previous entry) looked fine for countries, whose `mapX`/`mapY` are spread worldwide, but all 7 landmarks cluster tightly in one corner of the map (`data/landmarks.ts`'s range is ~7-29%/25-54%) — with several nearby, their fixed-width pills physically overlapped regardless of z-index, exactly as the screenshot showed. Root-caused as a spacing/design problem, not a CSS bug to patch around — replaced the always-on pill with a small dot marker plus a click-to-reveal name callout, in both places landmarks render:
    - `ui/screens/SelectLandmarkScreen.tsx` (the picker): now **two clicks**, not one. A marker's first click reveals its name in a small callout above the dot (`activeId` local state) without picking anything yet; a second click on that _same_ now-gold marker confirms the pick (`selectLandmark`). Clicking a different marker just moves which one is revealed — nothing is picked until you click the same dot twice. This also incidentally fixes a real misclick risk the old dense, overlapping buttons had (you could easily click the wrong one blind); now you always see the name before it locks in. `landmark.selectNote` copy updated to explain the two-tap flow (`en.ts`/`fr.ts`). A `title` attribute on each marker also gives desktop mouse users a native hover tooltip for free, alongside the tap/click interaction.
    - `ui/screens/MapScreen.tsx` (the status board): same dot treatment, but view-only — a click just toggles that landmark's name callout open/closed, nothing to confirm since it's already renamed. Proactively fixed here too, not just the picker: this board only shows _renamed_ landmarks (a subset that only grows over a run), so the exact same overlap bug was guaranteed to recur here too, likely within the very next couple of renames — no need to wait for a second bug report on the same root cause.
    - `styles/room.css`: replaced `.map-landmark`/`.map-landmark-button` (the old pill + its button modifier) with `.map-landmark-marker` (a 12px dot, `.is-active` turns it gold — "already renamed" on the status board, "currently revealed" on the picker) and `.map-landmark-callout` (the small name label, `pointer-events: none` so clicks still hit the dot underneath it). `.map-landmark-dot` (the small square icon used only in the map legend row, "● Renamed") is untouched — different element, kept as-is.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` under the stub — grepped specifically for the two changed files and confirmed every single error touching either one falls in the already-known noise categories (missing `react`/JSX types), none outside it; the full project error count moved from ~561 to 566, consistent with the noise scaling with a small amount of added JSX/onClick surface, not a new error category. `prettier --check`/`--write` on all 5 touched files (`room.css`, `SelectLandmarkScreen.tsx`, `MapScreen.tsx`, `en.ts`, `fr.ts` — `fr.ts` needed one reformat, a line-wrap on the updated note string). Same test-suite gap as the entry above (no `tsx`/vitest shim available this session) — this is pure UI/CSS interaction logic with no `engine`/`data` change, so the risk is low, but still unverified dynamically; flagged rather than assumed. Synced all 5 files to the device — the two `.tsx` files hit the project's familiar stale-write-on-first-attempt pattern (reported success, came back stale on re-stage); a `force: true` retry landed both byte-identical. `room.css`/`en.ts`/`fr.ts` landed clean on the first attempt.
  - **Two Canada-battle-roster fixes from one user report** (two screenshots of the roster standing on flag pastilles at battle start: "The canadians characters are not looking at the right direction at the begining. Some of the canadian players have white pixels around them. I gave you a trasnparent background (except for Mark Miller)"):
    - **Facing bug**: `createBattle` (`engine/battle.ts`) hardcoded `facing: 'down'` for every spawning unit on both sides, regardless of which side of the battlefield it spawns on (`data/battlefield.ts`'s `US_SPAWN_POSITIONS` sit on the left, low `x`; `ENEMY_SPAWN_POSITIONS` on the right, high `x`) — so at kickoff, before anyone had moved or attacked, every unit just faced the camera instead of facing the opposing side, which is what looked wrong in the screenshots regardless of which side a Canadian unit was standing on. Fix: US spawns now default to `facing: 'right'` (toward the enemy side), enemy spawns to `facing: 'left'` (toward the US side) — units still turn normally from there via the existing `moveUnit`/`attack` facing logic (see the "battle units now turn to face where they're moving or attacking" entry above); this only changes the very first frame, before anyone's acted. `BattleUnit.facing`'s doc comment updated to match (previously said facing "starts `'down'`", now describes the side-dependent spawn default). Harmless for the roster's partial-art units — Singh (`{front}` only) and Greenland's bears/seal (`{front, back}` only) — since `spriteForFacing` already falls back to `front` for any pose a unit's `BattleUnitSprite` doesn't have art for, so they'll keep showing front regardless of the new internal `'left'`/`'right'` spawn facing.
      - `engine/battle.test.ts`: new dedicated test ("spawns each side already facing its opponent, not a hardcoded default") asserting a `us`-side unit spawns `'right'` and an `enemy`-side unit spawns `'left'`; 3 existing assertions that previously checked a fresh spawn's facing was `'down'` (in the move-direction, no-op-move, and attack-turns-the-attacker tests) updated to expect `'right'` instead, since all 3 use US-side fixture units. The one other facing-related test (attacker faces the originally-_requested_ target, not wherever a backstab redirects the actual hit) needed no change — it computes facing via `directionTo` from real attacker/defender positions at attack time, not from the spawn default.
    - **White-pixel/shadow-blob bug**: not a fringe/matting issue (checked and ruled out first — direct pixel inspection along sprite edges showed the semi-transparent boundary pixels were dark/near-black at low alpha, not white-tinted, so a classic un-decontaminated-white-matte theory didn't hold). The actual cause, found via a full-roster "feet region" contact sheet built from the real sprite files: a large, disconnected, light-gray shadow blob baked directly into the _opaque_ alpha region under the feet of most non-front Canada sprite poses, inherited from the original shared `canada-roster-sheet.png` crop (Sept 14, one big multi-character sheet with no real transparency work done) — later individual "v2" sheets (`canada-carney-sheet-v2.png`, `-leblanc-`, `-trudeau-`, plus `canada-singh-sheet.png`, all Sept 17) gave `front` poses for Carney/LeBlanc/Trudeau/Singh clean transparency, but every `back`/`left`/`right` pose still traces back to the old flawed sheet, and Miller never got an individual sheet at all — explaining why the user's exception ("except for Mark Miller") was backwards from the actual scope: Miller needed cleanup on _all four_ poses, not zero, since none of his art ever had a clean source.
      - Cleaned 17 files in place under `public/assets/sprites/canada/`: `carney-back/left/right`, `joly-left/right`, `leblanc-back/left/right`, `miller-front/back/left/right` (all four), `sugar-shack-guy-left/right`, `trudeau-back/left/right`. `joly-back` was checked and found already clean (0px removed) — left untouched, along with all 8 other already-clean files (`carney-front`, `joly-front`, `leblanc-front`, `singh-front`, `sugar-shack-guy-front/back`, `trudeau-front`).
      - Method: a Python/PIL/numpy/scipy pass (one-off, run directly against the real files, not checked in as a script — consistent with this project's prior sprite-cropping work) — flagged opaque, light, low-saturation pixels (`alpha > 150`, `lightness > 140`, `saturation < 28`) as shadow-blob candidates, grouped them into connected components (`scipy.ndimage.label`), and zeroed the alpha of any component ≥100px whose centroid sits in the lower 35% of the image and whose bounding-box width exceeds 30% of the image width (i.e. a wide blob low in the frame, not a small legitimate light-colored detail like a shirt cuff or highlight). Removed ~26,255px of alpha total across the batch, ~1300-1800px per file. Re-measured after: 15 of 17 files show zero remaining flagged pixels; `joly-left`/`joly-right` show small residuals (72px/46px) confirmed via zoomed feet-crop review to be legitimate art detail, not blob remnant, and left alone.
      - Verified visually: built and reviewed a 25-sprite contact sheet from the actual saved files, composited over the game's real `--navy-light` battle-floor color — every sprite renders clean with no white halo/blob, and no legitimate detail (hair color, white blazer, ties, boots, props) was damaged. Also confirmed the cleanup (which only zeroes alpha values, never changes canvas dimensions) doesn't disrupt `.battle-unit-sprite`'s `object-fit: contain; object-position: bottom` alignment — measured bottom-opaque-row fraction stayed ~0.93-0.99 across the front/back/left sprites checked, consistent with pre-cleanup.
    - **Verification for both fixes**: `tsc --noEmit -p tsconfig.app.json` under the stub — zero errors of any kind in `engine/battle.ts` (pure engine code); `battle.test.ts` shows only the standard "cannot find module 'vitest'" noise every test file has, nothing new. `prettier --check` clean on both files (via the sandbox's global `/opt/node-tools` prettier install, since `npx prettier` hit the same npm-registry 403 this sandbox has hit before — noted here as a new access path for future sessions that hit the same registry block). Same test-suite-can't-run gap as several entries above (no local `vitest`/`tsx` shim this session) — the new/updated facing assertions in `battle.test.ts` are unverified dynamically, flagged rather than assumed passing. Synced `engine/battle.ts`, `engine/battle.test.ts`, and all 17 cleaned PNGs to the device — the two TS files landed byte-identical on the first `device_commit_files` attempt; all 17 PNGs came back byte-_different_ on re-stage (the known C2PA content-provenance stamp this project's device pipeline adds to every transferred PNG, documented earlier in this file) but were confirmed **pixel-identical** via a direct `PIL`/`numpy` RGBA-array comparison against the local originals, same verification method used previously for binary image syncs.
  - **Verification-method note: this sandbox's filesystem persists across sessions after all.** Multiple earlier entries in this file (including the one directly above) assumed "the sandbox filesystem doesn't persist" and treated every session's `node_modules` as starting from zero. That turned out to be wrong, or at least no longer true: this session found the _exact same_ checkout still sitting at `/home/claude/kakistocracy` from the prior session (this `CLAUDE.md`, `docs/`, and a `node_modules/vite` stub all present untouched), and a fully working hand-written `vitest`/`react`/`react-dom`/`zustand` shim — the one described in the Phase 6 balance-pass entry above as "built earlier in the session" — still alive in this session's own scratchpad at `scratchpad/verify/node_modules/`. Copied those four shim packages into this checkout's real `node_modules/` (alongside the existing `vite` stub) so `tsx --tsconfig tsconfig.app.json <file>.test.ts` runs any test file for real, immediately, with no rebuild needed. **If a future session finds `node_modules/vitest` already present here, it's this same shim — reuse it directly rather than re-deriving one from scratch or assuming it's fake.** `node_modules` is `.gitignore`d and never part of any device sync (syncs are always an explicit per-file list, never a directory copy), so this carries no risk of the shim ever reaching the user's actual project.
  - **Three fixes from one round of user feedback**, verified with a full real dynamic test run this time (all 33 `*.test.ts` files, 352 assertions, 0 failures — see the note just above) rather than the usual type-check-only pass:
    - **The stage no longer floors to the nearest whole-number zoom** (user: "The game should take all the screen (it's too dense)"). `ui/useStageScale.ts`'s `computeStageScale` used to snap _down_ to the nearest whole zoom (2×, 3×, 4×…) so every pixel stayed perfectly aliased — but on most non-4K windows that undershot the real fit by a wide margin (a common 1366×768 laptop window fits ≈2.84× and used to floor all the way down to 2×, wasting close to a third of the window). Now returns the exact fractional fit; `image-rendering: pixelated` (`global.css`) keeps the art reasonably crisp even at a non-integer zoom, and the window's own aspect ratio is still respected (`Math.min` of the two axis ratios), so some letterboxing on whichever axis isn't the tighter constraint is inherent to not stretching the pixel art out of proportion — not a bug, just unavoidable unless the window happens to be exactly 16:9. Moved the pre-existing `computeStageScale` tests out of `ui/format.test.ts` (which doesn't define this function — `useStageScale.ts` does; a stray earlier add, not this repo's usual "tests colocated with the code" convention) into a new dedicated `ui/useStageScale.test.ts`, updated for the new behavior, plus new cases for the exact-fit and degenerate-viewport paths.
    - **A Hint button next to the mute toggle** (user: "There should be a hint button next to the mute button. The hint could help you figure out what your next move should be. For example, if happiness is too low, you can send a stimulus check" — that example is close to verbatim what it now does). New `engine/hint.ts`, pure and tested: `suggestAction(state, actions, balance)` ranks the 5 stats that can actually end the run (debt, feltInflation, iq, happiness, defcon — `interestRate`/`headlines` have no ending threshold, so they're excluded entirely) by a 0-at-the-run's-starting-value/1-at-the-ending-threshold "danger" score, then — starting from the most urgent — looks for the best currently-_available_ action (`checkAvailability`, without an active-character check, since a hint isn't gated by who's currently selected) whose expected effect (success chance-weighted average of `onSuccess`/`onFail`, via the existing `summarizeEffects`) actually helps that stat, falling through to the next-most-urgent stat if nothing available helps the top one. Returns `null` when nothing's meaningfully at risk yet or nothing available would help. New `Balance.hint.minDanger` (0.35) tuning knob, inherited by every difficulty since it's not overridden in `BALANCES`. `ui/HintButton.tsx`: same click-to-reveal-a-callout interaction as the World Map's landmark markers, with three flavors of copy — a real suggestion ("{reason}. Try: {action} ({room})."), "no red flags right now" when nothing's at risk, and "nothing to suggest right now" when not actually mid-run (title screen, or the run's already over). 8 new i18n keys, en/fr.
    - **Blocking-teammate sprites fade instead of hiding a unit entirely** (user, screenshot of the battle spawn formation: "can we try to detect sprite from our own team that block our view. We could make them semi-transparent while we play"). Root cause: `data/battlefield.ts`'s spawn tiles stack two units exactly one tile-height apart on screen whenever their positions sit on the same isometric diagonal (`US_SPAWN_POSITIONS`' `(2,5)`/`(3,6)` pair and `(2,7)`/`(3,8)` pair both do this, and so does `ENEMY_SPAWN_POSITIONS`' `(16,6)`/`(17,7)` pair — not scoped to the player's own roster, the enemy formation has the identical bug), but `.battle-unit-sprite` renders each unit's body at 42px tall — well over two tile-heights (16px) — so the unit nearer the camera (bigger `x + y`, drawn later per `isoDepth`) visually pastes its sprite right over its teammate's face. New `ui/battle/spriteOcclusion.ts` (pure, tested against the real spawn-position data above, not just synthetic coordinates): reimplements `.battle-unit-sprite`'s box geometry from unit grid positions alone (`projectIso` + the same 26×42 box/anchor `battle.css` uses — kept in sync by hand, same situation as this file's other CSS-value JS constants), and flags a unit as "occluding" when its sprite box covers ≥25% of a farther-back unit's sprite area. `BattleView.tsx` computes this fresh every render off the current `living` roster (cheap, well under 20 units) and adds a new `.is-occluding` class (`battle.css`, `opacity: 0.4`, transitioned) to just the front unit's `<img>` — not its flag/badge or turn marker, since hiding _those_ was never the complaint. Recomputes automatically as units move mid-battle, not just at spawn.
  - **Russia's roster replaced with real people (Putin, Gorbachev, Yeltsin), overriding this file's own real-currently-serving-head-of-state policy for Russia specifically.** User supplied a reference sheet (front+back poses for Vladimir Putin, Mikhail Gorbachev, a Soviet-era tank, and Boris Yeltsin) and asked for the 3 placeholder Russia units to be replaced, "for the missing sprites, you can perform an horizontal flip." Before wiring anything in, surfaced a real conflict rather than silently picking a side: `battleRosters.ts`'s own doc comment explicitly keeps Russia/Iran/Venezuela on invented archetypes rather than real currently-serving heads of state, specifically because all three are in live real-world tension with the US — and Putin is Russia's current president, squarely the case that rule exists for (Gorbachev/Yeltsin are both deceased former leaders, not implicated). Asked via `AskUserQuestion`; the user's answer was explicit: **"Use him anyway"** — override the rule for Russia now that real art exists — and **"Replace all 3 slots (Recommended)"** — swap out all 3 existing placeholders rather than partially mixing real/invented units on one roster. `battleRosters.ts`'s doc comment updated in place to record this as a deliberate, scoped exception (real art unlocked it for Russia; doesn't generalize to Iran/Venezuela without the same trigger).
    - **Sprite generation**: Python/PIL, same alpha-bounding-box-crop family of technique used for every prior character sheet in this project. The reference sheet had 4 subjects stacked in horizontal bands (row y-ranges found via each row's opacity profile) with front/back poses side by side per subject (column x-ranges the same way) — cropped all 8 front/back images, then generated the "missing" left/right poses via `ImageOps.mirror()` (front unflipped → `right`, front mirrored → `left`) exactly as the user instructed. 16 files total in `public/assets/sprites/russia/`: `putin-`/`gorbachev-`/`yeltsin-{front,back,left,right}.png` (wired in) plus `soviet-tank-{front,back,left,right}.png` (cropped and saved but **not wired into any unit** — no roster slot for it in the user's "replace all 3" answer; kept on disk for a possible future use, e.g. a 4th unit, a different slot, or battlefield scenery).
    - **Roster mapping** (`battleRosters.ts`'s `russia:` array): `russia-press-secretary` → `russia-putin`, `russia-tv-pundit` → `russia-gorbachev`, `russia-fur-hat-guy` → `russia-yeltsin` — a straight identity/art swap, each new unit keeping the exact `move`/`range`/`power`/`maxComposure` of the slot it replaces (balance-neutral; no simulator re-run needed since none of the numbers actually changed). Each gets a full `sprite: {front, back, left, right}` object; `placeholder` fallback kept as a 2-letter-initials/color pair (`VP`/`MG`/`BY`, same colors as the slots they replace) per the mandatory-even-with-sprite convention `BattleUnitTemplate` already has.
    - **i18n**: `en.ts`/`fr.ts` both gained `battle.unit.russia.putin`/`gorbachev`/`yeltsin` = 'Vladimir Putin'/'Mikhail Gorbachev'/'Boris Yeltsin' (English spelling in both locales), replacing the old `pressSecretary`/`tvPundit`/`furHatGuy` keys. Followed Canada's existing precedent exactly (`battle.unit.canada.carney` etc. are 'Mark Carney' verbatim in `fr.ts` too) — real names aren't localized, per this file's rule 8 (character names only come from the name layer). Worth flagging: the user's own message spelled these in French ("Poutine", "Gorbatchev", "Eltsine") — the English spelling was a deliberate choice to match the existing real-name convention, not an oversight, but easy to switch if French spelling was actually wanted for the French locale specifically.
    - **Verification**: `grep`-confirmed the old ids/keys (`russia-press-secretary`, `pressSecretary`, etc.) appeared nowhere outside `battleRosters.ts`/`en.ts`/`fr.ts` before renaming, and `battleRosters.test.ts` only checks structural invariants (uniqueness, positive stats, `nameKey` defined, roster length vs. spawn capacity) with no hardcoded Russia ids — confirmed safe to rename with no test changes needed. `tsc --noEmit -p tsconfig.app.json` — zero errors in any of the 3 touched files (the only errors anywhere in the run are the sandbox's pre-existing, unrelated missing-`@types/react` noise in other files, same category documented elsewhere in this doc). Full real dynamic test run (`tsx --tsconfig tsconfig.app.json`, all 33 `*.test.ts` files, same recovered vitest shim from the entry above) — 0 failures, `battleRosters.test.ts` itself: 8 passed. `prettier --write` on all 3 touched files — no changes needed. Synced 19 files to the device (3 text files + 16 PNGs, tank included) — all landed on the first `device_commit_files` attempt; text files confirmed byte-identical via `cmp`, `putin-front.png` spot-checked pixel-identical via `PIL` despite a device-side byte-size difference (the project's known C2PA-provenance-stamp artifact, not corruption).
    - **Verification**: full `tsc --noEmit -p tsconfig.app.json` — 604 errors project-wide, every single one bucketed by `TS####` code and confirmed to fall in the long-documented noise categories above (`TS7026` JSX-intrinsics, `TS7006` implicit-any, `TS7016` missing-module, `TS2322` `key`-prop mismatches, `TS2882`), nothing new; `engine/hint.ts` and `data/balance.ts` show **zero** errors (pure files). One real bug the type-checker _did_ catch and this session fixed: `spriteOcclusion.ts`'s first draft used indexed `units[i]`/`units[j]` array access in a nested loop, which this repo's `noUncheckedIndexedAccess` correctly flagged as possibly-`undefined` despite the loop bounds making that impossible — rewritten with `.entries()`/`.slice()` instead. `prettier --check`/`--write` clean on every touched file. And — new this round, see the note above — a **real full dynamic test run**: all 33 `*.test.ts` files across the whole project, executed for real via `tsx` and the recovered shim, **352 assertions, 0 failures**, including the 9 new `hint.test.ts` cases (one of which caught a genuine test-authoring mistake on the first run — a fixture accidentally set `actionsLeft: 0`, which made _every_ action unavailable instead of just the intended one, and the dynamic run is what surfaced it, not the type-checker) and the 9 new `spriteOcclusion.test.ts` cases (anchored against the real `US_SPAWN_POSITIONS`/`ENEMY_SPAWN_POSITIONS` data, not just synthetic coordinates). Synced all 15 touched/new files to the device (stage → commit → re-stage → byte-diff) — `CLAUDE.md` itself needed a `force: true` retry (came back stale on the first re-stage at only 192 lines, an old snapshot, despite the commit reporting success — the same recurring pattern documented throughout this file), every other file landed byte-identical on the first attempt.
  - **World Map landmark markers were plotted in the wrong places** (user: "The landmark position of the map are off. Are you able to replace them yourself or you need help?"). Answered "yes, myself" and did it without needing a screenshot from the user: `data/landmarks.ts`'s doc comment already says `mapX`/`mapY` are "tuned by eye against `world-map.png`," but checking the actual asset showed the original values were guesses that never actually got checked against it — `pacificOcean` landed on Baja California/mainland Mexico instead of open ocean, `denali` sat off the edge of the Alaska landmass into open water, and `grandCanyon`/`mississippiRiver`/`lakeMichigan`/`niagaraFalls` were bunched tightly enough to nearly overlap. Method: rendered candidate dots directly onto `public/assets/maps/world-map.png` at pixel coordinates (`PIL`/`ImageDraw`, gridded/upscaled crops of the Alaska and continental-US regions for precise reading), compared against real US geography, and iterated 3 passes until every dot sat in its real location — confirmed `.map-world`'s CSS (`background-size: 100% 100%`, no letterboxing) means a straight percentage-of-image-size mapping is exactly right, no aspect-ratio correction needed. All 7 `mapX`/`mapY` pairs updated in `data/landmarks.ts`; doc comment above the array records why and how, so a future pass doesn't have to re-derive the method from scratch. No test file exists for landmark coordinates specifically (`grep`-confirmed only `countries.test.ts` references `mapX`/`mapY`, and it's about countries, not landmarks) and no `LandmarkDef` shape changed, so this is a pure data-value fix.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — zero errors touching `landmarks.ts` (only the sandbox's usual pre-existing missing-`@types/react` noise elsewhere, same category as every other entry in this file). `prettier --write` — no changes needed. Full dynamic test run, all `*.test.ts` files — 0 failures, unchanged in count (no test asserts on specific landmark coordinates). Synced to the device, confirmed byte-identical via `cmp`.
  - **The unit whose turn it currently is could render semi-transparent, mid-battle** (user, screenshot of a Russia battle at Round 3: "Melania is playing. She shouldn't be transparent"). Root cause: by Round 3 units had moved off their spawn tiles, and three US units ended up stacked one tile-height apart on the same screen column — a deeper version of the spawn-formation occlusion this session already found and fixed earlier (see "blocking-teammate sprites fade" above), except this time three deep instead of two. `spriteOcclusion.ts`'s own three-deep test case describes the correct general behavior (the two closer-to-camera units fade, the furthest-back one doesn't) and that logic was working exactly as designed — the actual gap was that the currently-active unit (Melania that round) had landed in the "closer to camera" half of the stack and got faded along with everyone else, with nothing in the code distinguishing "occluded" from "occluded _and this is the one you're currently deciding a move for_." Confirmed from the screenshot itself: the real flag pastilles all stayed fully visible even deep in the same occlusion stack (ruling out a separate badge-hiding bug for this report specifically — occlusion fades only `.battle-unit-sprite`, by design, and that held up here), so the fix is scoped precisely to the sprite-fade-vs-active-turn interaction, not the badge.
    - Fix: `BattleView.tsx`'s `<BattleUnitToken>` call now passes `isOccluding={occludingIds.has(u.id) && active.id !== u.id}` instead of the bare `occludingIds.has(u.id)` — one-line, since `isCurrent`/`active.id` was already being computed right there for the marker prop. Being occluded and being the active unit are orthogonal conditions; when both are true, staying fully visible wins, since the active unit is the one thing you most need to see clearly to decide its move. No change to `spriteOcclusion.ts` itself — the occlusion _detection_ was already correct, this only changes how `BattleView` _acts_ on it for the one unit where fading is actively counterproductive.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — no new errors touching `BattleView.tsx` beyond the file's pre-existing missing-`@types/react` noise (present before this change too, confirmed by grepping the file's error list specifically). `prettier --write` — no changes needed. No component-level test exists for `BattleView.tsx` to extend (same gap noted in this file's "battle units now turn to face" entry — JSX-level behavior here has never had test coverage in this project), so this is verified by the screenshot-driven root-cause analysis above rather than an automated test; flagged rather than silently assumed complete. Synced to the device, confirmed byte-identical via `cmp`.
  - **Investigated a separate, not-yet-reproduced report: "Look at JD Vance. The pastille is under the tiles."** (text-only, no screenshot for this one specifically). Built an isolated, pixel-faithful reproduction of a single unit's token plus its neighboring floor tiles (Playwright + the real `battle.css`/`room.css`/`global.css` against a real Vance sprite and the real US flag badge, geometry computed with the actual `projectIso`/`isoDepth` functions) to test whether the badge's own anchor position ever geometrically crosses into a neighboring tile's higher-z-index territory — it doesn't; an isolated unit's badge renders correctly and entirely within its own tile in every position checked. Also confirmed via the Melania screenshot investigated the same round that flag badges stay fully visible even deep in a real 3-unit occlusion stack, ruling out the "front unit's token covers the back unit's badge" theory that isolated-unit geometry alone couldn't rule out. **Unresolved**: without a screenshot of this specific report, there's no way to tell which of Vance's tiles, or which other unit/wall might have been involved, or whether "under the tiles" means hidden by a wall block (`IsoBlock`, not diamond-clipped like a floor tile, so a tall obstacle nearby could plausibly cover a badge in a way a floor tile can't) rather than another unit. Flagged to the user rather than guessing further or making a speculative code change with no reproduction to verify it against.
  - **Iran's roster replaced the same way Russia's was, independently re-triggering and re-confirming the same real-currently-serving-head-of-state override.** User supplied a 3-row reference sheet (a Khamenei-like Supreme Leader, a generic suited official, a generic militia fighter, each front+back) and asked for a 5-unit roster: "1 of the top sprites, 2 of the second, 2 of the third." Before wiring anything in, flagged the same conflict `battleRosters.ts`'s policy exists for — the top sprite reads as a clear likeness of Ali Khamenei, Iran's actual currently-serving Supreme Leader, squarely the case the rule covers (Iran is explicitly named alongside Russia/Venezuela in that doc comment). Asked via `AskUserQuestion`; answer was the same as Putin's: **"Use him anyway"**. A second question confirmed scope: **"Replace all 3 with the new 5"** — full replacement, not a partial merge, acknowledging up front that growing 3→5 units shifts battle balance (same lesson as Greenland's 5th unit) and would need a simulator re-tune afterward. `battleRosters.ts`'s top-of-file doc comment updated to describe both overrides together as separate, per-country asks rather than a blanket policy change (Venezuela still gets nothing beyond its invented archetypes).
    - **Sprite generation**: same row/column alpha-profile crop method as Russia's sheet (3 row bands instead of 4, 2 poses per row) — `khamenei-`/`official-`/`militia-{front,back,left,right}.png` in `public/assets/sprites/iran/`, left/right generated via `ImageOps.mirror()` same as before.
    - **Roster mapping**: only the top sprite (Khamenei) is a specific real/named person — the middle ("official") and bottom ("militia fighter") sprites read as generic archetypes with no identifiable named individual, so they kept this roster's existing invented-character spirit rather than needing their own real-person check. `iran-khamenei` is new; `iran-spokesperson` and `iran-tv-anchor` **keep their existing ids/i18n keys** and just gain the shared "official" art (2 units, same sprite — same "one sheet, several units" pattern as Greenland's bears); `iran-drone-hobbyist` is dropped entirely (its art never matched a militia fighter with a keffiyeh) in favor of two new units, `iran-irgc-fighter` and `iran-basij-volunteer`, sharing the "militia" art.
    - **Balance**: growing from 3 units (old total `maxComposure` 31) to 5 (initial guess totaled 53) dropped the simulated US win rate to 70.8% — technically over the `>0.7` regression guard in `scripts/simulateBattle.test.ts` but only barely, an uncomfortably thin margin compared to every other non-Canada country (all ≥85%). Tuned down before shipping rather than leaving it at a bare pass: Khamenei `power` 5→4/`maxComposure` 13→11, `irgc-fighter` `power` 4→3/`maxComposure` 10→9, `basij-volunteer` `power` 3→2/`maxComposure` 9→7 (via a scratch `scripts/iranTune.ts` for fast iteration, deleted after use, not checked in). Landed at **93.3%** — comfortably in the same "clearly winnable" tier as Russia (94.0%) and Greenland (85.3%).
    - **i18n**: `en.ts`/`fr.ts` both gained `battle.unit.iran.khamenei` = 'Ali Khamenei' (English spelling in both locales, real-name-not-localized convention, same as Russia's Putin/Gorbachev/Yeltsin and Canada's roster), `irgcFighter`/`basijVolunteer` (translated archetype titles, since these are invented-flavor units like the ones they're replacing — French: 'Combattant du CGRI' / 'Volontaire bassidji'). `spokesperson`/`tvAnchor` keys untouched.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — zero errors in any of the 3 touched files. Full dynamic test run, all 33 `src/*.test.ts` files — 0 failures, unchanged in count (`battleRosters.test.ts`'s structural checks pass with no updates needed, confirming the earlier grep-based safety check was right that nothing hardcodes the old Iran ids/keys). `scripts/simulateBattle.test.ts` (not part of the `src/` sweep, run separately) — both cases pass, Iran at 93.3%. `prettier --write` — no changes needed. Synced 15 files to the device (3 text files + 12 PNGs) — all landed on the first `device_commit_files` attempt; text files byte-identical via `cmp`, one PNG (`khamenei-front.png`) spot-checked pixel-identical via `PIL` despite the usual device-side C2PA-stamp byte-size difference.
  - **Two more real user-feedback fixes, landed together: enemy units facing literally up/down instead of toward the US, and the occlusion-fade feature firing on units unrelated to whoever's actually taking their turn** (user, mid-session: "the opponents (other team than the USA) should be facing USA. The only the in front and around the USA current playing player should be semi transparent"):
    - **Facing bug**: `directionTo(from, to)` (`engine/battle.ts`) picks whichever axis has the bigger absolute delta, ties favoring horizontal — correct for left/right, but once units maneuver off their spawn columns mid-battle a move or attack whose delta is dominated by the *y* axis resolves to a literal `'up'`/`'down'` facing, which reads as "facing the sky/ground" rather than "facing the opponent" for a side that's supposed to be oriented horizontally across the field. Fix: new private `towardOpponent(direction, side)` reorients only the `'up'`/`'down'` case — `'right'` for `us`, `'left'` for `enemy` — leaving an already-meaningful `'left'`/`'right'` result untouched. Applied in both `moveUnit` (facing the destination tile) and `attack` (facing the defender) right where each already calls `directionTo`.
    - **Occlusion-scope bug** (a narrowing of the whole-roster occlusion-fade feature from earlier this session, not a new bug in that feature itself — it was working exactly as built, just built at the wrong scope per this feedback): `spriteOcclusion.ts`'s `occludingUnitIds` computes every occluding pair on the field at once, which `BattleView.tsx` used to fade *any* front-half unit regardless of whose turn it was — including pairs nowhere near the currently active unit, read as distracting noise once you're actually looking at one specific unit to decide its move. New `unitsOccludingActive(units, activeId)` computes only the units currently covering one specific target's sprite (painter's-order check against just that unit, not an all-pairs scan); `occludingUnitIds` itself is kept, unchanged, both as the more general primitive this one is built from and because `spriteOcclusion.test.ts` still exercises it directly against real whole-roster spawn data. `BattleView.tsx` swapped its one `occludingIds` computation from `occludingUnitIds(living)` to `unitsOccludingActive(living, active.id)` — since the new function only ever returns units *other than* the active one, the extra `active.id !== u.id` guard this session's earlier Melania fix added to the `isOccluding` prop became redundant and was removed.
    - **Test coverage**: `battle.test.ts` — the existing "turns the unit to face the direction it moved" test's up/down cases now expect `'right'` (was `'up'`/`'down'`) for a US-side unit; new dedicated tests for both `moveUnit` and `attack` confirm a purely vertical delta reorients to `'right'`/`'left'` by side rather than staying literal. `spriteOcclusion.test.ts` — new `describe('unitsOccludingActive', ...)` block (4 cases, using the real `US_SPAWN_POSITIONS` data): flags only a unit actually in front of the active one and ignores an unrelated occluding pair elsewhere on the field; is empty when the active unit is itself the front of a pair (nothing occludes *it*); is empty when the active id isn't found.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — zero errors in any of the 3 touched TS/TSX files beyond the sandbox's long-documented pre-existing missing-`@types/react` noise elsewhere. Full dynamic test run (`tsx`, the recovered vitest shim) — 0 failures across the whole suite, including all new/updated cases above. `scripts/simulateBattle.test.ts` re-run separately to confirm the facing change is purely cosmetic — every country's US win rate unchanged from its last-recorded value (Canada ~43%, Iran 93.3%, Russia 94.0%, Greenland 85.3%, all others ≥85%). `prettier --write` on all 5 touched files. Synced `battle.ts`, `battle.test.ts`, `spriteOcclusion.ts`, `spriteOcclusion.test.ts`, `BattleView.tsx` to the device — all 5 landed byte-identical on the first `device_commit_files` attempt, confirmed via a fresh `device_stage_files` + `cmp` (no `force` retry needed this round).
  - **New hidden mechanic: Declare-War-on-Iran outcomes move a hidden oil-price index that feeds Felt Inflation** (user feedback: "When you attack Iran, if you loose, huge increase in oil price. You win, il price goes down."). Asked two clarifying questions first, since nothing like this existed anywhere in the codebase (confirmed by grep — the only prior "oil" hit was Venezuela's flavor-only `oil-executive` battle unit, no mechanic behind it): whether Oil Price should be its own visible HUD stat or a hidden modifier that just shows up through existing stats, and whether it should be a one-time jolt or an ongoing monthly economic pressure. User picked **hidden** (no new HUD element) **+ ongoing** (a real monthly economic effect, not just a single jolt at battle-resolution time) — the combination that needed the most actual engine work of the four options, not the simplest one.
    - **`GameState.oilPriceIndex: number`** (`engine/types.ts`), new, starting at 0 (`engine/state.ts`'s `createInitialState`) — same category as `elonRage`: a cabinet/geopolitics-adjacent mechanic, not a `StatKey`/`NationStats` value, never shown on the HUD. The player only ever feels it through Felt Inflation drifting, never as a number of its own.
    - **The shock itself** (`engine/effects.ts`'s new `applyOilPriceShock(oilPriceIndex, country, success, balance)`): a no-op for every country except Iran — same "a live-real-world-tension country gets its own carve-out, never a blanket rule" precedent this file's real-person roster overrides already established, just for a mechanic instead of a sprite. Lives outside the generic `Effect` DSL entirely (not a new `Effect` kind) because `declare_war`'s `onSuccess`/`onFail` lists are shared by every war target and have no way to say "only if it was Iran" — instead it's called directly from `engine/resolve.ts`'s `resolveBattleAction`, right after `finalizeAction`, using the `country`/`success` that function already receives. Clamped to `balance.oilPrice.maxIndex` (±100) so a losing streak can't compound forever.
    - **The monthly effect** (`engine/economy.ts`): `applyEconomyFormulas` gained an `oilPriceIndex` parameter and adds `oilPriceIndex * balance.oilPrice.feltInflationPerPoint` directly into the existing Felt Inflation formula, on top of its own drift toward official inflation — the one and only channel the hidden index reaches a stat the player can actually see. New `decayOilPriceIndex` multiplies the index by `balance.oilPrice.decayFactor` (0.85) every month-end in `tickMonth`, snapping to exactly 0 once it's below 0.5 so a run doesn't carry a permanent floating-point residue — a shock fades back toward neutral over time rather than being a permanent step change.
    - **Tuning** (`data/balance.ts`'s new `oilPrice` block, its own section same as `hint`'s): `lossJolt: 30`, `winRelief: -15` (asymmetric, matching "huge increase" vs. a plainer "goes down"), `maxIndex: 100`, `decayFactor: 0.85`, `feltInflationPerPoint: 0.03`. Inherited unchanged by every difficulty (`BALANCES` spreads `...BALANCE`, no override needed).
    - **Old-save compatibility**: `store/gameStore.ts`'s `continueGame` now normalizes `oilPriceIndex: saved.game.oilPriceIndex ?? 0` when loading `saved.game`, so a save from before this feature existed doesn't hand every economy formula an `undefined` — same "fall back rather than crash" comment already there for `melaniaRoomId`/`difficulty`, just applied to a `game`-level field for the first time (a real, if narrow, pre-existing gap this session noticed while adding this: `renamedLandmarks`/`elonRage` never got the equivalent guard when they were added).
    - **Test coverage**: `effects.test.ts` — new `describe('applyOilPriceShock', ...)` (no-op for every non-Iran country, adds the jolt/relief from any starting index, clamps both directions). `resolve.test.ts` — 3 new cases under `resolveBattleAction` (leaves the index untouched for a non-Iran win/loss; spikes/eases it correctly for an Iran loss/win, both to the exact configured `lossJolt`/`winRelief`; clamps to `maxIndex` across repeated Iran losses — refilling EA each loop iteration, since the test is exercising the clamp across many battle outcomes, not a real run's action budget). `economy.test.ts` — new `describe('tickMonth: hidden oil-price index', ...)`: a neutral index changes nothing versus the plain baseline; a positive index measurably raises Felt Inflation and a negative one measurably lowers it versus that same baseline; the index decays by exactly `decayFactor` each month in both directions and fades to exactly 0 after enough months. `state.test.ts` — asserts `createInitialState().oilPriceIndex` is 0.
    - **Verification**: `tsc --noEmit -p tsconfig.app.json` — zero errors in any of the 6 pure engine/data files touched (`types.ts`, `state.ts`, `effects.ts`, `resolve.ts`, `economy.ts`, `balance.ts`); the only errors anywhere near `store/gameStore.ts` are the sandbox's long-documented missing-`zustand`-types noise, unrelated to the one-line change there. Full dynamic test run (`tsx`, the recovered vitest shim), all 31 `src/*.test.ts` files — 346 assertions, 0 failures, including every new case above (one test-authoring mistake caught and fixed on the first run, not the type-checker: the original clamp test looped `resolveBattleAction` 20 times without refilling `actionsLeft`, so it started throwing "not available" after the first couple of iterations — fixed by resetting `actionsLeft` each loop, since the test is only exercising the clamp, not a real run's EA budget). `scripts/simulate.ts` (the economy Monte Carlo) re-run directly and confirmed to never touch the new mechanic at all — it resolves `declare_war` via the plain `resolveAction` path, not `resolveBattleAction`, so `oilPriceIndex` stays 0 throughout every simulated run; the small variance versus this file's last-recorded survival rates (random 0.6% vs. 1.5%, cautious 72.0% vs. 71.5%) is ordinary run-to-run RNG variance, not a regression, confirmed by the fact the code path this feature touches is never even called. `scripts/simulateBattle.test.ts` re-run separately and unaffected (2 passed) — the tactical-battle simulator has no economy model to touch. `prettier --write` on all 12 touched files (`resolve.ts`/`economy.ts`/`economy.test.ts` needed reflow, the rest were already clean). Synced all 12 files (6 engine/data source files, `gameStore.ts`, 4 test files, `GAME_PLAN.md`) to the device — all landed byte-identical on the first `device_commit_files` attempt, confirmed via a fresh `device_stage_files` + `cmp`, no `force` retry needed.
