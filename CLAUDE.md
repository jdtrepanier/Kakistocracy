# CLAUDE.md — Kakistocracy: Survive the Term

Instructions for AI coding assistants (and humans) working on this repo.
The full game design is in **[docs/GAME_PLAN.md](docs/GAME_PLAN.md)**. Read the relevant section before building a feature.

## What this is

An isometric satirical political management game for the web, styled like a cozy life-sim (think _The Sims_) rather than a top-down SNES RPG. You play a 4-year term (JAN 2024 → DEC 2027) as a cabinet of 6 officials, spending 3 Executive Actions per month while five national stats (Debt, Interest Rate, Inflation, Party IQ, Happiness) plus DEFCON try to end the run. Survive the term to win. _Shining Force_ is the reference for the screen layout (windows, portraits, cross-shaped menu) — and, for Declare War specifically, for actual turn-based tactical combat too (`engine/battle.ts`, GAME_PLAN §7.1). Every other action still resolves with a plain success roll, no grid involved.

## Commands

The project uses **Yarn 1** (`yarn.lock`). npm also works (`npm start`, `npm run verify`).

| Command           | What it does                                                                   |
| ----------------- | ------------------------------------------------------------------------------ |
| `yarn start`      | Dev server (http://localhost:5173), opens the browser                          |
| `yarn dev`        | Dev server without opening the browser                                         |
| `yarn build`      | Typecheck + production build into `dist/`                                      |
| `yarn preview`    | Serve the production build                                                     |
| `yarn test`       | Run all Vitest tests once                                                      |
| `yarn test:watch` | Tests in watch mode                                                            |
| `yarn typecheck`  | TypeScript only                                                                |
| `yarn lint`       | ESLint                                                                         |
| `yarn format`     | Prettier (write)                                                               |
| `yarn verify`     | **typecheck + lint + format check + tests. Run before calling any task done.** |

Requires Node 22 LTS (see `.nvmrc`; Node ≥ 20.19 works).

## Architecture rules (non-negotiable)

1. **`src/engine/` is pure TypeScript.** No React, no Zustand, no Pixi, no DOM, no `localStorage`. ESLint enforces this.
2. **No `Math.random()` or `Date.now()` in `engine/` or `data/`.** Use `createRng()` from `engine/rng.ts` (seeded, so runs are replayable) and the in-game calendar from `engine/calendar.ts`.
3. **Engine functions are pure:** take a state, return a new state. Never mutate `GameState`.
4. **Content is data.** Actions, characters, events, showdowns, rooms and endings live in `src/data/`. The engine interprets them.
5. **No magic numbers in the engine.** Every tunable value goes in `src/data/balance.ts`.
6. **`src/store/gameStore.ts` is the only bridge** between React and the engine. Components read from the store and call its actions; store actions call engine functions.
7. **No hard-coded UI text.** Add a key to `src/i18n/en.ts` _and_ `src/i18n/fr.ts` (TypeScript fails if French is missing a key), then use `const t = useT()` and `t('key')`.
8. **Character names only come from `src/data/characters.ts`** (the name layer, real vs. parody names). Never type a character's name in a component or string.
9. **Every engine change comes with Vitest tests**, colocated as `*.test.ts`.

## Layout

```text
src/
├─ engine/    pure game rules (calendar, rng, state; economy, effects, actions, endings, movement…)
├─ data/      content + balance numbers (actions, characters, countries, rooms, roomLayouts…)
├─ store/     Zustand store: the bridge
├─ i18n/      en.ts (source of keys), fr.ts, translate.ts
├─ ui/        React components (hud/, room/, menus/, dialogue/, screens/) + hooks
├─ styles/    global.css imports the other CSS files
├─ App.tsx    stage + screens
└─ main.tsx   entry point
public/assets/   sprites, tilesets, Tiled maps, audio
scripts/         Node scripts (balance simulator in Phase 1)
docs/            GAME_PLAN.md
```

Import with the `@/` alias (`@/engine/calendar`), relative imports inside the same folder are fine.

## Conventions

- TypeScript strict mode with `noUncheckedIndexedAccess`. No `any`; prefer `unknown` + narrowing.
- Named exports only (no default exports).
- Files: `camelCase.ts` for modules, `PascalCase.tsx` for components, tests next to the code (`calendar.test.ts`).
- Type-only imports use `import type`.
- Formatting is Prettier's job (single quotes, trailing commas, 100 columns). Don't hand-format.
- Commit messages: short imperative summary (`Add monthly economy tick`).
- Add dependencies with `yarn add` (or `yarn add -D` for tools), and ask before adding one. Current runtime deps: react, react-dom, zustand, @fontsource/press-start-2p.

## Pixel art and screen rules

- Internal resolution **480×270**, scaled up with whole-number zoom (`ui/useStageScale.ts`).
- Rooms are **isometric** (2:1 dimetric), not a flat top-down grid: `ui/room/isometric.ts` projects a room-grid `{x, y}` position to a diamond footprint **32px wide × 16px tall**; walls and objects are extruded blocks (`clip-path`-based, no new dependency). `engine/movement.ts` and `data/roomLayouts.ts` still describe rooms as a plain rectangular grid — only the rendering layer projects that grid isometrically, so a real PixiJS isometric renderer later only touches `ui/room/`.
- Font: Press Start 2P at **8px** (or 16px for titles). One character = 8px wide: count characters when laying out the HUD.
- Short names must fit in 8 characters.
- The 6 US officials and Canada's battle roster use real 4-direction pixel-art sprites (`public/assets/sprites/`, wired via `data/characters.ts`'s `sprite`/`spriteForDirection` and `data/battleRosters.ts`'s `sprite` field on `BattleUnitTemplate`). Every other character (other countries' rosters) still falls back to a coloured square with initials (`placeholder` in `data/characters.ts` / `battleRosters.ts`).

## Tone guardrails (docs/GAME_PLAN.md §14)

- Satirize public actions and policies, not bodies, families or private lives.
- No invented quotes that could pass for real statements. Parody must be obviously parody.
- Cartoon disasters only: wars and meltdowns are gags, maps and headlines, never casualties.
- Opponents, media and bureaucrats get roasted too.

## Status

- [x] **Phase 0 — Setup:** Vite + React + TS, ESLint (with engine guard), Prettier, Vitest, folder structure, name layer, EN/FR strings, seeded RNG, calendar, placeholder title screen + HUD.
- [x] **Phase 1 — "Spreadsheet" prototype:**
  - `engine/effects.ts`: effect DSL (`delta`/`spread`/`delayed`/`chance`/`flag`/`rage`) + delayed-effect queue (`tickPending`)
  - `engine/economy.ts`: month-end tick (§6 formulas, constants from `balance.ts`), DEFCON calm-recovery, Congress/midterms/impeachment tracking
  - `engine/actions.ts` + `engine/resolve.ts`: availability rules (3 EA/month, once-per-month safe actions, IQ gates), success %, success/backfire, diminishing-returns Headlines. _One-action-per-official is deferred to Phase 2_, since there's no character-selection UI yet.
  - `engine/endings.ts`: all loss endings (bankruptcy, hyperinflation, brainFreeze, revolt, nuclear, impeachment) + survival at DEC 2027, with fixed precedence
  - `engine/scoring.ts`: Headlines → rank
  - `data/actions.ts`: 17 actions from §9
  - UI: action list with success %, End Month button, month-end report, ending screens, all EN/FR strings
  - `scripts/simulate.test.ts`: Monte Carlo balance simulator (3000 runs × random/cautious policy) — also asserts survival-rate bounds as a regression guard, run via `yarn simulate`
  - Typecheck, lint and format all verified clean on the real toolchain. **`yarn test` / `yarn verify` / `yarn simulate` still need to be run on a native Windows or macOS/Linux shell** — the assistant's device-bridge shell is a Linux VM whose mounted `node_modules` only has Windows-native Rollup bindings (`@rollup/rollup-win32-*`, no `@rollup/rollup-linux-x64-gnu`) and no network access to fetch the missing one, so Vitest can't execute from there. Run `yarn verify` yourself once from a normal terminal to confirm the suite passes.
- [x] **Phase 2 — Room slice:**
  - `engine/movement.ts`: pure grid movement/collision (`isWalkable`, `moveWithin`) — data in, position out, so the CSS/DOM renderer below can be swapped for real PixiJS/Tiled later without touching it
  - `data/roomLayouts.ts`: Oval Office + Treasury as ASCII-grid tile layouts, one action-bound object each, a door connecting them
  - `data/countries.ts` + new `declareWar`/`purchaseCountry` effects: **World Map menu** — Declare War / Buy a Country each pick a random not-yet-hit country (Canada, Greenland, Panama, Mexico) via the seeded RNG; `GameState` tracks `atWarWith`/`countriesOwned`
  - UI: `RoomView` (CSS/DOM tile grid — see the isometric pivot below, arrow-key/WASD movement, facing + object hint), `OfficialWindow` + `RoomWindow` (§11's info boxes), `CrossMenu` (DECREE/TALK/ITEM/SWITCH), `MapScreen` (world status board), `ActionsOverlay` (DECREE = room-scoped actions, and a toolbar "Executive Actions" escape hatch to the full catalog so nothing is lost while only 2 of 10 rooms exist), TALK/ITEM stubs for Phase 3/5
  - Switching between 3 officials (Trump, Bessent, Vance) — one shared party position for now; per-official positions are a later refinement
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as Phase 1 for actually running the suite — see above)
- [x] **Phase 3 — Resolution & showdowns:**
  - `engine/preview.ts`: pure `summarizeEffects` — walks an action's effect list into an immediate/over-time stat summary for the Preview window (recurses into `chance`'s `then` branch, flags `hasChance` and `hasRandomCountry`, ignores `flag`/`rage`)
  - `engine/showdown.ts` + `data/showdowns.ts`: showdown DSL (3 rounds × 3 dialogue choices, each choice a success-chance modifier) — two showdowns, **Fed** (paired with Lower Interest Rates) and **Press Conference** (paired with a new `press_conference` action, action #18/§9 row 33, since none existed to pair with a second showdown)
  - `engine/resolve.ts`: `computeSuccessChance`/`resolveAction` gained a backward-compatible `successModifier` parameter so a showdown's total modifier reaches the actual roll, not just the preview display
  - `store/gameStore.ts`: `performAction` replaced by a `resolution` state machine — `beginAction` → (`showdown` rounds, only for the 2 paired actions) → `preview` (confirm/cancel) → roll → `result` (revealed after a brief delay) → `continueResolution` applies the state and logs it, same as the old month report
  - UI: `ResolutionOverlay` renders all three phases (showdown dialogue, preview with success % and stat-delta list, delayed roll reveal with headline flavor text and a stat-diff table matching the month-end report's style)
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as Phase 1/2 for actually running `yarn test`/`verify`/`simulate` — see above)
- [x] **Art direction pivot — isometric room camera (pre-Phase 4):** dropped the flat top-down 16-bit grid for a 2:1 isometric ("dimetric") camera, closer to _The Sims_' dollhouse view (see GAME_PLAN v0.3's changelog and §11).
  - `ui/room/isometric.ts` (new, pure, tested): `projectIso`/`isoGridBounds`/`projectIsoWithin` turn a grid `{x, y}` into a diamond-projected screen point; `isoDepth` gives painter's-algorithm draw order; `buildCubeFaces` builds the three `clip-path` polygons (top/left/right) for an extruded wall or object block
  - `ui/room/RoomView.tsx`: fully rewritten to render floor/door tiles as flat diamonds and walls/objects as extruded blocks, with a camera (`ui/room/useElementSize.ts` measures the viewport) that follows the player, translating a `room-iso-world` container instead of the old fixed 1:1 tile grid
  - `engine/movement.ts` and `data/roomLayouts.ts` are **untouched** — rooms are still plain rectangular grids; only the rendering layer reinterprets them isometrically, so a future real PixiJS isometric renderer only touches `ui/room/`
  - HUD chrome, the pixel font, and menu conventions are unchanged for now — this pivot is scoped to the room camera only; a broader UI restyle is a possible later art pass, not part of this change
  - i18n: `game.subtitle` (EN/FR) no longer says "16-bit"
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as above for actually running the suite)
- [x] **Pivot — Tactical battles for Declare War (pre-Phase 4):** Declare War now plays out as a _Shining Force_-style turn-based battle instead of an instant roll (GAME_PLAN §7.1); battle outcome (`usWin`/`enemyWin`) directly drives the action's success/fail effects.
  - `engine/battle.ts` (new, pure, tested): grid, interleaved turn order, BFS `reachableTiles`, `moveUnit`/`attack`/`endTurn`, a simple `enemyTakeTurn` AI, `checkOutcome`
  - `data/battlefield.ts` (new): the fixed battlefield grid + US/enemy spawn points
  - `data/battleRosters.ts` (new): `US_BATTLE_UNITS` (the 3 switchable officials) and `BATTLE_ROSTERS` per country — Canada's roster is the six names requested for this feature (5 real officials + 1 fictional wildcard), Greenland is polar bears; **Iran, Venezuela and Russia (new war-only targets) use invented, non-real-person rosters** rather than real current heads of state, since — unlike the original four — they're in live real-world conflicts (see that file's doc comment)
  - `engine/effects.ts`/`engine/resolve.ts`: `resolveWarEffects` substitutes the battle's actual target country into `declare_war`'s data-declared random-country effect at resolution time; `resolveBattleAction` rolls from the battle's own evolved RNG state, not the pre-battle one (so the battle's moves/attacks aren't replayed)
  - `store/gameStore.ts`: new `battle`/`battleCountry` resolution-phase state, `battleMove`/`battleAttack`/`battleEndTurn`/`battleRunEnemyTurn` actions, `settleBattle` helper
  - `ui/battle/BattleView.tsx` (new): the battle screen, wired into `ResolutionOverlay`'s new `'battle'` phase
  - `data/countries.ts`: added `iran`/`venezuela`/`russia` (war-only, not purchasable)
  - Typecheck, lint and format verified clean on the real toolchain (same Linux-VM Rollup blocker as above for actually running the suite)
- [x] **Phase 4 — Vertical slice:**
  - 3 new rooms + roster/action expansion (22 actions total), all 6 officials playable with their own actions
  - `data/events.ts` + `engine/events.ts`: 20-entry random monthly event system (`rollMonthlyEvent`, weighted, calls `checkEnding`), surfaced in the month-end report and ticker
  - Congress/midterms UI: banners in `MonthEndReport.tsx`, a Congress-lost `HudCell`
  - Save/load: `store/gameStore.ts`'s `SavedGame` (localStorage-backed autosave) + Continue/New Game on the title screen
  - `ui/audio/sfx.ts` + `MuteToggle.tsx`: placeholder Web Audio chiptune blips/jingles/ambient hum, wired in: `App.tsx` runs the ambient hum only on the room/battle screen and mounts `MuteToggle` next to `LangToggle`; `CrossMenu`/`GameScreen`'s toolbar play `blip` (or `confirm` for End Month); `ResolutionOverlay` plays `blip` for showdown choices and the result-continue button, `confirm`/`cancel` for the preview buttons, and `success`/`fail` at the moment a result is revealed; `MonthEndReport` plays `headline` when a new report appears; `EndingScreen` plays `success`/`fail` once when a run ends
  - Real pixel-art sprites for the 6 US officials and Canada's 6-person battle roster (`public/assets/sprites/us/`, `public/assets/sprites/canada/`, cropped from user-provided reference sheets), replacing their coloured-square placeholders on the title screen, room camera, official windows, resolution preview and battle screen — every other country's battle roster is still `placeholder` art
  - Title screen polish: roster in a framed panel with a hover highlight and per-official name-tag color, brief fade-in on load, and `playSfx` on Start/Continue/New Game
  - Typecheck, lint and format verified clean on the real toolchain by syncing to the device via the remote-devices bridge and running `tsc`/`eslint`/`prettier` there (found and fixed two files where hand-predicted Prettier formatting was slightly off). **`yarn test`/`vitest` still can't run from either the assistant's sandbox or the device-bridge shell** — same Linux-VM/Windows-Rollup-binary mismatch as every prior phase; run `yarn verify` yourself from a normal terminal to confirm the suite passes.
- [x] **Art update — US sprite sheet v2 (post–Phase 4):** replaced all 24 US official sprites (`public/assets/sprites/us/`) with a user-supplied transparent-background sheet (`public/assets/sprites/source/us-cabinet-sheet-v2.png`), extracted via an alpha-threshold bounding box (no more white-background ink detection) that also excludes the sheet's baked-in drop shadow so it doesn't double up with `.room-player-shadow`.
  - The new sheet's 4 poses per character are diagonal 3/4 views (front-left/front-right/back-left/back-right), not the old sheet's straight cardinal ones — `data/characters.ts`'s `CharacterSprite` slots are now assigned by which screen diagonal `ui/room/isometric.ts`'s `projectIso` actually draws each `Direction` moving toward (worked out from the projection formula, not guessed), so a character now visibly faces the right way when walking in every direction instead of just approximating it.
  - Canada's roster sprites are untouched (still the original sheet).
- [x] **Fix — player sprite z-index race during fast movement (post–Phase 4):** the player token could momentarily disappear behind a wall/object block while moving quickly. Root cause: `.room-player`'s `left`/`top` CSS glide (120ms, `ROOM_MOVE_TRANSITION_MS` in `ui/room/isometric.ts`) and its `isoDepth`-derived `z-index` (which jumps instantly when the store updates) fell out of sync mid-glide. A first attempt (delaying the `z-index` CSS transition by 120ms) only fixed the up/left case and made down/right worse, since a flat delay is wrong for moves that *increase* depth. Replaced with `ui/room/RoomView.tsx`'s `usePlayerDepth` hook: holds `Math.max(old depth, new depth)` immediately on every move (correct for both directions at once) and only settles to the new tile's true depth once the glide finishes, via a timer keyed to `ROOM_MOVE_TRANSITION_MS`; resets immediately (no `max`) on a room change, since depth isn't comparable across rooms. `.room-player-shadow` still uses the raw, un-held depth. `src/styles/room.css` no longer transitions `z-index` at all.
  - Typecheck/lint/format could **not** be run on the real toolchain for this fix — the device's local shell (`device_bash`) has been down since a Windows update on Sept 8 ("Windows update released September 8 prevents Claude's workspace from reaching your files"), so the change was synced to the device via direct file-write (`device_commit_files`) rather than the usual sync-and-verify flow. Run `yarn verify` yourself once the shell is usable again, and confirm in-game that the sprite no longer flickers/disappears in all four movement directions.
  - **Regression tests (`ui/room/playerDepth.ts` + `ui/room/playerDepth.test.ts`):** the max/settle decision that fixes the bug was pulled out of the `usePlayerDepth` hook into two pure functions — `holdPlayerDepth` (same-room: `Math.max(old, new)`; room change: snap to the new depth) and `settlePlayerDepth` (settle to the true depth) — so it can be tested directly with Vitest instead of through React rendering (this repo has no React component/hook test setup — no jsdom, no Testing Library — so keeping the actual fix logic pure and colocated with a plain `*.test.ts` file, like every other `engine`/`data` module, was preferred over adding that infrastructure for one hook). `playerDepth.test.ts` covers both directions explicitly using the real `step`/`isoDepth` functions (not just abstract numbers): down/right must rise to the new depth immediately, up/left must hold the old depth until settled, a room change must always snap rather than `max`, and a held-down key (repeated moves with no settle in between) must never dip. These tests reproduce the exact bug report ("only fixed for Left/Up, still disappears for Right/Down") and would fail against the first, CSS-only fix attempt.
  - Could not run this new test file on the real toolchain either — same `device_bash` outage as above, **and** this session's cloud sandbox currently gets `403 Forbidden` from the npm registry for any package (confirmed with a plain `npm view react version`), so `vitest` couldn't be installed there as a fallback. The test file was hand-traced instead (each assertion's expected value worked through by hand against `holdPlayerDepth`/`settlePlayerDepth`'s logic) and Prettier's line-wrap/collapse rules were approximated by hand (100-column check via a script). Run `yarn test` (or `yarn verify`) yourself to confirm the new tests actually pass and are clean.
- [ ] **Phase 5 — Content** (in progress):
  - **Rose Garden room** (`data/roomLayouts.ts`): the Oval Office hub's fifth walkable spoke, alongside Situation Room/Federal Reserve/Commerce from Phase 4. A new door on the Oval Office's north wall at `{x:6, y:0}` (distinct from Commerce's existing door at `{x:15, y:0}` on the same wall) leads to a new 22×7 room whose single DECREE object triggers the four actions already tagged `room: 'roseGarden'` in `data/actions.ts` — Rally, Press Conference, Flip and Be Best — which existed since Phase 1/3 but, until now, were reachable only through the toolbar's full-catalog escape hatch, never by walking there. No other code changed: `RoomView`, `ActionsOverlay`, and the door-transition logic in `store/gameStore.ts` all key off `RoomLayout`/`RoomId` generically, so a new room in `ROOM_LAYOUTS` is enough on its own.
  - `data/roomLayouts.test.ts`: extended the existing "hub spoke" loop test to include `roseGarden`, and added it to `hasRoomLayout`'s true-case list (now 6 of the 10 rooms in §10 have a walkable layout, up from 5).
  - Verified by hand against the actual test assertions (rectangular grid, object/door tiles match the legend, start tile walkable, bidirectional door) via a small Python reproduction of `roomLayouts.test.ts`'s checks — same toolchain gap as the two entries above (`device_bash` down, cloud sandbox npm registry blocked), so this still needs a real `yarn test` run to confirm.
  - **Pentagon, Starbase and National Mall rooms** (`data/roomLayouts.ts`): three more Oval Office hub spokes, each a small room around the one action already tagged with that room in `data/actions.ts` — Pentagon → Rename a Department, Starbase → Fire a Rocket, National Mall → Military Parade. Same pattern as Rose Garden: a new door on the Oval Office's wall (north at `{x:21,y:0}` for Pentagon, west at `{x:0,y:2}` for Starbase, south at `{x:22,y:10}` for National Mall — each on a wall that already had a door to a different room, so they share the wall but not the tile) and a matching door back on the new room's *opposite* wall, following the convention every prior spoke already used (exit the hub via wall W, enter the spoke via its own opposite wall). The Oval Office now has 8 doors total. This makes every `RoomId` walkable except `mapRoom`, which GAME_PLAN §10 deliberately keeps as the full-screen World Map status-board menu rather than a room — so this closes out "new room + its actions" as a category for Phase 5; anything still in the toolbar-only catalog from here on is there because it doesn't have a room yet in §10 either (e.g. `un_speech`, which the code already notes has no built UN room).
  - `data/roomLayouts.test.ts`: generalized the hub-spoke test to loop over all 7 spokes (was Rose Garden alone) plus an explicit door-count assertion on the Oval Office, and rewrote `hasRoomLayout`'s test to assert it's true for every `RoomId` except `mapRoom` rather than a hand-maintained true/false list.
  - Verified the same way as Rose Garden — a full Python reproduction of every assertion in `roomLayouts.test.ts` run against the final file content, all passing — since `device_bash` is still down and the cloud sandbox's npm registry access is still blocked. Still needs a real `yarn test` run to confirm.
  - **This closes out "add a room to an existing `RoomId`" as a task** — `engine/actions.ts`'s `RoomId` union only ever had these 10 values, and 9 of them are now walkable (`mapRoom` deliberately isn't). Capitol, Mar-a-Lago, the UN and Greenland from §10 are each a bigger lift than a room: none of them are `RoomId` values yet, and none of the actions that belong in them exist yet either (Government Shutdown/Pass a Big Beautiful Bill for Capitol, Launch a Memecoin/Strategic Crypto Reserve/Pay the Debt in Dogecoin for Mar-a-Lago, a real UN room for `un_speech` — currently tagged `room: 'mapRoom'` as a stand-in, per that action's own code comment — and Greenland is explicitly "unlockable... only after a successful purchase" so it needs that unlock condition wired up too, not just a grid). So the next room-shaped task in Phase 5 is really "extend `RoomId`, add the missing actions, then add the room" together, not a room in isolation.
  - Next up in Phase 5, roughly in order of self-containedness: round out the action catalog toward 32 (including the Capitol/Mar-a-Lago actions above, which would unlock those two rooms as a natural follow-on), the two remaining showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, ending art, full French translation, and difficulty modes.
  - **"Do them all" pass — action catalog, Capitol/Mar-a-Lago, both remaining showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, difficulty modes:** closes out nearly everything left in Phase 5 except ending art (no image-generation tool available in this environment) and three explicitly deferred lower-priority items — a Fire the Fed Chair showdown, an Impeachment Senate-trial showdown, and a real UN room (`un_speech` still stands in on `mapRoom`, per its own code comment).
    - **Action catalog → 37 actions** (`data/actions.ts`): added the 14 remaining §9 rows plus Elon's reconciliation action (15 total) — 90-Day Pause, Liberation Day (`oncePerRun`), Tariff Dividend, Launch a Memecoin, Strategic Crypto Reserve, Pay the Debt in Dogecoin, Sell Gold Cards, Build a Ballroom, Golden Dome, Post at 3 AM (`oncePerMonth`), Government Shutdown, Pass the Big Beautiful Bill, Executive Order Spree, Hire an Adult in the Room, and Patch Things Up With Elon. Every simplified mechanic (no per-tariff pending-effect tracking, no tariff-revenue ledger, no recurring monthly crypto swing, no Federal Judge injunction system, no randomized 1–3 month delay) is documented inline at the action that needed it, matching the precedent already set by `flip`/`vanish`/`un_speech`. `impose_tariff` gained a small `rage` effect it was missing (GAME_PLAN §8 lists tariffs as one of two things that fill Elon's meter; the doc comment on the new Liberation Day action already assumed it had one).
    - **Golden Dome and the two EA-modifying flags are real mechanics, not flavor:** `engine/effects.ts`'s `applyEffect` 'delta' case now intercepts and consumes a `goldenDomeActive` flag against the next negative `defcon` delta from any source (action, event, or battle loss); `engine/economy.ts`'s new `resolveActionsLeft` (called from `tickMonth`) caps the next month to 1 EA for `shutdownPending` or grants +2 EA for `executiveOrderBonus`, with a shutdown winning if a month somehow ends with both flags set.
    - **Elon's Rage Quit meter** (GAME_PLAN §8): `GameState` gained `elonRage: number` (0–100); the `rage` `Effect` kind (previously an intentional no-op stub) now actually adds to it, clamped; a new `unflag` `Effect` kind (`{kind:'unflag', clear: string}`) is the DSL's only way to remove a flag, used by the reconciliation action; `engine/effects.ts`'s new `checkElonRage` sets `elonLeftCabinet` once the meter hits 100 (idempotent, called at the tail of both `resolve.ts`'s `finalizeAction` and `events.ts`'s `rollMonthlyEvent`, the same way both already call `checkEnding`). `launch_rocket`/`doge_chainsaw` (Elon's own actions) gained `requires.notFlags: ['elonLeftCabinet']` so he can't act while gone. `data/events.ts` gained a `sabotage?: boolean` field and three sabotage events (SpaceX Pulls Out of a Federal Contract, Starlink "Mysteriously" Goes Dark, DOGE's Old Access Leaks Everything, 23 events total); `eligibleEvents` now returns *only* sabotage events while `elonLeftCabinet` is set and never returns them otherwise. `engine/actions.ts`'s `ActionRequirements` gained `flags`/`notFlags` (all-must-be-set / none-must-be-set) and `ActionLimit` gained `'oncePerRun'`, both used by the reconciliation action and Liberation Day respectively; `checkAvailability` enforces both, with a new `'flags'` `UnavailableReason`.
    - **Capitol and Mar-a-Lago rooms** (`data/roomLayouts.ts`): `RoomId` extended with `'capitol'`/`'marALago'`; two more Oval Office doors (north wall at `{x:25,y:0}` for Capitol, east wall at `{x:29,y:7}` for Mar-a-Lago — the Oval Office has 10 doors now) with matching return doors on each new room's opposite wall, same convention as every prior spoke. Capitol hosts Government Shutdown and Pass the Big Beautiful Bill; Mar-a-Lago hosts Launch a Memecoin.
    - **Buy a Country and Pass the Big Beautiful Bill showdowns** (`data/showdowns.ts`): the two remaining §17 showdowns, same 3-round/3-choice shape as Fed and Press Conference, wired automatically since `store/gameStore.ts`'s resolution flow already looks up a showdown by `actionId` generically rather than hard-coding the first two.
    - **Melania's hide-and-seek** (GAME_PLAN §8, store-level, not engine — it's UI/session flavor, not a deterministic rule): `gameStore.ts` gained `melaniaRoomId`/`melaniaFoundThisMonth`, re-rolled every `endMonth` with plain `Math.random()` (same precedent as the store's existing `randomSeed()`); `movePlayer` grants +1 EA (via a new pure `engine/economy.ts` helper, `grantBonusAction`) the first time a door transition lands the party in that month's room, surfaced through the existing news-ticker mechanism (`lastEventNameKey: 'melania.found'`) rather than a new toast component. Persisted in `SavedGame` with a fallback for older saves.
    - **Difficulty modes** (`data/balance.ts`): `DifficultyId` (`'intern' | 'normal' | 'thirdTerm'`) and `BALANCES`, each a handful of knobs nudged off the existing `BALANCE` (more/fewer EA per month, gentler/harsher debt and inflation formulas, more/less forgiving ending thresholds) rather than a parallel set of constants — `normal` *is* `BALANCE` unchanged. This was heavily de-risked by `Balance` already being threaded as an optional parameter through every engine entry point since Phase 1; the actual work was mostly `gameStore.ts` now resolving and passing the chosen `Balance` explicitly everywhere it used to rely on the default, plus a `difficulty` store field (persisted in `SavedGame`) and a title-screen picker (`TitleScreen.tsx`, `difficulty-picker` in `styles/title.css`). "Press Start" (the no-save path) now calls `newGame()` instead of `startGame()`, since the pre-built initial game state can't know a difficulty picked after the store was created.
    - **Test coverage added/fixed alongside every change above** (`CLAUDE.md` rule 9): `data/actions.test.ts` (count → 37, `truth_post_3am` added to the `oncePerMonth` list), `data/roomLayouts.test.ts` (Capitol/Mar-a-Lago added to the spoke loop and door count), `data/showdowns.test.ts` (all four showdown actionIds), `data/events.test.ts` (count → 23, sabotage-gating tests), `engine/events.test.ts` (sabotage-only-while-enraged tests), `engine/economy.test.ts` (new describe block for the shutdown/bonus EA refill), `engine/preview.test.ts`/`engine/effects.test.ts` (see the two real bugs below). No new tests were added directly for `store/gameStore.ts` — the store already has no test file (no React/hook test setup in this repo, per the existing precedent noted under the player-depth fix above) — but its logic is a thin, mostly type-checked pass-through over the now-tested engine/data functions.
    - **Two real bugs found and fixed while verifying this pass**, both from `elonRage`/`unflag` work done earlier in the same session, before the rest of the catalog existed to exercise them: `engine/effects.test.ts`'s `CTX()` test fixture was missing the now-required `elonRage` field (a compile error), and its `'applyEffect: rage'` test still asserted the *old* no-op stub behavior instead of the real one — both fixed, with new coverage added for `rage`'s accumulation/clamping, `unflag`, and `checkElonRage` itself, plus Golden Dome's DEFCON-absorption. Separately, `engine/preview.ts`'s `summarizeEffects` `switch` wasn't updated for the new `'unflag'` effect kind, which meant it silently fell through (a real compile-time exhaustiveness error) instead of treating it like `flag`/`rage` (no preview-visible effect) — fixed, with a test added.
    - **Verification method:** `device_bash` (the user's device shell) has been down since a Windows update on September 8, and this cloud sandbox's own npm registry access is blocked (`403 Forbidden`), so `yarn test`/`yarn verify` still could not be run for real — same gap as every entry above since the player-depth fix. This pass went further than the usual hand-traced Python reproduction, though: a global `tsc` binary (v6.0.3) happened to be available in the sandbox outside the project's own `node_modules`-less install, so every changed file was actually type-checked against a throwaway config with hand-written ambient stubs for `react`/`react-dom`/`zustand`/`vitest`/`@fontsource/press-start-2p` (real logic and imports fully checked; only those five packages' own type shapes are faked as `any`, which is what produces expected noise — implicit-`any` callback params, JSX `key`-prop mismatches, "cannot find module" for those five bare specifiers — that isn't a real bug). That check is what caught both bugs above. It is not a substitute for a real `yarn verify` run (JSX prop-shape checking against real React types, ESLint, Prettier, and actually *running* the test suite are all still unverified), so one is still needed to fully close this out.
    - Not done, by design: ending art (needs an image-generation tool this environment doesn't have), a Fire the Fed Chair showdown, an Impeachment Senate-trial showdown, and a real UN room — all explicitly deferred as lower-priority given the size of everything above.
  - **Closing the showdown count to 6/6** (`data/showdowns.ts`, `data/actions.ts`): re-checked Phase 5 against GAME_PLAN §17's own completion line item-by-item and found it was 2 showdowns short of "6 showdowns" despite the "two remaining showdowns" bullet above already being marked done (that bullet closed Buy a Country/Bill, not Fed Chair/Impeachment — a naming mixup in the earlier pass). Fixed properly this time:
    - **Fire the Fed Chair showdown** (`fedFiring`, paired with the existing `fire_fed_chair` action, which had no showdown until now): same 3-round/3-choice shape as the rest.
    - **Impeachment Senate-trial showdown** (`impeachment`, paired with a new action, `senate_trial`, action #38): there was no action to pair a trial showdown with — Impeachment has only ever been a `checkEnding` game-over condition (`lowHappinessStreak` vs. `impeachmentStreakMonths`, post-midterms), not something the player does anything about. Added `senate_trial` (Capitol, Trump, 2 EA, `oncePerMonth`, `requires.flags: ['congressLost']`) so there's something to survive: success is a flat +15 Happiness (there's no "reset the streak" `Effect` kind, so this just leans on `checkEnding`'s own logic — big enough Happiness reliably pushes back above `impeachmentHappiness` for the month, which is what actually breaks the streak); failure is -10 Happiness, i.e. leaning into it instead. `engine/economy.ts`'s `tickMonth` now adds a `'congressLost'` flag to `state.flags` the one month `congressLost` first flips true (it was already a `GameState` boolean, just never mirrored into `flags`, which is what `ActionRequirements.flags` actually reads) — a one-line, idempotent addition alongside the existing `resolveActionsLeft` flags plumbing.
    - Both showdowns needed no new UI or store code — `store/gameStore.ts`'s resolution flow and `ResolutionOverlay.tsx` already key off `getShowdownForAction(actionId)` generically, same as the Buy a Country/Bill pair before them.
    - Action catalog is now 38 (`data/actions.test.ts` updated); `data/showdowns.test.ts` now asserts all 6 actionIds. i18n: 1 new action name + 24 new showdown dialogue keys (12 per showdown) in both `en.ts`/`fr.ts` — 308 keys each, parity confirmed by script (no real `vitest` run possible here either, same toolchain gap as below).
    - New tests: `engine/economy.test.ts` (3 new cases for the `congressLost` flag: set on loss, not set when Congress is kept, never duplicated on a later tick), `data/actions.test.ts` (`senate_trial` is gated on `congressLost` and lives in `capitol`).
    - **Verification:** same toolchain gap as every entry above (`device_bash` still down as of this pass — re-tested, same Windows-update error — and the cloud sandbox's npm registry access is still blocked), but this time verified against a real `tsc` (v6.0.3) run over `engine`/`data`/`i18n` using the same throwaway harness/stubs built during the pass above, copied fresh from the current source: clean other than one pre-existing, unrelated stub-noise error (`expect.arrayContaining` isn't typed on the hand-written `vitest` stub's `expect`, a limitation of the stub itself, not the code — same category of noise documented above). Still needs a real `yarn test`/`yarn verify` run to fully confirm.
    - **This closes Phase 5's showdown-count and action-count line items.** The two items marked "not done, by design" two bullets up are unchanged and still open: ending art (needs image generation) and a real UN room (`un_speech` still stands in on `mapRoom`). Per user direction, these are accepted as permanent, environment-driven exceptions rather than blockers, and the project is moving on to Phase 6.
- [x] **Phase 5 — Content:** closed out except for the two accepted exceptions noted directly above (ending art, a real UN room) — every other §17 target (38 actions vs. a 32 target, 6/6 showdowns, Elon's Rage Quit arc, Melania's hide-and-seek, difficulty modes, full French translation, all `RoomId`s except the deliberately-menu `mapRoom` walkable) is done.
- [x] **UX fixes from first real playtest feedback (pre-Phase 6):** three issues reported directly against the running build, not the code:
  - **Removed the always-on ambient hum:** `ui/audio/sfx.ts`'s `startAmbientHum`/`stopAmbientHum` were wired into `App.tsx` to auto-start the moment the room screen mounted, with no way to stop just that drone short of muting all sound (SFX included) — reported as "the background sound non-stop." Fix: dropped the `useEffect` in `App.tsx` that called them; the functions themselves are left in `sfx.ts`, fully working and documented as currently unused, for whenever real ambient music (or a separate music-only toggle) replaces this placeholder. SFX jingles (blips, confirm/cancel, success/fail, headline) are untouched and still play normally.
  - **Moved the mute/language toggles onto the news ticker:** `styles/global.css`'s `.lang-toggle`/`.mute-toggle` used to float just above the ticker (`bottom: calc(var(--ticker-height) + 6px)`), which put them directly over the room screen's toolbar — specifically its rightmost "End the Month" button, as seen in the reported screenshot. Moved both to `bottom: 0` (i.e. actually on top of the ticker strip, per the request to put them "over the news feed") with `z-index: 1` so they paint above the ticker `<footer>` despite coming earlier in `App.tsx`'s DOM order, and shrank their padding to fit the ticker's 14px height.
  - **Cross menu (`ui/menus/CrossMenu.tsx`) now uses icons instead of text, smaller buttons:** GAME_PLAN §11 always described this as "four icons in a diamond" — the DECREE/TALK/ITEM/SWITCH text labels were a placeholder (same category as the mute button's own emoji, which already set this precedent), and long French labels (`ÉCHANGER`, `DÉCRET`) were overflowing/clipping inside the grid cells regardless. Replaced with 📜 (decree) / 💬 (talk) / 🎒 (item) / 🔄 (switch), each `aria-hidden`, with the real accessible label moved to `aria-label`/`title` on the button (same i18n keys the text used to render, so translations are unaffected). `styles/room.css`'s `.cross-menu` grid shrank from 40×16px cells to 18×16px now that there's no label-width math to satisfy.
  - **Verification:** same toolchain gap as always (`device_bash` still down, re-confirmed again this pass; cloud sandbox npm registry still blocked) — but this time checked with `tsc` against the *full* `src` tree including all UI/React components (previous passes' harness only covered `engine`/`data`/`i18n`), using the same hand-written stubs. Zero new errors from any of the five changed files (`App.tsx`, `CrossMenu.tsx`, `sfx.ts`, plus the two CSS files, which `tsc` doesn't check); every error the harness reported is pre-existing stub incompleteness in files this pass didn't touch (missing `src`/`disabled`/`role`/`ref`/etc. props on the hand-rolled JSX intrinsic-element stub, missing `useRef`/`CSSProperties`/`KeyboardEvent`/`RefObject` on the `react` stub, missing `it.each` on the `vitest` stub). Still needs a real `yarn verify`/`yarn dev` look to confirm the on-screen layout and that the hum is actually gone.
- [x] **Real world-map art, and picking your own Declare War target (more playtest feedback):**
  - **World Map background image:** the user supplied a real equirectangular world-map PNG (`public/assets/maps/world-map.png`, 1013×380, re-encoded RGB/no-alpha to shrink it ~30%) to replace `.map-world`'s flat navy rectangle. `styles/room.css`'s `.map-world` now sets it as a `background` with `background-size: 100% 100%` — a deliberate stretch rather than `cover`/`contain`, since `data/countries.ts`'s `mapX`/`mapY` are plain percentages of the box and stretching is what keeps "38% across" landing on the same spot on the image regardless of the flex-sized box's actual aspect ratio. All 7 `mapX`/`mapY` pairs were re-tuned by eye against this specific image (sampled it programmatically — a coarse land/ocean/ice color-classification grid — to locate each landmass rather than guessing) so the markers now sit roughly on the right country instead of on arbitrary placeholder coordinates; the doc comment on `CountryDef` says plainly these are "tuned by eye … not surveyed coordinates."
  - **Target selection for Declare War** (real user feedback: "you should be able to select your country to attack"): `declare_war` used to auto-pick a random eligible country the instant the preview was confirmed (`pickRandomCountry` in the old `confirmPreview`). Replaced with a real choice:
    - New `ResolutionPhase`, `'selectCountry'`, sits between `'preview'` and `'battle'`. `confirmPreview` now checks whether any war-eligible, not-already-at-war country exists and, if so, moves to `'selectCountry'` instead of picking one — the "ran out of countries" fallback (straight to the ordinary roll) is unchanged.
    - New store action, `selectBattleCountry(country)`: validates the pick is actually eligible (last-line sanity check, same pattern as `checkAvailability`), then does exactly what the old random-pick branch did — builds the rosters, starts the battle — just with the chosen country instead of an RNG draw.
    - New component, `ui/screens/SelectCountryScreen.tsx`: reuses the World Map's own `.map-world`/`.map-country` markup and the new background image, but only plots the currently-eligible countries, each as a real `<button>` (new `.map-country-button` CSS modifier for the hover/focus affordance a `<span>` didn't need) that calls `selectBattleCountry`. Wired into `ResolutionOverlay.tsx` as a new phase branch, same pattern as the existing `'battle'` → `<BattleView />` branch.
    - The preview screen's copy adjusted to match: the generic "targets a random country" note (`resolution.randomCountryNote`) is now suppressed for battle-gated actions specifically (still shown for Buy a Country, which is still a random pick — this request was about Declare War only) in favor of a new note, `battle.selectCountryNote` ("Pick who's about to have a very bad day."), reused as the new screen's own subtitle.
  - **Verification:** same toolchain gap as always (`device_bash` still down, re-confirmed again). `tsc` over the full `src` tree: zero errors from any of the changed/new files (`data/countries.ts`, `store/gameStore.ts`, `ui/screens/ResolutionOverlay.tsx`, the new `ui/screens/SelectCountryScreen.tsx`) beyond the one already-documented, pre-existing stub gap (`role`/`aria-modal` not typed on the hand-rolled JSX intrinsics — the same gap `MonthEndReport.tsx`/`BattleView.tsx` already hit before this pass). i18n parity (310 keys, en/fr) confirmed by script. Still needs a real `yarn verify`/`yarn dev` look, especially to eyeball whether the re-tuned map markers actually land on the right countries on screen.
- [ ] **Phase 6 — Polish & release** (started):
  - **Balance pass with the simulator (GAME_PLAN §17's Phase 6 line item), plus a real verification-method upgrade:**
    - **Extracted `scripts/simulate.ts` from `scripts/simulate.test.ts`.** The documented folder structure (`CLAUDE.md`'s own Layout section, and GAME_PLAN's) has always named `scripts/simulate.ts` as the balance simulator, but the actual pure logic (`runOne`, `simulate`, `printReport`, `pickAction`, the `random`/`cautious` policies) had only ever lived inside the `.test.ts` file itself, with no plain script to run outside Vitest. Moved all of it to `simulate.ts` (now exported), added an `import.meta.url` entry-point guard so `tsx scripts/simulate.ts` prints the report with no test framework involved, and slimmed `simulate.test.ts` down to importing those exports and wrapping them in the two survival-rate assertions. `yarn simulate` (`vitest run scripts/simulate.test.ts`) behaves identically to before — same report, same assertions, just reading from the new file.
    - **This was actually run, for real, dynamically — a first for this entire session.** Every previous "verification" this session (going back to before this file's `[x]` Phase 1–4 entries) was either hand-traced or `tsc`-type-checked only, because `device_bash` (the user's device shell) has been down since a Windows update and this cloud sandbox's own npm registry access returns `403 Forbidden`, so `vitest` itself was never installable to actually *run* anything. What changed: this pass discovered a working `tsx` binary (v4.21.0) already present in the sandbox outside the blocked registry, plus a minimal hand-written `vitest` shim (`describe`/`it`/`expect` with real assertion logic, not just type stubs) built earlier in the session for the `tsc` type-check harness. `tsx --tsconfig tsconfig.app.json scripts/simulate.ts` resolves the real `@/` path alias and actually executes the real `data`/`engine` modules end to end. The shim was missing several matchers other test files use (`toHaveLength`, `toContainEqual`, `toBeUndefined`, `toBeDefined`, `toMatch`, `expect.arrayContaining`, `it.each`) — added all of them, then ran **every one of the 28 `*.test.ts` files in the project** (engine, data, i18n, and the one pure-logic `ui/` test file) this way: **269 assertions, 0 failures, 0 crashes.** This is a real dynamic pass over the actual behavior of everything built this session (the full Phase 5 content pass, the UX fixes, the world-map/target-selection feature, and this refactor itself), not just types lining up — the strongest confirmation available in this environment short of the user's own `yarn verify` run. It does **not** cover ESLint, Prettier, `tsc -b`'s project-reference build mode specifically, or anything React-component-rendering-shaped (this repo still has no React test setup, by longstanding precedent) — those still need a real `yarn verify` on a working toolchain.
    - **Balance findings, read honestly rather than tuned reflexively:** `random` policy survives 1.5% of 3000 runs (well under the test's `<20%` bound), losing mostly to `brainFreeze` (63% of its losses) — Party IQ crashing to the floor, because a random policy never specifically prioritizes `read_briefing` and most actions cost a little IQ as their joke ("dumber is stronger, until it isn't," GAME_PLAN §1). `cautious` policy (which explicitly reads a briefing under IQ 25) survives 71.5% of runs (inside the `>30%`/`<95%` bound) with 0% `brainFreeze` losses and its remaining losses almost all `bankruptcy` (24.3%). Read together, this isn't a balance bug: it's the IQ-management mechanic working exactly as designed — ignore it and it's the actual primary failure mode; manage it and Debt becomes the real long-term threat instead. No balance numbers were changed off the back of this pass; both existing bounds pass with real margin, and the shape of the failure distribution matches the game's own stated design intent. Worth a second look if a future pass changes IQ-cost or `brainFreezeIq` numbers, now that this can actually be re-run in under a minute.
  - **"Juice" — screen shake and animated tick-up numbers (GAME_PLAN §16/§17):**
    - **Screen shake:** `store/gameStore.ts` gained a `shakeSeq: number` counter (session/UI flourish, not part of `GameState`/`SavedGame` — same category as `screen`/`overlay`) and a `triggerShake()` action (`shakeSeq + 1`). It fires from two places: `ResolutionOverlay.tsx`'s result-reveal effect, on a **failed** action only (a win never shakes, so the shake itself reads as "that went badly," on top of the existing fail jingle); and `battleAttack`/`battleRunEnemyTurn` in the store, on a landed hit (the enemy-turn case checks the battle log actually grew, since `enemyTakeTurn` can just reposition without attacking). `ui/ScreenShake.tsx` (new) wraps the stage's contents, subscribes to `shakeSeq`, and restarts the CSS animation on every bump — including rapid re-triggers — with the standard remove-class/force-reflow/re-add-class trick. `App.tsx` now wraps `Hud`/`CurrentScreen`/`LangToggle`/`MuteToggle`/`Ticker` in it, inside `.stage` rather than on `.stage` itself, so the shake's `transform` never fights `.stage`'s own zoom-scale `transform`. New CSS in `global.css`: `.stage-shake-layer`/`.stage-shake`/`@keyframes stage-shake` (a 350ms rattle). No extra `prefers-reduced-motion` rule was needed — the existing global rule (`animation: none !important`) already covers this new keyframe animation.
    - **Tick-up numbers:** `ui/useTickUp.ts` (new hook) animates a displayed number from `from` to `to` over 700ms via `requestAnimationFrame` with ease-out easing, snapping straight to the end value when `prefers-reduced-motion: reduce` is set (checked manually via `matchMedia`, since this is a JS-driven animation the CSS-only global rule can't reach) or when `from === to`. It returns the raw interpolated number so callers still run it through their normal formatter, so a mid-count frame reads with the right unit/rounding (e.g. "$41.3T" ticking up to "$42.0T") instead of a differently-formatted intermediate value. The month-end report and the action-result screen used to each have their own near-identical stat-table JSX; extracted into a shared `ui/screens/ReportStatsTable.tsx` (`ReportStatsTable`/`StatRow`) that calls `useTickUp` per row, now used by both `MonthEndReport.tsx` and `ResolutionOverlay.tsx` in place of the duplicated markup.
    - **Verification — a step up from the usual gap:** `device_bash` is still down (Windows-update issue, unchanged since Phase 5), so the device sync for this batch went through `SendUserFile` + `device_commit_files` directly, same as prior `device_bash`-outage work. But verification itself was stronger than most of this project's history: ran a real `tsc` project check over the full `src` tree (`tsconfig.verify-ui.json`) — every error reported was the same pre-existing hand-rolled-stub noise already documented above (`useRef`/`CSSProperties`/`KeyboardEvent`/`RefObject` missing from the `react` type stub; `ref`/`src`/`role`/`disabled`/`scope`/etc. missing from the JSX-intrinsics stub's narrow prop union), now also hit by the two new files (`ScreenShake.tsx`'s `ref`, `ReportStatsTable.tsx`'s `scope`) for the same reason every other component already hits it — no error outside that known category. Then, using this session's `tsx`-plus-vitest-shim breakthrough (first exercised for the balance-simulator entry above), **re-ran all 28 `*.test.ts` files for real**: 28/28 pass, 0 crashes — confirms the store/UI changes didn't regress any engine/data behavior (there's still no test file for `gameStore.ts` itself, or any React-rendering test, per this repo's long-standing no-React-test-setup precedent, so the shake/tick-up logic's own correctness was confirmed by code reading rather than an executed test). Still worth a real `yarn dev` look to eyeball the shake feel and the tick-up timing in the browser.
  - **Canada battle roster art refresh + Jagmeet Singh added (7th unit):** the user supplied 4 new individual reference sheets (Mark Carney, Justin Trudeau, Jagmeet Singh, Dominic LeBlanc — `public/assets/sprites/source/canada-*-sheet(-v2).png`), each 4 character poses over a transparent background with a text-label caption baked in below them. Per the user's own note, poses 1–2 are the same "front" direction and poses 3–4 are the same "back" direction (not four distinct directions like the US sheet's diagonal convention) — since `data/battleRosters.ts` only ever renders a single static `sprite` per battle unit (battle units aren't direction-aware like switchable officials, confirmed by reading `engine/battle.ts`'s `BattleUnitTemplate` and `ui/battle/BattleView.tsx`), only the first ("front") pose from each sheet was cropped and used; `-back.png`/`-left.png`/`-right.png` weren't regenerated for the 3 existing characters since the game never reads them regardless of source art vintage.
    - **Cropping:** an alpha-channel bounding-box scan (numpy, same family of technique as the Phase-4 US-sheet method) found each sheet's 4 pose columns and separated the sprite row-band from the caption text row-band beneath it; the first pose column was cropped tight to its own content with a few pixels of padding. Output sizes (~99–104px × 239–249px) landed close to the existing Canada sprites' own range (90–111 × 239–247) without any deliberate resizing — both the roster-swatch and on-field battle sprite are rendered with CSS `object-fit` (`cover`/`contain`), so exact pixel dimensions don't need to match anyway.
    - **Replaced** `public/assets/sprites/canada/carney-front.png`, `trudeau-front.png`, `leblanc-front.png` in place (same filenames, new art). **Added** `singh-front.png` plus a new roster entry, `canada-singh` (`data/battleRosters.ts`, between LeBlanc and the fictional wildcard), and its i18n name key (`battle.unit.canada.singh`, en/fr — 311 keys each, parity confirmed by script).
    - **Roster size went from 6 to 7**, which the battle system had a real hard cap on: `data/battlefield.ts`'s `ENEMY_SPAWN_POSITIONS` had exactly 6 slots, sized specifically because "Canada's roster has exactly 6" (its own prior doc comment). Asked the user whether Singh should replace the fictional "Guy in a Mackinaw Jacket" wildcard (keeping the roster at 6, no battlefield change) or join as a genuine 7th unit — they chose **expand to 7**. Added a 7th `ENEMY_SPAWN_POSITIONS` tile at `{x:8, y:3}` (one column back from the existing `x:9` column, which already spans the field's full open height; confirmed clear of the `x:5` obstacles and every other spawn tile by the updated `battlefield.test.ts` assertions actually running). Updated both doc comments (`battlefield.ts`, `battleRosters.ts`) accordingly.
    - **Test coverage updated to match:** `battleRosters.test.ts` (`getBattleRoster('canada')` length 6→7, enemy-capacity ceiling 6→7 slots), `battlefield.test.ts` (comment + `ENEMY_SPAWN_POSITIONS.length` bound 6→7 — this also re-validates, dynamically, that all 7 enemy spawns including the new one are on open floor and that no two spawns collide, since that's exactly what the existing "on open floor" / "unique, non-overlapping" tests already check generically over the full array).
    - **Verification:** `device_bash` still down (same Windows-update outage, re-confirmed), so this batch was synced via `SendUserFile` + `device_commit_files` again. But both checks that matter most here actually ran for real: `tsc` over the full `src` tree showed zero new errors from any of the six changed/added TS files (identical error list, byte-for-byte, to the pre-change baseline — confirmed by diffing the two `tsc` runs directly rather than eyeballing); and, more importantly for this change specifically (a spawn-grid collision and an array-length invariant are exactly the kind of thing static types can't catch), **the real dynamic `tsx` test run** (all 28 `*.test.ts` files) passed 28/28, including the updated `battlefield.test.ts`/`battleRosters.test.ts` assertions that directly exercise the new spawn tile and the 7-unit roster. i18n key-parity (311/311) confirmed by script. Still worth a real `yarn dev` look to confirm Singh's sprite renders and positions correctly on the battlefield's new 7th spawn tile in an actual Declare-War-on-Canada battle.
  - **HP/MP and per-unit battle quirks (user feedback: "It would be nice to see HP (Health) and MP (Magic)", plus 8 specific per-character requests):** the biggest engine change to `engine/battle.ts` since it was first built — every attack now runs through a small quirk pipeline instead of a flat "attacker hits defender" roll. Asked the user two clarifying questions first (both are genuinely game-design decisions, not something to guess at): whether Carney's "Charm magic" and Melania's "Sorcerer magic" should be a real player-facing cast action (new UI, target-picker) or an automatic proc like everyone else's quirk — they chose **automatic proc**, so there's no new interactive battle action anywhere; and what the two spells should actually do — they picked **Charm = flips the target to attack their own side for one hit, Sorcerer = a lingering power-halving curse instead of direct damage** (not the other two options offered: skip-a-turn/big-nuke, or self-heal/ally-shield).
    - **The requested roster:** Trudeau (Low HP, heals when hit by a woman — the only woman who can ever hit him is Melania, `isWoman: true`), Carney (High HP + Charm magic), the "Random guy" wildcard/Guy in a Mackinaw Jacket (Really High HP, above Carney's), Miller (50% chance his attack backstabs a random living Canada teammate instead), LeBlanc (50% chance any incoming hit just whiffs — a dodge), Trump (60% chance he flees his own attack instead of landing it — "escaping like a coward" read as backing out on offense, distinct from LeBlanc's defensive dodge), Vance (20% chance his attack redirects onto himself), Melania (Sorcerer magic). `data/battleRosters.ts` doc comment explains each choice, including the HP tier ordering (Trudeau 7 < roster baseline ~10 < Carney 18 < wildcard 24).
    - **`engine/battle.ts`'s `attack()`** is now the single place every quirk resolves (used by both the player's click and `enemyTakeTurn`'s AI, so nothing needed to change at either call site): a charged caster's magic is checked first (cast instead of attacking), then flee, then self-hit-or-backstab (attacker-side redirects — mutually exclusive per unit), then dodge (defender-side, checked against whoever the final defender ends up being), then the heal-vs-damage check, in that order. `BattleUnitTemplate` gained optional `fleeChance`/`selfHitChance`/`backstabChance`/`dodgeChance`/`isWoman`/`healsFromWomen`/`magic` fields; `BattleUnit` gained a runtime `magicCharge: number` (0 initially, +1 every one of that unit's own turns it doesn't cast, reset to 0 on a cast — a simple MP pool with no separate regen-rate config, `magic.max` doubling as both the pool size and the charge-up time). Carney's Charm (`castCharm`) resolves the "target fights for Carney's side for one turn" idea **immediately**, as one forced friendly-fire hit against a random living ally of the target's own side, rather than tracking a "skip your real turn" status across future turns — simpler to implement, and reads just as clearly in the log. Melania's Sorcerer (`castCurse`) permanently halves the target's `power` (floored at 1, stacks diminishingly if cast again), no direct damage.
    - **`BattleLogEntry` is now a 5-kind union** (`'attack'` — now with an optional `healed` flag — plus new `'flee'`/`'dodge'`/`'charm'`/`'curse'` kinds, every kind sharing a common `attackerId`/`defenderId` base so existing generic log-reading code didn't need touching). `ui/battle/BattleView.tsx` gained a `battleLogText` function that switches on `entry.kind` to phrase each one correctly (new i18n keys: `battle.log.heal`/`flee`/`dodge`/`charm`/`charmHit`/`charmNoTarget`/`curse`, en/fr — 320 keys each, parity confirmed by script), replacing the old inline "always an attack" JSX.
    - **UI — actually seeing HP and MP:** `UnitRow` now shows the current HP number next to the existing composure bar (previously just an unlabeled bar) and, only for Carney/Melania, a row of MP pips (filled left-to-right as `magicCharge` builds toward `magic.max`) underneath — a plain bar was considered for MP too but pips read better at this pixel scale for a 0–3 pool, and skip the layout cost of a second bar on the 11 units that don't have one. Both stat rows carry a `title` attribute with the exact numbers for anyone who hovers. New CSS: `.battle-stat-col`/`.battle-stat-row`/`.battle-stat-value`/`.battle-magic-pips`/`.battle-magic-pip`.
    - **Test coverage:** `engine/battle.test.ts` gained two new `describe` blocks (`'attack quirks'`, `'attack magic'`) covering every quirk and both spells individually, plus the "no living ally/teammate to redirect to" fallback cases and the "hasn't charged yet, chance is never even checked" case — every probabilistic test cranks its chance to exactly 1 (or 0) rather than hunting for a seed, same determinism trick this file already used (`power: 99`) for guaranteed one-shots. Existing tests that read `entry.amount`/`entry.defeated` needed a small `asAttack()` type-narrowing helper added, since `attack()`'s return type is now the full `BattleLogEntry` union rather than always `BattleAttackLogEntry` — a real, intentional type change, not a workaround. `data/battleRosters.test.ts` gained a generic invariant test (every quirk/magic chance is a valid 0–1 probability, every magic pool's max is ≥0) plus a specific test asserting each of the 8 requested units actually got its requested quirk and the HP tiers are correctly ordered relative to each other.
    - **Verification:** `tsc` over the full `src` tree came back byte-identical (after accounting for shifted line numbers from the added code) to the pre-change baseline — zero new errors, only the same pre-existing stub-noise category documented throughout this file. The real dynamic `tsx` run of all 28 test files passed 28/28, including every new quirk/magic test actually executing and asserting real outcomes (not just type-checking) — this is exactly the kind of change (RNG branching, redirect logic, a spawn-adjacent stat pipeline) where that real-execution check matters most, not just a nice-to-have. i18n parity (320/320) confirmed by script. Still worth a real `yarn dev` look to see the MP pips and HP numbers render correctly at actual pixel scale, and to watch Carney/Melania's spells actually fire in a live battle.
  - **Fix — battle mode crashed on load (`Cannot read properties of undefined (reading 'replace')` in `translate.ts`, from `UnitRow`'s new HP-label `t()` call):** not a code bug — `src/i18n/en.ts`/`fr.ts` on the device were stale, missing `battle.unit.hpLabel` and several rounds of other recent keys (back to before Jagmeet Singh was even added), despite `device_commit_files` having reported success on every prior sync this session. Root cause never fully pinned down (`device_bash` is still down, so there's no way to inspect the device's filesystem/timestamps directly), but re-verified by staging the "synced" files back down and diffing byte-for-byte against the local copy, rather than trusting the tool's success response alone — the practice going forward for every device sync in this project, not just when something's reported broken. Found and fixed two real discrepancies this pass:
    - `en.ts`/`fr.ts`: genuinely stale on the device (missing everything from the Jagmeet Singh round onward) — re-sent and re-verified identical.
    - `styles/global.css`: the **local** copy was the stale one here, missing mobile/touch-polish CSS (`-webkit-tap-highlight-color`, a `touch-action: none` on `.viewport`, and a `@media (hover: hover) and (pointer: fine)`-scoped hover rule) that only existed on the device, from before this session's "juice" work — my `Read`-then-edit that added `.stage-shake-layer` had unknowingly started from an already-incomplete local file and clobbered the touch-polish CSS when it synced. Fixed by adopting the device's copy (a strict superset — it already had both the touch-polish rules and the shake CSS merged) as the new local source of truth instead of re-pushing the incomplete local version over it.
    - `CLAUDE.md` was also stale on the device (a very old, pre–Phase-5 snapshot) — re-pushed and re-verified identical.
    - Everything else checked in this pass (`engine/battle.ts`, `data/battleRosters.ts`/`.test.ts`, `data/battlefield.ts`/`.test.ts`, `engine/battle.test.ts`, `ui/battle/BattleView.tsx`, `styles/battle.css`, `App.tsx`, `store/gameStore.ts`, the "juice" UI files, and the 4 re-cropped Canada sprite PNGs) matched byte-for-byte between local and device, so this wasn't a systemic sync failure — just these specific files, for reasons still unclear.
  - **Battle music (user supplied `d:\tmp\battle01.mp3`, a real ~3-minute MP3, not a placeholder):** `ui/audio/sfx.ts` gains its one and only real-audio-file exception — `playBattleMusic()`/`stopBattleMusic()`, backed by a plain `HTMLAudioElement` (`loop = true`, `volume = 0.5`) rather than the module's usual synthesized Web Audio oscillators, since generating a 3-minute loop procedurally isn't reasonable. Otherwise a first-class citizen of the module: respects the same `muted` module flag (`setMuted` now also pauses/resumes `battleMusic` in place), and never throws over a playback failure — `.play()` rejections (autoplay policy edge cases) are swallowed exactly like `playSfx` already does. The asset lives at `public/assets/audio/battle01.mp3` (copied in as-is, no transcoding — already a reasonable 64kbps/48kHz stereo MP3).
    - **Lifecycle:** `ui/battle/BattleView.tsx` starts it in a mount-only `useEffect` (`playBattleMusic()`) with `stopBattleMusic()` as the cleanup — `BattleView` only ever mounts while `ResolutionOverlay` is showing the `'battle'` phase and unmounts the instant it isn't, so this plays the track for exactly a battle's duration regardless of whether the battle ends in a win, a loss, or the player backing out mid-fight; no separate win/lose-path handling needed.
    - **Verification:** this session's `node_modules` (including the hand-written `tsx`-runnable vitest shim used for real dynamic test execution) had been wiped since the last round — re-discovered a live copy still sitting in the scratchpad `verify` checkout's own `node_modules` and ran everything from there instead of trying to reinstall. `tsc` over the full `src` tree came back with only the same pre-existing stub-noise category documented throughout this file (nothing new from `sfx.ts` or the new import in `BattleView.tsx`); all 27 `*.test.ts` files ran for real via `tsx` and passed 27/27 (no test added specifically for the music feature itself — it's a browser-only `HTMLAudioElement` side effect with no pure-engine logic to unit-test, consistent with this repo's no-React-rendering-test precedent).
    - Synced `sfx.ts`, `BattleView.tsx`, and `battle01.mp3` to the device via `SendUserFile` + `device_commit_files`, then re-verified all three byte-for-byte via a fresh `device_stage_files` + diff — per the practice adopted in the sync-bug fix above, not just trusting the "written" response. All three matched exactly.
    - Not yet confirmed in a live `yarn dev` session: whether the browser actually allows autoplay on battle entry (depends on whether a user gesture already happened that tab) — if it doesn't play on the very first battle, a click anywhere first should unblock it, same as any other autoplay-gated audio.
  - **Fix — the box-shadow rectangle around each unit sprite (user: "The rectangle under the characters came back"), plus a country flag pastille (user: "put a pastille with the logo of the country under the character"):** `styles/battle.css`'s `.battle-unit-iso.is-us`/`.is-enemy`/`.is-current` drew a 1–2px `box-shadow` ring directly around the whole unit sprite to mark its side and whose turn it was — fine over the original colored-square placeholders, but a loud, distracting rectangle once real character art (Phase 4's US sheet, this session's Canada sheets) was sitting inside it. Removed entirely and replaced with two smaller, more legible signals:
    - **A flag pastille under each unit's feet:** a small 10px circle, ringed gold (`is-us`) or red (`is-enemy`) — same at-a-glance side information the rectangle gave, just contained to a badge instead of framing the whole sprite — showing a Unicode flag emoji as the placeholder "country logo" (no real crest/logo art exists yet, same placeholder spirit as the coloured-square sprites and the synthesized SFX). `data/countries.ts`'s `CountryDef` gained a `flag` field (one emoji per country, doc-commented as a placeholder for real art later) plus a standalone `US_FLAG` constant (the US is the player, never a `CountryId`, so it doesn't belong in `COUNTRIES`). `BattleView.tsx` picks `US_FLAG` for `side: 'us'` units and `getCountry(resolution.battleCountry).flag` for enemy units.
    - **A bobbing `▼` marker over the current unit's head** (Shining Force/Fire Emblem-style turn pointer, `@keyframes battle-unit-marker-bob`, already covered by the project's existing `prefers-reduced-motion: reduce` blanket disable) replacing the old cream box-shadow ring for "whose turn it is."
    - **`BattleUnitToken`** went from a single sprite element carrying all three box-shadow variants to a small wrapper (`.battle-unit-iso`, still the one absolutely-positioned/z-indexed element) containing the marker (conditional), the sprite/placeholder (now `.battle-unit-sprite`), and the flag pastille, stacked in a column — the sprite itself no longer carries any side-specific styling at all.
    - **Verification:** `tsc` over the full `src` tree came back with the exact same error count (45) as the pre-change baseline, only shifted line numbers from the new code — confirmed no new error category, not just eyeballed. All 27 `*.test.ts` files ran for real via `tsx` and passed 27/27, including a new `countries.test.ts` assertion that every country's `flag` is non-empty, unique, and distinct from `US_FLAG`. Synced `countries.ts`, `countries.test.ts`, `BattleView.tsx`, and `battle.css` to the device via `SendUserFile` + `device_commit_files`, then re-verified all four byte-for-byte via `device_stage_files` + diff. Still worth a real `yarn dev` look to see the pastilles and bobbing marker at actual pixel scale.
  - **Bigger battlefield with a scrolling Shining-Force-style camera (user: "we could make the battle much bigger and we could scroll the map like in Shining Force"):** asked two clarifying questions first, since this was a real architecture change to a screen whose doc comment explicitly said "never panned — a tactical battle shows the entire field at once" — whether the camera should only auto-follow the active unit each turn or also let the player manually pan, and whether the battle screen should go full-screen (map takes over, rosters become a compact overlay) or keep the existing 3-column layout just with a bigger center map. User picked **auto-follow + manual pan** and **full-screen**.
    - **`data/battlefield.ts` grew from 11×8 to 20×14** (280 tiles, up from 88) — big enough that the projected grid (544×294px) no longer fits in the battle viewport on either axis at this session's estimated viewport size, so there's genuinely something to scroll. The obstacle cluster grew from a 2-tile pair to a 6-tile plus-shape-plus-two-loners rather than just stretching the same two tiles across a much bigger open field, and `US_SPAWN_POSITIONS`/`ENEMY_SPAWN_POSITIONS` were re-centered on the new interior (rows 1–12) rather than reused verbatim from the old 8-row field. `battlefield.test.ts`'s assertions are fully generic (border walls, spawns on open floor, no spawn collisions, spawn count ≥ largest roster) so they re-validate the new layout for real rather than needing new hardcoded expectations.
    - **`ui/battle/battleCamera.ts`** (new, pure, DOM-free, fully unit-tested) holds the actual math: `centerOn(point, viewportSize)` computes the raw "put this point in the exact center" offset; `panCamera(camera, dx, dy)` adds a manual-pan delta; `clampCamera(camera, gridSize, viewportSize)` clamps per axis so the map can never scroll past showing blank space beyond its own edge — except an axis where the grid is smaller than the viewport, which centers instead of clamping to a min/max range (a clamp range would have min > max there). Same "pure logic file has its own real tests, the React hook wrapping it doesn't" split this project already uses for `room/playerDepth.ts` vs. `RoomView.tsx`'s `usePlayerDepth`.
    - **`ui/battle/useBattleCamera.ts`** (new) is the React glue: re-centers on the active unit whenever a `followKey` built from both the unit's id *and* its grid position changes (so moving the active unit mid-turn re-centers the camera on them again, not only a turn change), and exposes `pan(dx, dy)` for `BattleView.tsx` to call from keyboard/drag input. Manual pan during a turn is left alone by everything else — only a real turn/position change or the viewport's very first real measurement (`useElementSize`'s `ResizeObserver`, reused as-is from `RoomView.tsx` — no reason to write a second version) recenters it.
    - **`BattleView.tsx`**: the map now renders inside a `.battle-viewport` (fixed to whatever size `.battle-body`'s flex layout gives it, `overflow: hidden` — same shape as `RoomView.tsx`'s existing `.room-viewport`) with `.battle-grid-iso` translated by the camera offset inside it. Arrow keys pan by one tile (`PAN_STEP`, `KEY_PAN`) via a keydown handler on the focused viewport; a pointer drag pans continuously, converting client-px deltas to stage-px deltas by dividing by `useStageScale()` (otherwise a drag would scroll faster than the cursor at any zoom above 1×) and suppressing the click that would otherwise fire on whatever tile ends up under the pointer at release (`suppressClickRef` + a capture-phase click handler — without it, releasing a drag over a tile would accidentally move or attack with it). The camera glides on a 160ms CSS transition normally, overridden to instant (`0ms`, via `CAMERA_TRANSITION_MS`) while actively dragging so a hand-pan tracks the pointer 1:1 instead of easing behind it.
    - **Roster panels became compact overlays** (`.battle-roster-compact`, absolutely positioned in the top corners over the map, `.battle-roster-us`/`.battle-roster-enemy`) instead of full-height flex columns competing with the map for width — this is most of where "full-screen" actually comes from, since `.battle-body` was already the full remaining stage height either way; the map now gets nearly the whole area instead of a third of it.
    - **Verification:** `tsc` over the full `src` tree added new errors versus the prior baseline (30 vs. 45, since the file moved around, but every single one individually confirmed, line by line, to be the same already-documented hand-rolled-stub-gap category — `useRef`/`KeyboardEvent`/`CSSProperties`/`RefObject` missing from the `react` stub, now joined by `MouseEvent`/`PointerEvent` for the same reason, and `ref`/`src`/`role`/`disabled`/`scope` missing from the JSX-intrinsics stub, now joined by `tabIndex`/`onPointerDown`/`onPointerMove`/`onPointerUp`/`onPointerCancel`/`onClickCapture` — a small script paired every error with its continuation line and flagged anything outside that known set; it found nothing). One genuinely new issue the stub caught correctly: `style={{ transitionDuration: isDragging ? '0ms' : undefined }}` doesn't type-check against this stub's `Record<string, string | number>` (no `undefined`) even though real React's `CSSProperties` allows it — fixed by always providing a string (`CAMERA_TRANSITION_MS`) instead of relying on `undefined` to mean "unset." All 28 `*.test.ts` files ran for real via `tsx` and passed 28/28, including the 6 new `battleCamera.test.ts` cases (centering, panning, both clamp directions, the smaller-than-viewport centering case, and the exactly-equal case). Synced all 8 changed/added files (`BattleView.tsx`, `battleCamera.ts`, `battleCamera.test.ts`, `useBattleCamera.ts`, `battle.css`, `en.ts`, `fr.ts`, `battlefield.ts`) to the device via `SendUserFile` + `device_commit_files`, then re-verified every one byte-for-byte via `device_stage_files` + diff.
    - **Not yet confirmed in a live `yarn dev` session** (no real browser available in this sandbox): whether the estimated viewport size actually leaves enough room for a legible map at this pixel-art scale, whether the drag-vs-click threshold (`DRAG_CLICK_TOLERANCE`, 5px) feels right, and whether the compact roster overlays sitting over the map corners ever obscure a unit spawned near a top corner.
  - **Fix — drag-to-pan direction, plus reworking the pastille into an oval color badge (user: "The scroll is not working great. It should follow the mouse instead of following the isometric weird angles. Also, the pastille should be under the character like if it was sitting on it. You can give it [an] oval [shape] and a color."):** two real bugs/gaps from the previous round, both from an actual screenshot of the game running on the user's Windows machine.
    - **Drag-to-pan had the wrong sign.** `handleViewportPointerMove` panned the camera by `-dx/-dy` — scrollbar-style ("push the view", content moves opposite the cursor) — when a drag-to-pan gesture should be grab-style ("push the map itself", content moves *with* the cursor, so whatever point was under the cursor at pointerdown stays under it as it moves). Flipped to `pan(dx / scale, dy / scale)` (no sign flip). The user's "following the isometric weird angles" description was this same bug: panning opposite of the drag direction reads as extra disorienting on a diamond-tile isometric map versus a plain grid, not a separate isometric-specific bug — the camera's `translate()` is a plain 2D CSS transform with no rotation/skew of its own; only the individual tiles' static positions use the isometric projection formula.
    - **The flag emoji pastille rendered as plain text on Windows.** Confirmed by the user's own screenshot: 🇨🇦 showed as literal "CA" letters instead of a flag picture — Windows' emoji font deliberately renders many flag emoji as two-letter country-code text rather than a flag image (unlike macOS/Android), a known platform quirk, not a bug in this app's code. Per the user's own suggestion ("give it an oval and a color"), replaced the emoji entirely with a flat color: `data/countries.ts`'s `CountryDef.flag` field (and the `US_FLAG` constant) became `color`/`US_COLOR` — plain hex strings, same convention `battleRosters.ts`'s `placeholder.color` already uses, so no font/glyph support is needed at all. `countries.test.ts`'s flag-uniqueness test became a hex-color-validity + uniqueness test (`toMatch(/^#[0-9a-f]{6}$/i)`).
    - **The pastille is now a real oval** (`.battle-unit-flag`: 16×7px, `border-radius: 50%` on a non-square box renders an ellipse) filled with the unit's color, no text/emoji content at all — and it visually overlaps the sprite's bottom edge (`margin-top: -6px`) instead of sitting below it with a gap, so the character reads as standing on it rather than the two being stacked separately. Getting the *paint* order right (character in front of/on top of the oval) while still wanting the oval positioned *last* visually (at the feet) needed decoupling layout position from stacking order: `BattleUnitToken` renders the oval before the sprite in DOM order (so it paints underneath, following normal DOM-order stacking), and `battle.css` gives the sprite `order: 1` / the oval `order: 2` to move the oval below it visually — CSS `order` reorders flex layout position but not paint order, so this gets both "oval renders behind" and "oval sits at the feet" at once without a z-index hack.
    - **Verification:** `tsc` over the full `src` tree came back with the same 30 known-category errors as the prior round (script-verified line by line again, nothing new or unexpected). All 28 `*.test.ts` files ran for real via `tsx` and passed 28/28, including the updated `countries.test.ts` color assertions. Synced `countries.ts`, `countries.test.ts`, `BattleView.tsx`, and `battle.css` to the device — the first `device_commit_files` call reported success but all four files came back stale on re-staging (the same silent-write failure documented earlier this session, not a one-off); a retry of the same call and a fresh re-stage confirmed all four byte-for-byte.
    - **Still not confirmed in a live browser**: the exact drag feel (does 1:1 client-to-stage-px conversion feel right at every zoom level), and whether the oval's overlap amount (`-6px`) looks right against the actual sprite art's own transparent padding, which varies slightly per character.
  - Battle scrolling round 2 — tile clicks broken, auto-end-turn, pastille stacking (user feedback: "I cannot select a tile anymore. Also, when the player move, and cannot attack, it should end it's turn automatically. ... The pastille should be drawn before the character (zindex lower)"):
    - **Tile-click regression**: real bug — the drag-pan handlers called `setPointerCapture` and started panning on the very first `pointermove`, even for a plain click with near-zero movement, which stole the click away from the tile buttons before it could fire. Fixed with a deferred-drag-start pattern in `BattleView.tsx`: `dragRef` now carries a `dragging: boolean` flag, and nothing drag-related (pointer capture, `setIsDragging`, click suppression) happens until the pointer has moved past `DRAG_CLICK_TOLERANCE` (5px) from its `pointerdown` origin. A plain click never crosses that threshold, so it's never touched by drag logic at all, and tile `onClick` fires normally again.
    - **Auto-end-turn after a move with no attack targets**: new behavior, not a bug fix — real friction the user called out ("it should end it's turn automatically"). `gameStore.ts`'s `battleMove` action now checks, right after a move actually happens, whether the mover has any `targetsInRange` left; if not, it calls `endTurn` (same as the manual "End Turn" button) instead of leaving the turn open. Attacking itself is still always a manual click via `battleAttack` — who to hit stays the player's choice, never auto-picked. No dedicated test was added for this store-level behavior, consistent with this project's standing precedent of not unit-testing the Zustand store layer directly and instead relying on `engine/battle.ts`'s own coverage of the underlying `moveUnit`/`targetsInRange`/`endTurn` functions it calls.
    - **Pastille painting on top of the character instead of underneath it**: real bug, and a mistaken fix from the previous round. The prior pass assumed CSS `order` only affects layout position and that paint order follows raw DOM order — so it put the oval first in the markup and used `order: 2`/`order: 1` to visually push the sprite above it, expecting the sprite (earlier in DOM order) to paint on top regardless. That assumption is wrong: per the Flexbox spec, flex items with `z-index: auto` (the default) paint in *order-modified document order*, so the oval — having the higher `order` value — was actually painting last, i.e. on top, the whole time. Fixed properly in `battle.css`/`BattleView.tsx`: the JSX went back to natural DOM order (turn marker, then sprite, then oval), and both elements got explicit, non-`auto` `z-index` values (`.battle-unit-sprite { z-index: 2 }`, `.battle-unit-flag { z-index: 1 }`), which opts them out of order-based stacking entirely and into normal numeric z-index comparison — the oval now reliably paints underneath the sprite at their overlap.
    - **Verification**: `tsc --project tsconfig.verify-ui.json` — 30 errors, all re-confirmed (via the same error-header/continuation-line pairing script used every round) to fall in the sandbox's known category (the hand-rolled `react`/JSX type shim's missing members — `useRef`, pointer/keyboard event types, `style: Record<string,...>` without `undefined`, etc.), none new. Full test suite (`tsx --tsconfig tsconfig.app.json` over all 28 `*.test.ts` files) — all passing, unchanged by this round since none of the three fixes touched pure `engine`/`data` logic covered by tests. Synced to the device (`BattleView.tsx`, `battle.css`, `gameStore.ts`) and confirmed byte-identical via re-stage-and-diff; as has now happened on every device sync this entire session, the first `device_commit_files` attempt for all three files came back reporting success but was stale on re-staging, and a retry with `force: true` fixed it on the second attempt.
    - **Still not confirmed in a live browser**: whether the click-vs-drag threshold (5px) feels right rather than occasionally eating a very fast intentional drag as a click, and whether auto-ending the turn immediately after a no-target move ever feels too abrupt (e.g. if the player wanted a beat to see the new position before the turn passes).
  - Next up in Phase 6: final art & audio (same image/audio-generation gap as Phase 5's ending art), playtests, and an itch.io web build (needs a working `yarn build` — currently blocked by the same toolchain gap as everything else, since bundling needs the real `vite`/`vitest` npm install this environment has never had).
